# WP #126 — Поиск, единая сортировка и мультивыбор в фильтрах (delivery + client)

- **OpenProject:** [WP #126](https://openproject.contenthunter.ru/work_packages/126) «Добавить поиск к фильтру по клиенту/проекту/телефону»
- **Дата:** 2026-05-25
- **Ветка:** `wp126-search-sort-filters`
- **Приоритет:** Низкий
- **Статус:** В разработке

## 1. Проблема и цель

Фильтры по клиенту/проекту/телефону в обоих фронтах — это голые нативные `<select>`. Из них вытекают три боли пользователя (из описания WP):

1. **Нет поиска.** Чтобы найти нужный проект/телефон в длинном списке, приходится глазами вычитывать весь список (нативный type-ahead неудобен и непредсказуем).
2. **Сортировка не единая.** Где-то опции в порядке прихода из API, где-то по алфавиту; на клиентском дашборде латиница (`Boris Kostin Real Estate`) и мусор (`--status`) перемешаны с кириллицей (см. скриншот img19).
3. **Нет мультивыбора.** Нельзя отфильтровать сразу по нескольким проектам/телефонам.

**Цель:** во всех перечисленных фильтрах — поиск по подстроке, единая алфавитная сортировка и мультивыбор там, где это осмысленно.

**Исключения из мультивыбора** (явно от пользователя): страницы «Уникализация» (`/client/schemes`) и «Мой пакет» (`/client/contract`) — там остаётся одиночный выбор.

## 2. Затронутые системы

Задача охватывает **два независимых фронта в разных репозиториях**:

| Фронт | URL | Репозиторий / чекаут | Стек |
|---|---|---|---|
| **delivery** (менеджерский) | delivery.contenthunter.ru | `GenGo2/delivery-contenthunter`; dev-чекаут `/home/claude-user/autowarm-testbench/`, прод `/root/.openclaw/workspace-genri/autowarm/` | vanilla JS, единый `public/index.html` (~14.4k строк), без сборки |
| **client** (кабинет клиента) | client.contenthunter.ru | `/home/claude-user/validator-contenthunter/` | Vue 3.4 + Tailwind 3.4 + Pinia, **без UI-библиотеки**, Vite-сборка |

Готового searchable / multi-select компонента нет ни там, ни там.

## 3. Решение по фазам

Реализация **последовательная, один общий spec — два плана/PR**. Сначала delivery целиком (Фаза 1, приоритет), потом client (Фаза 2).

- **Фаза 1 — delivery.** Виджет `makeSearchSelect()` + единая сортировка на всех фильтрах + клиентский мультивыбор; мультивыбор на «Планировщике» с правкой autowarm-бэкенда (серверная фильтрация). Один PR в `delivery-contenthunter`, деплой в прод.
- **Фаза 2 — client.** Компонент `SearchableSelect.vue` + замена 7 селекторов проекта + правки validator-бэкенда под список `project_id` для 5 страниц с мультивыбором. Отдельный PR.

Каждая фаза имеет свой implementation plan (следующий шаг — `writing-plans` для Фазы 1).

## 4. Архитектура: переиспользуемый searchable-dropdown

**Выбранный подход:** свой мини-виджет в каждом фронте (без новых зависимостей и без библиотек). Альтернативы (минимальный апгрейд нативного `<select>`; готовая либа типа Tom Select) отклонены: первая не даёт поиска (главное требование), вторая тянет зависимость в single-file vanilla и в «Vue без UI-либы».

### 4.1 Единое поведение (контракт виджета, общий для обоих фронтов)

- **Триггер** показывает состояние: `все` / имя выбранного / `выбрано: N`.
- **Всплывашка:** поле поиска (фильтр по подстроке, регистронезависимо) → прокручиваемый список опций.
  - **Single:** клик по опции выбирает и закрывает.
  - **Multi:** чекбоксы; строки-действия «Все» / «Сбросить»; **применение фильтра при закрытии всплывашки** (а не по каждому клику) — критично для серверных фильтров, чтобы не слать запрос на каждый чек.
- **Пин `все`/`Все проекты`** всегда первой строкой, не участвует в алфавитной сортировке.
- Закрытие по клику вне виджета и по Esc; доступность с клавиатуры — best-effort (стрелки/Enter), не блокер.

### 4.2 Правило сортировки (единое везде)

Чистая функция сравнения по отображаемому имени:

```
localeCompare(b, 'ru', { sensitivity: 'base', numeric: true })
```

- регистронезависимо; кириллица/латиница — естественный RU-порядок;
- `numeric: true` → телефоны и нумерованные паки сортируются «по-человечески» (№2 раньше №10);
- **без хостинга выбранного наверх** — чистый алфавит, предсказуемо (чинит мешанину из img19);
- сентинел `все` пинится первым отдельно от сортировки.

Эта функция выносится в тестируемый модуль в каждом фронте.

### 4.3 Семантика мультивыбора

Мультивыбор фильтра = **OR**: «показать строки по любому из выбранных значений». Пустой выбор = `все` (без фильтра). По месту фильтрации:

- **Клиентская фильтрация (без бэкенда):** строки уже на клиенте — фильтруем массив/Tabulator по `value ∈ selectedSet`.
- **Серверная фильтрация (нужен API под массив):** эндпоинт учится принимать список `project_id`. Обратная совместимость: одиночный `project_id` продолжает работать (старые вызовы не ломаются).

На клиентском кабинете мультивыбор означает **агрегацию данных по нескольким проектам** (аналитика/публикации/аккаунты суммируются по выбранному набору) — это сознательное изменение бэкенда, согласовано.

## 5. Фаза 1 — delivery (детально)

Файл: `public/index.html` (строки приблизительны — проверить при написании плана, чекаут мог сдвинуться).

### 5.1 Матрица страниц

| Страница | Маршрут | Фильтры | Мульти | Где фильтрует |
|---|---|---|---|---|
| Ручная выкладка | `#publishing/publishing-manual` | Тел.№, Проект, Пак | да | клиент (`mpqApply`) |
| Запланировано | `#publishing/publishing?sub=up:queue` | Проект | да | клиент (Tabulator) |
| Опубликовано | `#publishing/publishing?sub=up:tasks` | Проект | да | клиент (Tabulator) |
| Планировщик | `#publishing/publishing?sub=up:planner` | Проект | да | **сервер (autowarm)** |
| Дашборд выкладки | `#publishing/publishing-dashboard` | Проект | да | уточнить локус при плане |

(Фильтры «Платформы» и «Статус» на Ручной выкладке — уже мультичекбоксы; их не трогаем, но новый виджет должен сосуществовать с ними.)

### 5.2 Точки интеграции (по разведке)

- **Ручная выкладка:** `MPQ_COLS` (~12053–12064), рендер фильтра `mpqFilterCell()` (~12123–12154), применение `mpqApply()` (~12115–12121). Заменяем `select`-ветку рендера на виджет; в `mpqApply` фильтры по phone/project/pack становятся «значение ∈ множество».
- **Запланировано:** `#up-project-select` (~2430), наполнение `loadQueueProjects()` (~11363–11379), фильтр `upColFilter()` (~11350–11352) → `_upqTable.setFilter`; сброс `upClearFilters()` (~11354–11360). Мульти → кастомная Tabulator-filter-функция «project ∈ set».
- **Опубликовано:** `#upt-project-select` (~2502), `loadTasksProjects()` (~11232–11248), `uptColFilter()` (~11227–11229) → `_uptTable.setFilter`; `uptResetAndLoad()` (~11250–11253).
- **Планировщик:** `#planner-project-select` (~2468), `plannerInit()` (~10889–10898), `plannerLoad()` (~10920–10957). Список проектов из `/api/validator/projects`. Мульти → `plannerLoad` шлёт набор `project_id`; **autowarm-эндпоинт планировщика учится принимать список** (ниже).
- **Дашборд выкладки:** `#dash-filter-project` (~2312), наполнение (~11784–11792) из `/api/publish/queue/projects`, применение `_dashApplyFilters()` (~11944–11950).

### 5.3 Бэкенд delivery (autowarm)

Только эндпоинт «Планировщика». Он учится принимать несколько `project_id` (формат — повторяемый параметр или CSV, выбрать при плане), фильтруя по `IN (...)`. Одиночный `project_id` сохраняет поведение. Остальные delivery-фильтры — клиентские, бэкенд не трогаем.

### 5.4 Тестирование Фазы 1

Чистую логику (сравнитель сортировки, фильтр-предикат «значение ∈ множество», нормализация состояния выбора) выносим в тестируемые функции по образцу `public/mpq_pure.js`; покрываем `node --test`. UI-проводку — смоук на testbench вручную.

### 5.5 Деплой Фазы 1

Прод delivery — это `public/index.html` в `/root/.openclaw/workspace-genri/autowarm/` с auto-push git-hook. Изменение чисто фронтовое → отдельный kill-switch не нужен, но **обязателен смоук на testbench до прода**. PM2-рестарт не требуется (статический файл), но проверить отдачу нового `index.html`.

## 6. Фаза 2 — client (дизайн)

Новый компонент `frontend/src/components/SearchableSelect.vue` (Tailwind, без либ), props: `options`, `modelValue`, `multi`, `placeholder`, `labelKey`/`valueKey`. Внутри — общий контракт из §4.1 + сортировка из §4.2.

### 6.1 Матрица страниц

| Страница | Маршрут | Компонент | Мульти |
|---|---|---|---|
| Дашборд | `/dashboard` | `pages/client/ClientDashboard.vue` (селектор ~47–55, уже сорт `localeCompare('ru')` ~528, тумблер «Все проекты» ~43) | да |
| Публикации | `/client/publications` | `PublicationsPage.vue` (~6–12) | да |
| Аккаунты | `/accounts` | `AccountsPage.vue` (~6–12) | да |
| Аналитика | `/client/analytics` | `AnalyticsPage.vue` (~7–12) | да |
| Распаковка | `/client/brand` | `BrandPage.vue` (~7–12) | да |
| Уникализация | `/client/schemes` | `SchemesPage.vue` (~42–46) | **нет** |
| Мой пакет | `/client/contract` | `ContractPage.vue` (~6–11) | **нет** |

Источник списка проектов — `useProjectStore` / `/projects`. Селектор виден только admin/manager/producer (клиент видит свой проект).

### 6.2 Бэкенд client (validator)

Эндпоинты данных для 5 мульти-страниц учатся принимать список `project_id` и агрегировать (аналитика суммирует метрики по набору, публикации/аккаунты объединяют выборку). Обратная совместимость с одиночным `project_id`. На дашборде согласовать с существующим режимом «Все проекты». Изменения бэкенда — под обратно-совместимый контракт (старый фронт не ломается).

### 6.3 Тестирование/деплой Фазы 2

Unit-тесты компонента и util-сортировки (Vitest, если настроен — проверить при плане). Деплой: `npm run build` имеет postbuild-хук, который сразу копирует в `/var/www/validator/` — пре-флайт `package.json` обязателен.

## 7. Вне скоупа

- Кнопочные тумблеры (платформы/дни/режим) — это не выпадашки, их не трогаем.
- Существующие мультичекбоксы «Платформы»/«Статус» на Ручной выкладке — оставляем как есть.
- Добавление новых фильтров (напр. фильтр по телефону на страницах, где его нет) — кроме явно перечисленных. Вопрос «по телефону в ручной» — там фильтр уже есть, доп. работы нет.
- Чистка мусорных проектов в данных (`--status` и т.п.) — не наша задача; сортировка просто разместит их по алфавиту.

## 8. Риски и заметки

- **Размер `index.html`.** Файл огромный и редактируется параллельными сессиями — атомарные коммиты, `git fetch` перед стартом, не оставлять half-broken state.
- **Сосуществование виджетов.** Новый виджет не должен конфликтовать с уже существующими мультичекбоксами на Ручной выкладке (общий popup-менеджер / закрытие по клику-вне).
- **Агрегация на клиентском кабинете.** Семантика суммирования метрик по нескольким проектам в аналитике требует аккуратности — детали в плане Фазы 2.
- **Cross-repo.** Две фазы = два репозитория и два PR; не смешивать.
- **Кодекс-ревью.** Spec и каждый план прогоняются через `codex review` до отдачи пользователю.

## 9. Критерии приёмки

**Фаза 1 (delivery):**
- На всех 5 страницах фильтр проекта (и Тел.№/Пак на Ручной) — виджет с полем поиска.
- Опции отсортированы единым правилом (§4.2), `все` пинится первой.
- Мультивыбор работает (OR) на всех 5 страницах; «Планировщик» корректно фильтрует серверно по набору проектов; одиночный выбор не сломан.
- `node --test` зелёный; смоук на testbench пройден.

**Фаза 2 (client):**
- 5 страниц — мультивыбор с поиском и единой сортировкой; «Уникализация» и «Мой пакет» — одиночный выбор с поиском и сортировкой.
- Бэкенд агрегирует по набору `project_id`, одиночный контракт совместим.
- Unit-тесты зелёные; сборка/деплой проходят.
