# WP#203 — разведка in-app upload флоу TikTok (живой проход)

**Дата:** 2026-05-31
**Устройство:** №19 = RF8YA0W57EP (SM-A175F, 1080×2340), testbench, raspberry=7, adb `147.45.251.85:15068`
**TikTok:** 44.x, аккаунт залогинен (тест-телефон, разрешён Данилом)
**Цель:** подтвердить флоу `«+»`→камера→галерея и зафиксировать стратегию выбора видео (открытый
вопрос спеки `2026-05-31-wp203-tt-inapp-upload-design.md`). Ничего не публиковалось.

## Воспроизведение

| Шаг | Действие | Результат |
|---|---|---|
| 1 | launch TikTok | `MainActivity` (лента) |
| 2 | tap `«+»` навбара (540,2180) | **`SAASceneWrapperActivity` (камера)** — FM-A воспроизведён вживую |
| 3 | tap текст «ПУБЛИКАЦИЯ» (540,2135) | **без эффекта** — это уже выбранный режим, не кнопка |
| 4 | tap тайл-миниатюру внизу-слева `id/zne` (112,2126) | **галерея открылась** (та же activity, dump 56KB→87KB) |
| 5 | tap вкладку «Видео» (531,294) | сетка **только видео**, по свежести |

## Ключевые узлы (bounds на 1080×2340; в коде искать по структуре, НЕ хардкодить)

**Камера (`SAASceneWrapperActivity`, дампится, ~56KB):**
- Вкладки режимов: `ФОТО` [443,1592][638,1685], `ТЕКСТ` [638,1592][838,1685] (text, clickable=false)
- Нижние режимы: `ЭФИР` [211,2072][349,2199], `ПУБЛИКАЦИЯ` [349,2072][731,2199],
  `ТВОРЧЕСТВО` [731,2072][1080,2199] — text-ноды, **clickable=false** (тап по тексту не работает)
- **Тайл-вход в галерею:** clickable `id/zne` [0,2047][225,2205] → центр (112,2126)
- Запись: clickable `id/rts` [385,1696][694,2005]

**Галерея (в той же activity, ~87KB):**
- Альбом-дропдаун `Недавнее` `id/zce` (514,163)
- Вкладки: `Все` (200,294), **`Видео` clickable desc=Видео (531,294)**, `Фото` (871,294)
- Кнопка `Далее` `id/vst` [551,2036][1046,2160] (640,2098) — активна после выбора
- Чекбокс «Выберите несколько вариантов» `id/nu_` (323,2099)
- Сетка тайлов: 3 колонки, центры строк y≈542/905/1268; первый тайл (182,542)
- **Тайлы без `content-desc`**; у каждого видео-тайла **оверлей `MM:SS`** в нижней части
  (напр. `00:39` [223,656], `00:56` [939,656], `00:16` [229,1019])

## Выводы для дизайна

1. **`«+»`→камера подтверждено** как штатный (не аварийный) исход после switch на этом устройстве.
2. **Вход в галерею = тайл `id/zne`** (низ-лево), а не вкладка «ПУБЛИКАЦИЯ». Корректирует
   `_tt_enter_upload_from_camera`.
3. **«В галерее» детектим по UI-маркерам** (Все/Видео/Фото + Далее), foreground не меняется.
4. **Селектор:** обязательна вкладка **«Видео»** (иначе «Все» подмешивает фото/скриншоты, и первый
   тайл = не видео). После фильтра первый тайл = новейшее видео. Опционально — верификация по
   оверлею `MM:SS` против длительности `remote_media_path`.

## Ops: отключение com.samsung.storyservice на флоте (FM-B, слой 1)

In-app upload уводит флоу от системного share (где всплывает оверлей), а foreground-дисмисс
(`_tt_recover_from_storyservice_fg`) — страховка. Но самый чистый способ убрать FM-B на корню —
отключить пакет на флоте (исполняет Данил):

```
# на каждое устройство (обратимо):
adb -s <serial> shell pm disable-user --user 0 com.samsung.storyservice
# откат:
adb -s <serial> shell pm enable com.samsung.storyservice
# проверка персистентности после reboot/OTA:
adb -s <serial> shell pm list packages -d | grep storyservice   # должен быть в disabled
```

На RF8YA0W57EP (№19) уже отключён. Код-дисмисс по foreground-пакету покрывает устройства, где
disable не применился/слетел после OTA.

## Гигиена

Временные дампы/скрины `/sdcard/wp203_*` удалены с устройства; публикация не выполнялась; девайс
возвращён на лаунчер.
