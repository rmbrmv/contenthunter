# WP#203 iter6 — откат story-editor BACK-escape (DEFAULT OFF): SHIPPED+DEPLOYED+ВЕРИФ

- **Дата:** 2026-06-04
- **OpenProject:** #248 «TikTok: tt_story_editor_unrecoverable — лидер падений 04.06 (41/91)» → **Готово**; реальный root-cause → follow-up **#250** (Бэклог)
- **Репо/деплой:** delivery-contenthunter, PR #161 → main `3e52e1b`; прод autowarm (`/root/.openclaw/workspace-genri/autowarm`) — правка в `publisher_tiktok.py`; PM2-restart не нужен (publisher per-task spawn, `scheduler.js:585`); миграции/`.env`-правок нет.

## Триаж TikTok 04.06 (прод host-PG `localhost:5432/openclaw`, `publish_tasks`)
91 failed / 20 done. Распределение по `error_code`:

| error_code | кол-во |
|---|---|
| **tt_story_editor_unrecoverable** | **41** |
| tt_inapp_upload_unreached | 24 |
| phone_or_email_link_required (banned, ops) | 6 |
| tt_publish_button_not_activated | 4 |
| tt_caption_field_not_focused | 3 |
| прочие (≤2 каждый) | 13 |

`tt_story_editor_unrecoverable` — **новый код, появился только сегодня** (его эмитит ровно PR #156 / WP#203 iter6). 41 фейл на 18 аккаунтах / 15 устройствах, 10:45–14:38 МСК — общий UI-сценарий, не девайс.

## Root cause: iter6 — это РЕГРЕССИЯ, а не «нет восстановления»
Причинная связь с деплоем (всё 04.06):
- PR #156 (iter6) смержен на прод **10:42 МСК**; первый `tt_story_editor_unrecoverable` — **10:45 МСК**.
- Done-rate TikTok: **до 10:00 = 32%** (13/41) → **после (iter6 активен) = 10%** (7/70). Весь обвал объясняется ровно 41 новым story-editor-фейлом.
- **0 восстановлений из 41**: BACK ни разу не вывел из in-app story-редактора.

Полный таймлайн (task 15243/15324/15393/15396, единый паттерн):
```
story-derail детектирован (escape #1) — BACK     # ветка c (старая WP#203), share-экран «Добавить в историю»
выбран видео-тайл                                 # BACK → story-галерея → выбор видео
in-app story-editor детектирован (escape #1) — BACK
in-app story-editor детектирован (escape #2) — BACK
in-app story-editor детектирован (escape #3) — BACK
story-editor не покинут BACK-ом за лимит — честный фейл   # кап MAX_STORY_EDITOR_ESCAPE_ITERATIONS=3
```
После выбора видео поток входит в Story-композер («Ваша история» + «Aa» + «Автомонтаж»), и BACK из него **не выводит** (3 BACK подряд, без возврата в галерею — застрял). До iter6 этот экран шёл в ветку g (reset-to-feed, iter5) → `tt_inapp_upload_unreached`. И reset-to-feed (iter5), и BACK (iter6) лечат симптом постфактум и не работают.

## Фикс (mitigation, TDD)
Флип дефолта kill-switch `TT_INAPP_STORY_EDITOR_ESCAPE_ENABLED` `true`→`false` (`publisher_tiktok.py:1470`). Ветка a3 (story-editor BACK-escape) нейтрализуется дефолтом; код и детектор `_tt_detect_inapp_story_editor` оставлены для будущего фикса реального root-cause. Обратимо (env var включает обратно для экспериментов).

- **TDD:** red→green `test_orchestrator_story_editor_escape_default_off` (без env var ветка не эскейпит, 0 BACK); 2 feature-теста переведены на явный opt-in kill-switch. `85 passed` в `tests/test_publisher_tt_inapp_upload.py`.

## Верификация (реальная публикация на тестфоне №19)
Testbench TikTok-публикация **#15475** на phone #19 (`RF8YA0W57EP`, Pi #7, аккаунт `user70415121188138`) на прод-коде с откатом:
- **Публикация успешна** — «✅ Публикация успешна: TikTok» (15:57): editor (`tt_fp_editor matched`) → caption (фокус подтверждён, 77 симв.) → «Опубликовать» найдена+нажата → post-publish success inferred. Подтверждено скрином опубликованного ролика.
- **iter6-ветка НЕ сработала: 0 событий** `tt_story_editor*`.
- 1 story-derail (старая ветка c, не iter6) обработан одним BACK → флоу продолжился и опубликовал.
- Мелочь (не фейл): статус `awaiting_url`/`screencast_stop_failed` — url-capture отдал profile-URL вместо `/video/` (известный WP#204).

Запуск canary: `python3 -c "import testbench_orchestrator as t; t.tick({'idx':1})"` из прод-дир (`idx=1`=TikTok; `--once` даёт `idx=0`=IG). Тестбенч-runner `autowarm-testbench` (`testbench_scheduler.js`, `__dirname`=прод) спавнит прод `publisher.py` → тест валиден.

## Остаётся (follow-up OP#250)
1. Live-verify recovery done-rate по следующей естественной пачке TT (возврат к ~32%).
2. Реальный root-cause — почему смена аккаунта приземляет TikTok на share-экран «Добавить в историю» вместо ленты (сага WP#44 iter3 / WP#203 iter4-6). НЕ переоткрывать iter5/iter6 как есть; чинить источник, TDD + canary на №19.

## Грабли
- Сырой requeue старых упавших задач (`status='pending'`) **не работает** для теста: `media_path` обнуляется post-cleanup-кроном после прогона (`server.js:7370`), staged-файл удалён → префлайт `❌ Путь к медиафайлу не указан`. Для боевого теста использовать testbench-canary (стейджит seed-media сам).
