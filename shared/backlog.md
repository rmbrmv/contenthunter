# Backlog — Задачи агента

## OPEN
- [2026-06-03] **WP#215 follow-up: безусловный seed `autowarm_settings`** — при деплое WP#215 выяснилось, что seed-блок `INSERT INTO autowarm_settings ... ON CONFLICT DO NOTHING` в `server.js` (delivery-contenthunter) НЕ выполняется на обычном старте прода: в прод-таблице отсутствует даже `auto_sync_device_mapping`. Видимо, блок под условием (sample/empty) или путь initDB не доходит до него. Не критично (контроллер/UI используют дефолты через фолбэк; ключи `retry_cutoff_hour_msk=14`/`retry_extra_days=0` добавлены в прод вручную), но новые настройки не попадают в БД автоматически. Разобраться, почему seed не идёт, и сделать его безусловным/идемпотентным при boot. Низкий приоритет. | от: self (WP#215 deploy)
- [2026-06-03] **WP#215 verify — ретраи день в день** — SHIPPED+DEPLOYED 03.06 (delivery main 66806a7, прод pull + pm2 restart id35). Проверить по «Лог событий» (delivery → Аналитика → 📜 Лог событий) после ближайшего часа отсечки (14:00 МСК): (1) появляются `handoff` с правилами `after_cutoff_manual` / `daily_cap_exhausted`; (2) после 14:00 упавшие реально уходят в ручную очередь операторам, а не висят failed до завтра; (3) до 14:00 ретраи идут как раньше; (4) `device_unreachable` до 14:00 троттлится (WP#210 цел). Затем OP#215 → Готово. Откат: `RETRY_SAME_DAY_ONLY_ENABLED=false`. Evidence: `docs/evidence/2026-06-03-wp215-retries-same-day-only-shipped.md`. | от: self (WP#215 deploy)
- [2026-06-02] **WP#152 follow-up: аудит `scheme.get(col, default)` на NULL-колонки** — фикс audio_bitrate выявил паттерн: `.get('x', default)` НЕ срабатывает, если колонка есть со значением NULL (вернёт None, не default). В `unic-worker/worker.py` пройтись по всем `scheme.get(...,<default>)` и заменить на `... or <default>` там, где колонка может быть NULL (новые схемы часто имеют NULL в редко используемых полях). Низкий приоритет — рендер-тест 36/36 прошёл, защитная мера. | от: self (WP#152)
- [2026-06-02] **WP#152 verify после 1-го реального слота клиента** — когда заведут проект клиента ~100 акк и сгенерят превью: проверить, что схема подобралась, ролик с круглым лицом+тряской отрендерился и выложился; затем OP#152→Готово. | от: self (WP#152)
- [2026-06-02] **WP#203 iter5 verify — `tt_inapp_upload_unreached` (driver B)** — SHIPPED+DEPLOYED 02.06 (PR #148 delivery-contenthunter main `94dadba`; детект экрана профиля → роут на «Главная»; детерминированный `am start` reset-к-ленте, повторяемый, кап `MAX_INAPP_UNKNOWN_RESETS=3`, замена iter4 one-shot; kill-switch `TT_INAPP_UNKNOWN_RESET_ENABLED` ON). Осталось: live-verify сутки по прод host-PG `publish_tasks` — (1) `tt_inapp_upload_unreached` падает с ~13/день к хвосту; (2) событие `tt_inapp_unknown_reset` появляется и завершается достижением редактора (а не исчерпанием капа 3 → если массово упирается в кап, значит `_tt_reset_to_feed` не доводит до ленты на части устройств, смотреть дампы); (3) рост TT success. Driver A (`tt_upload_confirmation_timeout`) — отдельная WP#218, не здесь. Откат: `TT_INAPP_UNKNOWN_RESET_ENABLED=false`. Evidence: `docs/evidence/2026-06-02-wp203-iter5-tt-inapp-upload-unreached-shipped.md`. OP#203 → Тестирование. | от: self (WP#203 iter5 deploy)
- [2026-06-02] **WP#222 verify — счётчик генерации превью ≤100% на след. генерации** — SHIPPED+DEPLOYED 02.06 (owner-guard воркера от двойной обработки + зажим прогресса backend/UI + бэкфилл 2 строк). Осталось: по следующей реальной генерации схем убедиться, что индикатор «Генерация превью…» доходит до `N/N`=100% и завершается (нет «зависания» за 100%); в БД `unic_tasks` нет новых `scheme_preview`-строк с `schemes_done > schemes_total`. Если переполнение повторилось — смотреть, не обходит ли owner-guard какой-то путь переотдачи таска (watchdog/restart/реплики). Evidence: `docs/evidence/2026-06-02-wp222-scheme-preview-progress-overflow-shipped.md`. OP#222 → Тестирование. | от: self (WP#222 deploy)
- [2026-06-02] **WP#217 follow-up: явный `task_type='unic'` гард в `manual_queue_assign.js`** — сейчас ручная очередь оператора защищена от scheme_preview лишь *косвенно* (INNER JOIN по `(meta->>'slot_id')::int`, которого у превью нет). Defense-in-depth: добавить явный `AND ut.task_type='unic'` в кандидатный SELECT `assignManualPublishQueue` (зеркало allowlist из assign_candidates.js). Не блокер (0 риска: ни одна scheme_preview-задача не несёт slot_id), но устранит зависимость от совпадения. Codex flagged как Minor на финальном ревью; есть защитный регресс-тест `test_manual_queue_preview_safe_live.test.js`. Контекст: `docs/evidence/2026-06-02-wp217-scheme-preview-leak-shipped.md`. | от: self (WP#217 follow-up, codex final review)
- [2026-05-18] **IG `ig_caption_fill_failed` root-cause hunt (after defense WP #81 / PR #67 shipped)** — defense merged: early-abort + observability dump в момент CAPTION_MARKERS detection. Backlog: (1) 24h live verify — ожидаем появление `ig_caption_screen_not_reached` в error_code distribution; (2) собрать 2-3 fails с новыми `caption_marker_detected_step*` dump'ами, сравнить с `caption_verify_fail_0` dump'ами — это покажет, что меняется в UI между detection и focus_surgical (гипотезы: race condition / IG layout change / tutorial overlay timing). Evidence/контекст: `docs/evidence/2026-05-18-ig-caption-fill-defense-shipped.md`. | от: self (WP #81 follow-up)
- [2026-05-14] **TT iter#2–#5 — 24h soak подтверждение** — серия SHIPPED (PR #52/#54/#57/#59/#60): восстановлен device-verified путь к переключателю аккаунтов TikTok (профиль → меню → drawer → Настройки → scroll → «Сменить аккаунт»). iter#5 verified на устройстве RF8Y90GCXWX + против реальных дампов. Осталось: 24h soak — `tt_account_menu_unknown_layout` 13/24ч → ~0 (deadline ~2026-05-15 14:00 UTC). Evidence: `docs/evidence/2026-05-14-tt-account-switcher-settings-nested-shipped.md`. | от: self (TT триаж 2026-05-14)
- [2026-05-11] **24h метрика TT music rights handler** — измерить fail-rate `tt_upload_confirmation_timeout` на raspberry=9 за 24h после deploy `8ec5c53`. Expect: < 3 (was 12). Query в evidence doc. | от: self (T10 deferred)
- [2026-05-10] **`tt_fg_lost` после успешного music rights handle (pt 4523)** — Phase 1 investigation: AI Unstuck multi-tap может накликать nav buttons / background TT после tt_music_rights_accepted. Hypothesis. Sample: pt 4523 events 19:42:40-19:55:15. | от: self (T10 live verify finding)
- [2026-05-10] **Re-enable unic-sweep PM2 flag (после dup incident 2026-05-08)** — `sudo pm2 unset autowarm:UNIC_SWEEP_DISABLED && sudo pm2 restart autowarm`. Пер memory `project_publish_dup_incident_2026_05_08` — 3 spec'а merged, остался последний шаг re-enable. Verify: `sudo pm2 env autowarm | grep SWEEP`. | от: self (initial backlog scan)
- [2026-05-10] **server.js:6190 SQL bug ($2/$1 P3)** — one-line fix готов к PR (per memory `project_dispatcher_sql_param_bug`). Silent fail в inactive-account skip path. | от: self (initial backlog scan)
- [2026-05-09] **IG Share Tier 2 impl** — spec `be23e8706` готов (long-press escalation 2×200ms). Sample size 3 fails/7д — low priority. После /aif-plan → impl в worktree. | от: self (sub-project carry-over)
- [2026-05-04] **multi-logo (validator)** — design brief `.ai-factory/plans/multi-logo-brief-upload-brief-20260506.md` готов (Опция B рекомендована — отдельная таблица `validator_brand_logos`). Не запущен. | от: self
- [2026-05-04] **account-checker (delivery-contenthunter)** — research-карта переиспользуемого кода `.ai-factory/plans/account-checker-reusable-code-brief-20260504.md` готова. Открытые вопросы для автора ТЗ: набор признаков, триггер запуска, нужен ли UI. | от: self

## DONE
- [2026-05-27] ~~WP #162 канарейка кросс-репо контракта skip_reason='moved_from_slot%' (follow-up #154)~~ | закрыто (Готово — ноль рантайм-правок, проверять в бою нечего): 2026-05-27 | **2 PR** GenGo2/validator-contenthunter#23 (`26e056c`) + GenGo2/delivery-contenthunter#113 (`3797aea`), merged в main, прод-деревья ff-pull (autowarm `3797aea` / validator `26e056c`), PM2 restart НЕ нужен (тесты+комментарии). **Контракт:** валидатор пишет `skip_reason='moved_from_slot_<src>_to_<dst>'` (`schedule.py move_unpublished` → `pipeline_reversal` write-site), delivery re-queue по `LIKE 'moved_from_slot%'` (`assign_candidates.js`); смена текста без координации = тихий регресс re-queue (как до #154). **Сделано (рантайм не тронут):** (1) канарейка validator `test_schedule_pipeline_reversal.py` — `in`→`startswith('moved_from_slot')` (зеркалит LIKE-префикс); (2) контракт-комменты в обоих репо; (3) починен красный delivery `test_pipeline_guards.test.js` (mock-десинк `slotIsEffectivelyManual`, WP#125). Проверки: канарейка 1 passed, `test_pipeline_guards` 11/11, codex 0 находок. **Контекст:** дизайн+реализация автоворкера (спека одобрена Данилом, codex-clean) остановились до отгрузки → доведено другой сессией (verify→codex→PR→merge→ff-pull). Spec/план/evidence: `docs/superpowers/specs/2026-05-27-wp162-skip-reason-contract-canary-design.md`, `docs/superpowers/plans/2026-05-27-wp162-skip-reason-contract-canary.md`, `docs/evidence/2026-05-27-wp162-skip-reason-canary-shipped.md`. | от: self (OpenProject #162, follow-up WP #154)
- [2026-05-27] ~~IG `ig_target_not_in_picker` forward-fix — picker fg-guard на надёжном пробнике, после рецидива over-fire (WP #119)~~ | закрыто (Тестирование, verify утренним daily-batch): 2026-05-27 | **2 PR** GenGo2/delivery-contenthunter#111 (foreground-пробник) + #112 (sheet-маркеры), оба merged в main (`48d27a4`→`e265dbc`, per-task spawn), `IG_PICKER_FG_GUARD_ENABLED=1` + рестарт autowarm id35. **Контекст:** гард #119 в R2 (21–26.05) обрушил IG 79%→22% (over-fire) → был откатан флагом. **Первопричина over-fire (по коду):** `_detect_foreground_pkg` (account_switcher.py) брал ПЕРВЫЙ `package=` из XML-дампа uiautomator → systemui/launcher → ложный «foreign» почти на каждой задаче. **Фикс #111:** новый `_reliable_foreground_pkg` (делегирует в `_ig_probe_foreground_pkg` WP#135 — bare-dumpsys topResumedActivity; shim-fallback try/except; `'unknown'`/не-строка→`''`=no-op) + 2-sample confirm (паттерн WP#129: чужой fg только при 2 замерах подряд → транзиент лаунчера не валит) + лог `ig_picker_fg_probe`; глобальный `_detect_foreground_pkg` НЕ тронут (YT/TT на нём). **Осторожная выкатка вскрыла ВТОРОЙ over-fire** (не мой пробник): sheet-валидация `_ig_on_account_switcher_sheet` (наследие R2/PR#102) искала устаревшие маркеры `account_switcher_recycler`/`bottom_sheet_container` — в текущем IG их НЕТ → ложный negative на реально открытом sheet → 3/3 guard-reaching задачи упали `ig_picker_wrong_screen`→`wrong_foreground` (12:07–12:27). Откатил флаг, нашёл по прод-дампу (18KB sheet): текущие маркеры = `recycler_view_container_id` + тексты «Добавьте аккаунт Instagram»/«Перейти в Центр аккаунтов» (проверено: есть в sheet, нет на профиле/Modal/MediaCapture). **Фикс #112:** освежил маркеры (legacy + текущие) + DRY `_ig_is_on_unexpected_screen`→делегат. Риск-асимметрия: ложный negative=over-fire (устраняем); ложный positive=мелкий `ig_target_not_in_picker` («меньшее зло»). Переврубил флаг — over-fire НЕ повторился; единственная live-guard-задача = реальный стойкий TikTok-hijack (probe=`com.zhiliaoapp.musically`, дамп 100% TikTok) → честный `wrong_foreground` (корректно: с гардом OFF был бы misleading `ig_target_not_in_picker`). Тесты: 107 зелёных на 3 файлах (test_account_switcher + test_canonical_error_codes + test_account_switcher_ig_picker_fg_guard, вкл. dedicated-набор обновлён под надёжный пробник + 2-sample, не-вакуумные recovery-ассерты), 0 регрессий (15 pre-existing падений идентичны main). Процесс: brainstorm→spec→plan→subagent-driven (TDD), codex 0P1 на обеих итерациях + opus final-review (поймал упущенный 3-й тест-файл — устранён). Spec/план/evidence: `docs/superpowers/specs/2026-05-27-wp119-ig-picker-fg-guard-reliable-probe-design.md`, `docs/superpowers/plans/2026-05-27-wp119-ig-picker-fg-guard-reliable-probe.md`, `docs/evidence/2026-05-27-wp119-ig-picker-fg-guard-reliable-probe.md`. **Также закрывает бэклог «8 IG-тестов красные на main»** (падали из-за этого гарда — изолированы/обновлены). **Остаток:** verify на утреннем daily-batch (IG success не просел; `ig_target_not_in_picker`→~0; `ig_account_switcher_wrong_foreground` только на реальных hijack; живой позитив «sheet прошёл гард» не пойман — дневной объём ~0) → при подтверждении WP→«Готово». Откат: `IG_PICKER_FG_GUARD_ENABLED=0` + рестарт; daily-report 9:50 МСК ловит обвал. | от: self (OpenProject #119, Данил)
- [2026-05-26] ~~TT `tt_profile_tab_broken` — stale-uiautomator guard в own-profile верификации (WP #131)~~ | закрыто (Тестирование, 24h verify ~27.05): 2026-05-26 | прод GenGo2/delivery-contenthunter main `f569f5d` (merge --no-ff ветки `wp131-tt-stale-ui-guard`, `account_switcher.py` на main не менялся с базы → без конфликтов; `git push origin main` `1af91ce..f569f5d`; Python per-task spawn — без PM2 restart). **НЕ дубль #130** (разведка: держится 2-3/день и после деплоя #130 22.05). `tt_profile_tab_broken` = catch-all из ≥3 причин (5 свежих дампов): доминирует **stale uiautomator** (dumpsys стабильно=TikTok, но uiautomator отдаёт протухший XML лаунчера → 4 own/logged_out/reauth/foreign детектора ложно-False → retap-петля исчерпывается). #130 и маркеры исправны, гниёт сам дамп; рестарт uiautomator невозможен (WP #105). **Фикс:** при подтверждённом stale-UI (`_tt_probe_looks_stale` + `_tt_dumpsys_confirms_foreground` ×3, приём WP #105) — профиль-тап + `break` из петли → существующая vision-проверка аккаунта (2911+, скриншот при stale корректен) постит ТОЛЬКО при `current==target`, иначе bottomsheet-switch. Codex P1 (wrong-account: editor_triggers≠аккаунт) → отказ от прямого tap+verify, break в vision; P2 → профиль-тап до break. Kill-switch `TT_STALE_UI_OWN_PROFILE_GUARD`. 13 тестов, регресс 87/87; spec-compliance + code-quality (Approve) + codex (спек/план/дифф) — clean. Subagent-driven в изолированном worktree. Spec/план/evidence: `docs/superpowers/specs/2026-05-26-wp131-tt-profile-tab-stale-ui-guard-design.md`, `docs/superpowers/plans/2026-05-26-wp131-tt-profile-tab-stale-ui-guard-plan.md`, `docs/evidence/2026-05-26-wp131-tt-stale-ui-guard-shipped.md`. **Остаток:** 24h verify (`tt_own_profile_stale_ui` частота + `tt_profile_tab_broken`→0 в stale-bucket) → WP→«Готово». Out-of-scope child: **#159** («История просмотров» модалка) + **#160** (разлогин «Вы вышли»). | от: self (OpenProject #131, авто-воркер триаж)
- [2026-05-25] ~~Логи и статусы автовыкладки — ретрай / перевод на ручную (WP #138)~~ | закрыто (Тестирование, ждёт браузерную приёмку Данила): 2026-05-25 | prod + origin/main GenGo2/delivery-contenthunter `c50e87b` (ff-push без force; перед этим ребейз на уехавший main — без конфликтов, файлы не пересекались с WP #117/#118/#119 R2; `git pull --ff-only` + `sudo pm2 restart autowarm` id=35, online, сервер 200). **Observability поверх retry engine (WP #108) — логику ретраев НЕ трогали.** `retry_controller.js` при авто-`requeue`/`handoff` пишет человекочитаемое событие в `publish_tasks.events` (атомарно, в одной транзакции с переходом статуса) + `publish_queue.last_retry_reason`; тексты причин в `retry_labels.js` (transient_within_limits/fixed_at_reanimated → перезапуск; structural_error banned/ui_changed, window_exhausted → ручная). Дашборд delivery.ru (`public/index.html`): бейджи «🔁 Повтор после ошибки · попытка N» (из `last_retry_reason`+`attempt_count`) и «❗→✋ Ошибка → ручная» (из `status=cancelled`+`skip_reason LIKE retry_handoff:%`) в очереди и задачах (задачи — из `pt.events`, т.к. join `pq↔pt` рвётся после requeue), маркеры `had_retry`/`auto_handoff` на карточках планировщика (`buildPlannerCards`), иконки 🔁/🤚 в модалке лога. Миграция: `publish_queue.last_retry_reason/last_retried_at` (аддитивно, nullable; `retry_count` не нужен — `attempt_count` уже считается). Процесс: brainstorm→spec→plan→subagent-driven (10 коммитов, исполнители + ревью диффов), 28/28 тестов (test_retry_labels 7 / test_retry_controller +3 / test_publish_planner +3), codex на спеке/плане/диффе поймал 3 бага до прода (битый join pq↔pt → бейджи задач из событий; неатомарность лога handoff; kill-switch для last_retried_at). Spec/план: `docs/superpowers/specs/2026-05-25-wp138-autowarm-publish-logs-design.md`, `docs/superpowers/plans/2026-05-25-wp138-autowarm-publish-logs.md`. **Остаток:** браузерная приёмка Данила на delivery.ru (метки/записи на реальных задачах; контроллер тикает раз в 5 мин) → при подтверждении WP→«Готово». Откат фичи без отката кода: env `RETRY_VISIBILITY_ENABLED=false` + рестарт. | от: self (OpenProject #138, Анастасия)
- [2026-05-25] ~~Переоткрытая тройка #117/#118/#119 R2 — рецидив после фиксов 21.05~~ | закрыто (Тестирование, verify утром 26.05): 2026-05-25 | **3 PR** GenGo2/delivery-contenthunter#102 (#119 IG) / #103 (#118 TT) / #104 (#117 YT), все merged, прод main `8826fd8` (per-task spawn — без рестарта PM2), 80 тестов зелёных в прод-копии. Разведка субагентами + видео-разбор (по #118 анализ только событий дал неверный вывод — решило видео). **#119:** IG на переднем плане, но на ленте, а не на sheet выбора аккаунта (гард проверял только пакет) → детектор `_ig_on_account_switcher_sheet` (маркер `account_switcher_recycler`, взят из `_ig_is_on_unexpected_screen`) + re-open list (2 попытки) + честный `ig_account_switcher_wrong_foreground`, kill-switch `IG_PICKER_SHEET_GUARD_ENABLED` (отдельный от FG-гарда; call-site не тронут). **#118:** ложно-негатив (аналог IG #73) — публикация проходит, но промо-плашка TikTok Amplify «Пусть вас заметят» перекрывает детектор (подтверждено кадрами task 9357) → хендлер `_detect/_handle_tt_amplify_modal` по образцу promo-inbox WP #82 → `inferred_success`, kill-switch `TT_AMPLIFY_MODAL_HANDLER_ENABLED`. **#117:** desc-trap recovery выкидывал из app (BACK → профиль → лаунчер, task 9628) → post-BACK verify `topResumedActivity` + helper `_yt_restart_upload_activity` (URI до HOME) + cap=2, события `yt_desc_trap_restart`/`yt_desc_trap_back_left_app`, kill-switch `YT_DESC_TRAP_GUARD_ENABLED` (1 рецидив — слабый сигнал, но механика была сломана). Процесс: brainstorm→spec→plan→subagent-driven (3 PR параллельно в изолированных worktree, spec+quality review + opus final **approve ×3**), codex на каждом поймал 2 бага до прода (небезопасный feed-маркер «Входящие» #118 — убран, порядок HOME/uri #117 — исправлен). Spec/план/evidence: `docs/superpowers/specs/2026-05-25-trio-117-118-119-residual-fixes-design.md`, `docs/superpowers/plans/2026-05-25-trio-117-118-119-residual-fixes.md`, `docs/evidence/2026-05-25-trio-117-118-119-residual-fixes-shipped.md`. **Остаток:** verify пачка 26.05 (`ig_target_not_in_picker`→~0 / `tt_upload_confirmation_timeout`→падает / yt desc-trap→0), при подтверждении WP→«Готово». Откат: 3 env kill-switch. | от: self (OpenProject #117/#118/#119, переоткрытая тройка)
- [2026-05-25] ~~Правки к ручной выкладке (WP #128)~~ | закрыто: 2026-05-25 (Готово, приёмка Данила на живом delivery) | prod GenGo2/delivery-contenthunter main `8f68365` (FF-merge feature; фронт-only деплой статикой + 1× `sudo pm2 restart 35` под бэкенд `manual_date`). **6 правок + 3 доработки по фидбэку.** Базовые: (1) чекбоксы фильтра статусов в шапке колонки «Статус»; (2) точечное обновление поллинга — `mpqReconcile` (диф add/remove/patch + group-qualified order-check по `<phone>#<unic>`) + sig-guard карточки вместо полного 5s-ререндера, kill-switch `window.MPQ_TARGETED_REFRESH`; видео убрано из карточки (осталась ссылка); (3) колонка «Ручная дата» (=`created_at`, БЕЗ миграции) после «План дата»; (4) убран пункт «План выкладки» из сайдбара (секция/роутер целы); (5) фикс дёрганья ширины колонки статуса (`.mpq-col-status` 160px). Доработки: фильтр по платформам (чекбоксы, дефолт все), фильтр-диапазон дат на «План/Ручная дата» (дефолт текущая неделя Пн–Вс, AND — принято Данилом), дефолт статусов queued+in_progress+partial, кнопка «Взять в работу» внутри карточки пака. Тестируемое ядро `public/mpq_pure.js` (`mpqStatusVisible`/`mpqDiff`/`mpqPlatformVisible`/`mpqDateInRange`/`mpqCurrentWeekRange`/`mpqIsClaimable`, UMD как `paginated-table.js`) — 8 node-тестов; бэкенд `manual_publish_queue.js` (+`to_char(created_at) AS manual_date`) + `test_mpq_rowtodict`. Процесс: brainstorm→spec→plan→subagent-driven (8 задач, spec+quality review каждая + opus final), codex review на каждой волне (поймал суммарно 6×P2: stale-порядок при сортировке по статусу, узкая row-сигнатура, фиктивный syntax-check, устаревшие group-заголовки, over-filter пустой даты — все закрыты). Spec/план: `docs/superpowers/specs/2026-05-25-wp128-manual-publish-edits-design.md`, `docs/superpowers/plans/2026-05-25-wp128-manual-publish-edits.md`. Kill-switch: `window.MPQ_TARGETED_REFRESH=false` (per-browser; сайт-вайд откат = git revert + pm2 restart). | от: self (OpenProject #128, Данил)
- [2026-05-22] ~~TT `tt_post_switch_verify_unrecoverable` Layer 3 — modal-agnostic confirm (WP #67)~~ | закрыто (Тестирование, 24h soak pending): 2026-05-22 | PR GenGo2/delivery-contenthunter#99 squash `063f401`, prod deployed (fast-forward git pull, Python per-task spawn — без PM2 restart; уцелел при последующем merge WP #129 PR #100). **Завершение серии WP #67** (Layer 1 @-логин PR #62, Layer 2 whitelist PR #70). Остаток 1–3/сут (19.05=1, 20.05=3, 21.05=1, 22.05=2) = 3 под-режима (модалка/переходный экран сбивает post-switch verify, TikTok в foreground — НЕ drift/WP #130): A «Подпишитесь на друзей» поверх ленты ломает renav (9124), B переходный/loading-экран (8573), C «Быстрая проверка безопасности» поверх профиля после renav (7715). Фикс — 3 независимых кирпича под env-kill-switch: (1) banner «Вы вошли как X» (`_tt_read_login_confirm_banner`/`_tt_early_banner_confirm`/`_tt_post_switch_confirm`, `TT_POST_SWITCH_BANNER_DISABLED`); (2) generic SAFE_DISMISS loop (`_tt_find_safe_dismiss`/`_tt_dismiss_and_confirm_loop`, affirmative-кнопки исключены, `TT_POST_SWITCH_GENERIC_DISMISS_DISABLED`/`TT_DISMISS_MAX`) — **поглощает Layer 2 whitelist** (старый `_try_dismiss_and_redump`/`_TT_POST_SWITCH_DISMISSIBLE_MODALS` оставлен dead-code, unit-тесты зелёные); (3) settle-retry (`_tt_screen_is_transitional`, `TT_POST_SWITCH_SETTLE_DISABLED`/`TT_SETTLE_S`/`TT_SETTLE_RETRIES`). Реворк `_tt_handle_post_switch_unknown` (pre_feed dismiss → settle → feed-detect → pre_renav dismiss → renav → post_renav dismiss → честный fail), врезка early-banner+confirm в `_switch_tiktok`. **Fail-closed + mismatch сохранены**, WP #93 fail-fast не тронут. 31 новый тест + миграция 4 L2 integration-тестов, 130 switcher-suite зелёный (rebase на main с WP #130/#132/#105 — без конфликтов). Ревью: per-task spec+quality ×4 + opus final + codex (spec/plan/код) — 0 critical/important. Docs PR rmbrmv/contenthunter#12 (`2fd734c0a`). Live-smoke OK: 9385 (el_cosmo46, под-режим C) `recovered_via_renav`, 9386 (wellroomcare, под-режим A) clean — оба `awaiting_url`, 0 ошибок. **Остаток:** 24h soak → WP #67 «Тестирование»→«Готово»; перехватываемость баннера в dump НЕ подтверждена (в smoke не сработал) — смотреть `tt_post_switch_confirmed_via_banner` за сутки, если 0 → отключить kill-switch'ем; follow-up debt (dead-code whitelist, `exact=True` для tap, защита парсинга env-var). Новый триаж-сигнал `tt_post_switch_blocked_no_safe_button`. Evidence: `docs/evidence/2026-05-22-tt-post-switch-modal-agnostic-shipped.md`. Kill-switches: 6 env-var.
- [2026-05-19] ~~Manual publish flag + matcher (WP #85)~~ | закрыто: 2026-05-19 16:45 UTC (deployed) | **4 PR**: validator-contenthunter#14 `95b421b` (core), delivery-contenthunter#82 `d2435e9` (matcher cron), validator-contenthunter#15 `e16e0ae` (hotfix inline toggle ClientDashboard.vue per `feedback_validator_two_slot_renderers` — admin'ы по умолчанию приземляются на `/dashboard` через inline-рендер, не SlotCard), validator-contenthunter#16 `ccf22d6` (admin-only «Выкладка по слотам» в ContentDetail.vue + новый endpoint `GET /api/schedule/by-content/{content_id}`). Admin-only тогл «Выложить вручную» в карточке слота (RBAC: только role=admin, тройная защита backend + viewer_role filter + frontend). При включении автоматика отменяется через cancel_downstream_for_content (с keep_slot_id=slot_id чтобы не нулять matched_* у других слотов того же контента). Cron `runSlotMatcher` в autowarm каждые 5 мин сопоставляет factory_inst_reels со слотами по описанию (substring=1.0 или pg_trgm similarity ≥0.7 в окне ±3д), с дезамбигуацией (min |slot_date - post_ts|, max score). Scope (codex P1): matcher работает только для `manual_publish=true OR status='published'` — filled-auto слоты пропускаются чтобы не создать дубль. assignUnicResultsToQueue получил guard на manual_publish=true. UI: SlotCard.vue (manager view), inline в ClientDashboard.vue (admin landing) + новая секция в ContentDetail.vue (admin-only). ManagerDashboard calendar stats badge, новый Analytics блок «Источник публикаций». Endpoint `GET /api/admin/publication-stats` агрегирует auto/manual/manual_unconfirmed/total_filled. Migration 005 (7 cols + 2 partial indexes + unic_settings.matcher_enabled + pg_trgm extension). Kill-switches: ENV `SLOT_MATCHER_ENABLED=false` (hard), DB `unic_settings.matcher_enabled=false` (soft). Prod deploy: validator pulled+migrated+rebuilt, autowarm подхватил slot_matcher_cron (другая сессия pull'нула в рамках WP #87). Систёмная зачистка: `validator-backend.service` (systemd) disabled — конфликтовал с PM2 `validator` за порт 8000, restart-looping 11000+ раз с 14 мая. Spec/план/evidence: `docs/superpowers/specs/2026-05-19-wp85-manual-publish-flag-design.md`, `docs/superpowers/plans/2026-05-19-wp85-manual-publish-flag-plan.md`, `docs/evidence/wp85_smoke_2026-05-19.md`. Tests: backend 56 (test_manual_publish 13 + test_publication_stats 5 + test_publish_summary 5 + test_pipeline_reversal 15), frontend 6 vitest, autowarm 12 node tests, smoke E2E 9/9. Codex 4 раунда (P1 matcher scope + P2 isSlotPast midnight + P1 stale account_switcher + P2 bool body).
- [2026-05-19] ~~TT `tt_profile_tab_broken` — promo-модалка (контакты / FB friends) поверх профиля (WP #106)~~ | закрыто: 2026-05-19 12:00 UTC | PR #77 merge `cc5a5e6`, prod deployed (git pull + pm2 restart autowarm restart#36). Топ-1 причина TT-фейлов 2026-05-19 (4 из 17 после исключения сетевой проблемы adb_device_not_ready, 31 за 7д). 4 retap-дампа показали `content-desc="Диалог"` поверх профиля — variant'ы «Чтобы связаться в TikTok» (3/4) и «Разрешить TikTok доступ к списку ваших друзей» (1/4). Existing `_tt_dismiss_security_prompt` не матчил (его whitelist = «Быстрая проверка безопасности» + «Нижняя шторка»). Зеркало WP #67 Layer 2 (PR #70): новый constant `_TT_PROFILE_PROMO_DISMISSIBLE_MODALS` + module helper `_tt_try_dismiss_profile_promo` + method `_tt_dismiss_profile_promo_dialog` (с kill-switch env `TT_PROFILE_PROMO_DISMISS_DISABLED=1`) + 1 блок в `_switch_tiktok` retap-loop между security_prompt и `_tt_is_own_profile` (cap=1 dismiss/retap). 14 новых тестов (8 unit helper + 4 method + 2 integration), 118/118 switcher suite зелёный, codex review 0 P1. Spec/план/evidence: `docs/superpowers/specs/2026-05-19-tt-profile-promo-dismiss-design.md`, `docs/superpowers/plans/2026-05-19-tt-profile-promo-dismiss-plan.md`, `docs/evidence/2026-05-19-tt-fails-triage.md`. 24h soak deadline ~2026-05-20 12:00 UTC (ожидаем `tt_profile_tab_broken` 3-7/день → ~0). Kill-switch: env-var.
- [2026-05-18] ~~TT switch blocked by «Необходимо обновить аккаунт» modal (WP #93)~~ | закрыто: 2026-05-18 19:02 UTC | PR #75 squash `52efe66`, prod deployed (git pull + pm2 restart autowarm). Новый detector `_TT_SWITCH_BLOCKING_MODALS` + helper `_tt_detect_switch_blocking_modal` + method `_maybe_handle_switch_blocking_modal` + call-site вставка в `_switch_tiktok` (после `_save_dump`/`_maybe_screenshot`, ДО `_post_switch_verify_handle`). Match → `error_code='tt_switch_blocked'` + `account_blocks.set_block_by_username(reason='phone_or_email_link_required')` + `notify_escalation`. Bundle: revert ошибочной строки `('Привязать номер...', 'Не сейчас')` из WP #67 Layer 2 whitelist (title_substr матчил текст BUTTON). 14 новых тестов (8 unit + 5 integration + 1 layer2-no-match), 6 WP #67 тестов переориентированы на `save_login` fixture. 133/133 switcher-related suite зелёный, 3 codex round'а до 0 P1. Evidence: `docs/evidence/2026-05-18-wp93-tt-switch-blocking-modal-shipped.md`. 24h soak deadline ~2026-05-19 19:02 UTC. Kill-switch: `_TT_SWITCH_BLOCKING_MODALS = ()`.
- [2026-05-18] ~~IG `ig_caption_fill_failed` defense + observability (WP #81)~~ | закрыто: 2026-05-18 | PR #67 squash `b4215eb`, prod deployed (git pull + pm2 restart autowarm). Early-abort в `_fill_instagram_caption_and_publish` при focus-fail (больше не льём adb_text вслепую), новый `error_code=ig_caption_screen_not_reached`, `IG_CAPTION_SCREEN_MARKERS` без `'Аудитория'`, observability dump в editor loop. 6 новых тестов, 53/53 caption-related, 613 suite (1 pre-existing fail unrelated), codex 0 P1. Evidence: `docs/evidence/2026-05-18-ig-caption-fill-defense-shipped.md`. Defense, не root cause — root-cause hunt в OPEN.
- [2026-05-14] ~~TT switcher iter#2 — `tt_account_menu_unknown_layout`~~ | закрыто: 2026-05-14 | PR #57 `8e7623a`, prod deployed (PM2 autowarm restart), evidence `docs/evidence/2026-05-14-tt-account-switcher-settings-nested-shipped.md`. Settings-nested fallback в `_open_tt_account_switcher`. 55 tests green, codex clean. Smoke/soak — отдельным OPEN-пунктом.
- [2026-05-10 19:55] ~~TT music rights confirmation dialog handler~~ | закрыто: 2026-05-10 19:55 | PR #28 `8ec5c53`, evidence `docs/evidence/2026-05-10-tt-music-rights-dialog-shipped.md`, live verify pt 4523 handler fired ✅
- [2026-05-10 11:30] ~~ig_gallery_no_video_candidate Mode A~~ | закрыто: 2026-05-10 16:36 | PR #26 `4643c7d`, Mode B closed pending re-occurrence

