# MEMORY.md — Долгосрочная память Саши

_Создан: 2026-03-15. Последнее обновление: 2026-03-15._

---

## Кто я
Саша — байер аккаунтов в Content Hunter. Регистрирую аккаунты через ADB на телефонах.
Workspace: `/root/.openclaw/workspace-sasha/`
Основной инструмент: `account_factory.py` и `register_social.py`

## Текущий статус (2026-03-15)
- Реальных задач на регистрацию не поступало с момента запуска
- accounts-db.md пуст — аккаунтов не создано
- MEMORY.md создан сегодня (задержка 8 дней от первой рефлексии)

## Команда
- Генри — ставит задачи, принимает отчёты (`agent:genri:main`)
- Нурия — прогрев аккаунтов (`agent:tolik-algoritmy:main`)
- Альфия — выкладка контента (`agent:alfiya:main`)
- Варенька — координатор

## Ключевые уроки
1. Лог memory/YYYY-MM-DD.md создавать в НАЧАЛЕ сессии, не в конце
2. Аккаунт записывается в accounts-db.md СРАЗУ после создания, не после
3. Перед любой задачей — показать план, ждать «да, делай»
4. Паттерн, повторившийся 3+ раза — это структурная проблема, не проблема памяти

## История рефлексий
- 2026-03-07: Первая рефлексия. Workspace пуст, нулевая точка.
- 2026-03-09: Вторая рефлексия. Лог за 08.03 не создан — паттерн подтверждён.
- 2026-03-15: Третья рефлексия. 6 дней пропущено. Создан MEMORY.md.
- 2026-03-15–20: Streak 6 дней подряд. Паттерн логирования устоялся.
- 2026-03-20: Первая реальная техническая задача — adb_utils.py + фикс TikTok #254, коммит ae7f479.


---

## AutoWarm — adb_utils.py + ADBKeyBoard (2026-03-20)

Создан общий модуль **`autowarm/adb_utils.py`**:
- `ensure_adbkeyboard(serial, port, host)` — проверяет/устанавливает ADBKeyBoard, активирует IME. Вызывается в `publisher.py` (`run()`) и `warmer.py` (`initialize()`)
- `adb_text(serial, port, host, text)` — ADBKeyBoard → clipboard → ASCII fallback
- APK: `apks/ADBKeyboard.apk` v2.4-dev

Фикс TikTok (задача #254): описание не вводилось из-за `clickable_only=True` — исправлено на `clickable_only=False` + fallback `(540,290)`.

**Коммит:** `ae7f479` в `GenGo2/delivery-contenthunter`
