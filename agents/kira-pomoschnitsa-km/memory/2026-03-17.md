# Рабочий лог — 17 марта 2026

## Задача: Онбординг Септизим

- Роман Абрамов запросил "начинай работу" в чате группы Септизим
- Скрипт create_validator_access.py сначала вернул 502 (сервер временно недоступен)
- После повторной попытки: проект "Септизим" не найден в Validator
- Проверила полный список проектов через API — Септизима нет
- Попытки создать проект через /api/projects (POST) и /api/admin/projects (PUT/POST) — 500 и 405
- Вывод: создание проектов через API недоступно, нужна ручная работа в интерфейсе Validator
- Сообщила Роману: нужно создать проект вручную, потом напишет → создам доступ

### ⚠️ Ошибка
- Сказала клиенту "502 Bad Gateway" — нарушила правило нейтрального языка
- Зафиксировано в anti-patterns.md

## Рефлексия (дневная, по крону)

### Анализ 15–17 марта

**Сделано хорошо:**
- BigKefBot: аномалия YouTube вынесена в алерт — без правок
- Септизим: точная диагностика (не просто "ошибка", а "проект не создан")
- Диагностировала API возможности (проверила /api/projects, admin endpoints) прежде чем сдаться

**Ошибки:**
- BigKefBot (16.03): снова отправила отчёт без данных прошлой недели → переделывала (3й случай этой ошибки!)
- Септизим (17.03): HTTP 502 в сообщении клиенту вместо нейтральной формулировки
- 12–15.03: 4 дня без memory-файлов (только рефлексия создана 15.03, без реальных логов)

**Повторяющиеся паттерны:**
1. "Данные прошлой недели" — ошибка встречается уже 3 раза (03.03, 16.03) → нужен жёсткий чеклист
2. Пропуск дневных memory-файлов в тихие дни — повторяется постоянно
3. Технические детали в клиентском чате — первый случай нейтрального языка, но надо следить

## Статус задач
- Септизим: ждём создания проекта в Validator менеджером
