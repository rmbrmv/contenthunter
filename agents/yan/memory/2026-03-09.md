# Memory — 9 марта 2026

## Контекст из 7 марта 2026

### Статус проекта RELISME
- 6 кампаний активны на rizz.market
- Vision-анализ завершён: 161/161 блогеров
- Финальный отчёт: `reports/bloggers_cards_final.html` (243 KB, msg 422)
- **✅ 8 принять | ⚠️ 31 рассмотреть | ❌ 123 отклонить**

### Топ-8 рекомендованных (✅):
1. @ketrin_22_ — score 80, cf=20, ER=12.1%, mv=3002
2. @natali.frames — score 80, cf=25, ER=10.9%, mv=2195
3. @ak_lidiya — score 77, cf=12, ER=3.9%, mv=3609
4. @kristina__iiim — score 71, cf=21, ER=8.5%, mv=1796
5. @zd.lova — score 69, cf=16, ER=11.1%, mv=7307
6. @d_krasillnikova — score 69, cf=14, ER=9.3%, mv=1855
7. @x_ellka — score 67, cf=12, ER=15.8%, mv=1109
8. @mari_psh_ — score 61, cf=21, ER=41%, mv=527 (vision ✅ «дорого без брендов»)

### Создан скилл rizz-blogger-review
- Путь: `skills/rizz-blogger-review/` + `.skill` файл
- Содержит: collect_offers.js, scrape_offer_metrics.js, accept_decline.js, gen_report.py
- Assets: report_template.html (HTML-шаблон карточек)
- References: scoring.md, vision_prompt.md, rizz_api.md
- Варенька добавила: rizz-review.lobster (Lobster-пайплайн с approval gate)

### Важные технические факты о метриках:

**ER формула (финальная):**
- Instagram: `like_rate + comment_rate` (сохранения/репосты Instagram не отдаёт публично)
- TikTok: `(diggCount + commentCount + shareCount + collectCount) / playCount × 100` — полный ER доступен!
- YouTube: только `likes / viewCount`

**Поля в profiles_enriched.json:**
- ER хранится как `like_rate` (НЕ `er`!) + отдельно `comment_rate`
- TikTok доп. метрики: из `/tmp/tt_30posts.json` — `shareCount`, `collectCount`, `repostCount`

**Instagram публичный API не отдаёт:**
- Сохранения (savesCount)
- Репосты/пересылки (sharesCount)

### Что обсуждали но не завершили:
- Пересчёт TikTok-блогеров с полным ER (23 блогера в выборке) — Роман спросил, ответ дан, решение не принято
- accept/decline откликов — ждём решения Романа по списку из 8

### Ключевые уроки (добавить в anti-patterns):
1. Поле ER в profiles_enriched.json = `like_rate`, не `er`
2. При вставке JS в HTML — проверять количество `<script>` тегов
3. Двоеточие в YAML описании → использовать `description: >` (блочный скаляр)
4. HTML-шаблон отчёта не упрощать — использовать утверждённый формат с ТЗ-чеклистом

---

## Обновление 20:19 UTC — Анализ структуры 525 офферов

### Разбивка 525 офферов:
- **339** — без никнейма (скрейпер не определил платформу при сборе) — не анализировались
- **186** — с никнеймом → 100 уникальных блогеров + 86 дублей

### Из 100 уникальных с никнеймом:
- 62 — вошли в наш анализ (profiles_enriched.json)
- 38 — новые (появились после 7 марта)

### Главный пробел:
339 офферов без никнейма — скорее всего YouTube или аккаунты без данных в API
Нужен отдельный scrape по offerUrl чтобы определить платформу

### Ожидаем решения Романа:
- Смотреть ли 339 офферов без никнейма?
- Запускать ли accept/decline для топ-8?

---

## Обновление 20:33 UTC — Раскрыты 339 офферов без никнейма

### Результат fetch_missing_offers.js:
- Запрос через GraphQL API: `socialNetwork { username type }` для каждого offerId
- Заполнено: 339/339 (0 ошибок)
- Instagram: 198 | TikTok: 51 | YouTube: 90

### Полная картина 525 офферов:
- Уникальных блогеров: **261**
  - Instagram: 87 (14 новых, не в нашем анализе)
  - TikTok: 27 (3 новых)
  - YouTube: 47 (не анализируем)
- **Новых для Apify+Vision: 17**

### Новый скрипт:
- `/root/.openclaw/workspace-yan/scripts/fetch_missing_offers.js`
- Только чтение — запрашивает socialNetwork.username и type по offerId

### Ожидаем:
- Решение Романа: запускать ли Apify на 17 новых блогеров?
