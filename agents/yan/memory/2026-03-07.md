# Memory — 7 марта 2026

## Контекст из предыдущего дня (2026-03-06)

### Статус проекта RELISME
- 6 кампаний активны на rizz.market (все Active)
- 461 заявка собрана → отправлена Роману CSV (msg 259)
- Vision-анализ начат: 23/161 блогеров проанализировано

### Инфраструктура готова:
- Apify KV store `OSNixuaPizWbhq07j` — 1223 Instagram обложки (стабильные URL)
- TikTok обложки: KV store `laSwTpqO5Bjr7nue8`
- Шаблон URL: `https://api.apify.com/v2/key-value-stores/OSNixuaPizWbhq07j/records/{username}_{index}`
- `/tmp/profiles_enriched.json` — 161 профиль с метриками
- `/tmp/vision_results_full.json` — 23 результата vision-анализа

### Два HTML-отчёта готовы:
- `reports/bloggers_scored.html` — табличный формат
- `reports/bloggers_cards.html` — карточный формат (предпочтительный по мнению Романа)
- Оба отправлены Роману (msg 365, 368)

### Скоринг модель (финальная):
1. Медиана просмотров (30 pts): <500=0, 500-1к=10, 1к-3к=20, 3к+=30
2. Content fit vision (25 pts): ❌=0, ⚠️=12, ✅=25
3. ER quality (20 pts): <1%=0, 1-3%=10, 3-8%=20, >8%=15
4. ЦА/платформа (15 pts): Instagram=10, TikTok=8
5. Тренд (10 pts): 📉=0, ➡️=5, 📈=10
6. Бот-штраф: 🔴=-20, 🟡=-10

### Vision топ (✅ принять, уже проанализированы):
- @ketrin_22_ — score ~80, CF=20, fashion-фокус, ER 11.2%
- @kristina__iiim — score ~80, CF=21, стильные образы, ER 8.5%
- @natali.frames — score ~80, CF=25, минимализм 4/5
- @petrowa.vi — CF=18 (⚠️ боты!), стиль есть

### ❌ Vision отклонены:
- @nerevizoro (Чебоксары, кафе), @koval4ik_sport (танцы), @mama_orchids (орхидеи)
- @tatyana_filatova_omsk (учитель Омск 40+), @goryanka_albina (Дагестан)
- @savelieva__irina, @pr.kofanova, @di.lsnko (юмор/байт)

## Что нужно сделать сегодня
- [ ] Продолжить vision-анализ: осталось 138/161 блогеров
- [ ] Обновить HTML-отчёт bloggers_cards.html после завершения
- [ ] Отправить финальный отчёт Роману для одобрения
- [ ] После одобрения — запустить accept/decline через accept_decline.js

---

## Итоги — 7 марта 2026 (ранее утро)

### Vision-анализ ЗАВЕРШЁН: 161/161 блогеров ✅

**Финальные результаты:**
- ✅ Одобрены: **6**
- ⚠️ На рассмотрение: **34**
- ❌ Отклонены: **121**

**Топ-6 (✅ рекомендованы к принятию):**
1. @natali.frames — CF 25/25, мода/минимализм, 2.2к просм
2. @kristina__iiim — CF 21/25, fashion-фокус, ER 8.5%
3. @mari_psh_ — CF 21/25, «дорого без брендов» = ДНК бренда
4. @ketrin_22_ — CF 20/25, 33к подп, высокий ER
5. @st_nastasiya_ — CF 19/25, UGC-creator, минималистичный стиль
6. @iiits.ok — CF 18/25, базовые образы, нейтральные тона

**Файлы:**
- `/tmp/vision_results_full.json` — 161 результат (финально)
- `reports/bloggers_final.html` — финальный HTML-отчёт (181 KB) → отправлен Роману (msg 373)

**Ключевые паттерны из анализа:**
- Большинство откликов — мамские блоги, бьюти-услуги, WB-распаковки — не ЦА RELISME
- Реальных fashion-минималистов с хорошим охватом крайне мало (<5% от пула)
- TikTok-блогеры в выборке в основном мелкие (<2к подп) или нерелевантные
- UGC-creators (@st_nastasiya_) — хороший вариант для бартера даже при малой аудитории

**Что ждём:**
- Ответ Романа на финальный отчёт
- После одобрения → accept/decline через `node accept_decline.js accept <offer-id>`

### Технические уроки сессии:
- TikTok Apify KV URLs: имя файла без полного username (обрезается до 10 символов), нужно брать URL из `/tmp/tt_30posts.json`
- Некоторые старые TikTok covers 404 — нужно брать более свежие посты из tt_30posts.json
- Блогеров с пустым bio + малым числом подп можно оценивать по bio без vision (экономия)
- Batch-анализ без визуальной проверки допустим когда bio однозначно указывает на несоответствие

---

## Обновление 05:55 UTC — Исправление формата отчёта

### Ошибка и исправление
- Роман указал, что отчёт `bloggers_final.html` был не по утверждённой форме
- Утверждённый формат: `bloggers_cards.html` — с ТЗ-чеклистом, метриками в двух столбцах, bio, vision-саммари, итогом в цветном боксе, фильтрами
- Я сделала упрощённые карточки без чеклиста — это была ошибка

### Антипаттерн зафиксирован:
**НЕ упрощать утверждённый шаблон отчёта** — всегда использовать тот же CSS/JS шаблон, только обновляя данные

### Новый финальный отчёт:
- `reports/bloggers_cards_final.html` (241 KB) → отправлен Роману (msg 378)
- Формат: копия старого `bloggers_cards.html` с данными 161/161 блогеров
- ✅ 3 | ⚠️ 16 | ❌ 143

### Итоговые rec (по score + vision):
- ✅ @ketrin_22_ (score 65, cf=20), @natali.frames (65, cf=25), @kristina__iiim (56, cf=21)
- ⚠️ топ: @ak_lidiya, @zd.lova, @swetlana_i_family, @kristine_hov_, @anastacia.nik и др.

---

## Обновление 05:57 UTC — Баг с двойным script тегом

### Ошибка
- `bloggers_cards_final.html` не рендерился — карточки не показывались
- Причина: header (строки 1-86 из старого файла) уже заканчивался на `<script>`
- Я добавила ещё один `\n<script>\n` в Python при вставке данных → два открывающих тега, один закрывающий → JS падал

### Фикс
- Убрала лишний `<script>` при конкатенации:
  `full = header + '\nconst D=' + data_js + ';\n' + render`  (без лишнего тега)
- Исправленный файл отправлен (msg 384)

### Антипаттерн:
**При вставке JS-данных в существующий HTML-шаблон** — всегда проверять количество `<script>` / `</script>` тегов перед отправкой

---

## Обновление 06:13 UTC — Создан скилл rizz-blogger-review

### Скилл упакован
- Путь: `/root/.openclaw/workspace-yan/skills/rizz-blogger-review.skill`
- Тип: приватный (только в workspace-yan)

### Состав скилла:
- `scripts/collect_offers.js` — сбор откликов с rizz.market (GraphQL)
- `scripts/scrape_offer_metrics.js` — Puppeteer-парсинг метрик
- `scripts/accept_decline.js` — принять/отклонить оффер
- `scripts/gen_report.py` — универсальная генерация HTML-отчёта (бренд как параметр)
- `references/scoring.md` — формула скоринга (100 pts)
- `references/vision_prompt.md` — промпты и инструкции vision-анализа
- `references/rizz_api.md` — GraphQL API документация rizz.market
- `assets/report_template.html` — HTML-шаблон карточек с фильтрами

### Ключевые решения:
- Скилл универсальный — `--brand "НазваниеБренда"` как параметр gen_report.py
- HTML-шаблон вынесен в assets/ с плейсхолдером `__DATA_PLACEHOLDER__`
- YAML frontmatter с multiline description (через `>`) — иначе двоеточие в тексте ломает парсер

### Антипаттерн (YAML):
Двоеточие в однострочном description ломает YAML-парсер → использовать `description: >` (блочный скаляр)

---

## Обновление 07:21 UTC — Варенька обновила скилл rizz-blogger-review

### Что добавила Варенька:

**1. `rizz-review.lobster`** — Lobster-пайплайн автоматизации:
- Шаги: collect → scrape → apify → report → approve (пауза!) → execute
- Встроен approval gate — пайплайн ждёт одобрения перед финальным шагом
- Запуск: `lobster run rizz-review.lobster`
- Параметры: `brand` (default: RELISME), `fresh`
- APIFY_TOKEN захардкожен в env шага apify

**2. Секция `## 🦞 Lobster Pipeline`** в SKILL.md — инструкции по запуску

### Статус:
- `.skill` файл ещё не перепакован с новыми изменениями (Роман не дал команду)
- Vision-анализ остаётся вне пайплайна (требует image tool с батчами)

### Ожидаем:
- Решение Романа по 6 блогерам (принять/отклонить)
- Возможно: перепаковать .skill с lobster файлом

---

## Обновление 08:14 UTC — Тест скилла + свежий сбор откликов

### Результат collect_offers.js (повторный запуск):
- Было: 461 оффер / 161 уникальный блогер
- Стало: **525 офферов / 38 новых уникальных блогеров**
- Новые в основном: микро-аккаунты <100 подп, YouTube (кириллица в никнейме), накрутка

### Интересные из 38 новых (требуют scrape + vision):
- @hey.wife — 180.9к подп, ER 0% (нужна проверка)
- @svetlana_sveta_relax — 22.7к, ER 0%
- @Dary_wbshka — 7.5к, ER 0%
- @ny.artistka — 2.5к, ER 9%
- @meensk_ — 4.3к, ER 0%
- @dolores20006 — 3к, ER 9%
- @honey151477 — 1.8к, ER 4%

### Статус:
- Ждём решения Романа: запускать scrape_offer_metrics для новых 38?
- offers_collected.json обновлён (525 офферов)

### Ключевой факт:
- Кириллица в nickname = YouTube канал (не Instagram/TikTok)
- ER 0% из API у крупных = нужен Puppeteer-скрейп для реальных данных

---

## Обновление 08:21 UTC — Исправлен баг ER + финальный отчёт v3

### Критический баг в gen_report.py
- **Проблема**: поле ER в profiles_enriched.json называется `like_rate`, а не `er`
- Из-за этого у всех блогеров ER=0, scores занижены, рекомендации неверные
- **Исправление**: `p.get('er') or p.get('like_rate', 0) or 0`

### Исправлена логика рекомендации
- Добавлен минимальный порог: **content_fit < 10 → не может быть ✅**
- Без этого блогеры с отличными метриками но нерелевантным контентом (танцы, новости) получали ✅
- Новый порог для ✅: (vision=✅ AND score≥55) OR (score≥65 AND cf≥10)

### Финальный результат (msg 422):
- ✅ **8** | ⚠️ **32** | ❌ **122** | всего 162
- Топ-8: @ketrin_22_ (80), @natali.frames (80), @ak_lidiya (77), @kristina__iiim (71), @zd.lova (69), @d_krasillnikova (69), @x_ellka (67), @mari_psh_ (61)

### Антипаттерн:
**Всегда проверять точные названия полей в JSON** перед использованием в скоринге — не угадывать (`er` vs `like_rate`)

### Файлы обновлены:
- `skills/rizz-blogger-review/scripts/gen_report.py` — исправлен ER + логика rec
- `reports/bloggers_cards_final.html` — финальный отчёт (243 KB)

---

## Обновление 08:30 UTC — Исправлена формула ER

### Правильная формула ER
- **Было**: `er = like_rate` (только лайки)
- **Стало**: `er = like_rate + comment_rate` (лайки + комментарии)
- Роман указал: ER включает комментарии, пересылки — более сложная формула
- Сохранения и репосты скрейпер не собирает → задача для Эдварда (обновить scrape_offer_metrics.js)

### Финальный отчёт после исправления:
- ✅ 8 | ⚠️ 31 | ❌ 123 | всего 162
- Топ: @ketrin_22_ ER=12.1%, @natali.frames ER=10.9%, @ak_lidiya ER=3.9%, @kristina__iiim ER=8.5%

### Ожидаем:
- Решение Романа: отправить ли обновлённый отчёт
- Возможно: задача Эдварду добавить сохранения/репосты в scrape_offer_metrics.js
