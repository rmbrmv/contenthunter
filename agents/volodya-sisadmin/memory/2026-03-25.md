## validator — смена ANTHROPIC_API_KEY (2026-03-25)

**Изменение:** В `validator/backend/.env` заменён ключ Anthropic.

**Было:** `anthropic_genri` → ❌ протух (401)  
**Стало:** `anthropic_default` → ✅ рабочий

**Статус ключей Anthropic по состоянию на 2026-03-25:**
- `anthropic_default` → ✅ рабочий (используется в validator)
- `anthropic_genri` → ❌ протух
- `anthropic_manual` → ❌ протух
- `anthropic_systematika` → ❌ протух

**Действие:** когда `anthropic_default` протухнет — нужно будет обновить ключи в `/root/.openclaw/secrets.json` (у Романа) и заменить в `.env`.

**Файл:** `/root/.openclaw/workspace-genri/validator/backend/.env`  
**Сервис PM2:** `validator` (id=18) — перезапущен через `pm2 delete` + `pm2 start bash` с `source .env`

## autowarm — фикс счётчика лайков фарминга (2026-03-25, от Генри)

**Изменение в warmer.py** (GenGo2/delivery-contenthunter, коммит `ac129be`):

Лайки из Фазы 0 (поиск по ключевым словам) не суммировались с лайками Фазы 2 (лента рекомендаций) → в БД и UI показывалось 0 лайков, хотя реально ставились.

**Фикс:** `progress.likes` теперь = `likes_feed + search_likes` (итог). Поле `progress.search_likes` сохраняется отдельно для аналитики.

**Конфигурационных изменений нет.** Сервис autowarm перезапущен через PM2.

## autowarm — универсальное сохранение суб-навигации (2026-03-25)

В delivery.contenthunter.ru добавлен механизм сохранения активного подраздела при F5.

**Как работает:** URL-хэш дополняется `?sub=key:value`. Пример: `#publishing/publishing?sub=up:tasks`
**Охват:** Выкладка (Запланировано/Опубликовано), Аккаунты (Instagram/TikTok/YouTube/WhatsApp/Telegram + вкладки Аккаунты/Задачи), Admin (подразделы).
**Коммиты:** `b6495cf`, `c5d9cfa`, `851cd18` → GenGo2/delivery-contenthunter

---

## 21:00 UTC — Рефлексия за 23-25.03

### Что хорошо:
- 25.03: anthropic_genri протух → быстро нашёл рабочий ключ, перезапустил validator правильно, зафиксировал статус всех 4 ключей
- 25.03/23.03: обновления от Генри фиксируются сразу

### Ошибки:
1. full history парсинг упал ~14:01 UTC 24.03 — обнаружено только сейчас (>30 часов). Итог: 5550 сообщений из ~56 чатов, остаток не залит. Роман не уведомлён.
2. anthropic_default — временный фикс, нет проактивного алерта когда протухнет

### Повторяющийся паттерн:
- Долгие фоновые процессы запускаю и не проверяю на следующий день
- Результат batch-процессов не сообщаю Роману

### Добавлено в learning:
- approved: ротация API-ключа из secrets.json
- anti: full history упал без уведомления — нет watchdog и финального алерта
- anti: протухший API-ключ найден реактивно, не проактивно
