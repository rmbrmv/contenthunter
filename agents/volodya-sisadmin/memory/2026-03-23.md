# 2026-03-23 — Дневной лог

## 06:55 UTC — Фикс validator (client.contenthunter.ru) от разработчика

**Источник:** межагентное сообщение  
**Git:** `ff55b6b` в GenGo2/validator-contenthunter

### Что было исправлено:
- В таблице `validator_unic_content` был неверный цвет хромокея: `0x19af3e` вместо правильного `0x00ff30`
- Из-за этого зелёный фон не вырезался в превью схем уникализации
- Обновлено 10 записей, превью перегенерированы

### Правило для мониторинга:
**При добавлении новых оверлей-видео в `validator_unic_content`:**
- `chromakey_color` должен быть `'0x00ff30'`
- Если хромокей снова не режет — первым делом проверить это поле

### Проверка:
```sql
SELECT id, name, chromakey_color FROM validator_unic_content 
WHERE chromakey_color != '0x00ff30';
-- должно вернуть 0 строк
```

---

## 17:25 UTC — Фикс delivery.contenthunter.ru (массовое создание задач) от Генри

**Git:** `6844bb8` в GenGo2/delivery-contenthunter

### Что было:
- «📋 Массовое создание» задач в `/farming/tasks` создавало задачи без аккаунта → `preflight_failed`

### Что исправлено:
- Новый API-эндпоинт: `POST /api/tasks/bulk` (requireAuth)
- Автоматически подбирает аккаунт по платформе протокола
- Работает через PM2-сервис `autowarm` (порт 3848)

### Для мониторинга:
```bash
pm2 logs autowarm --lines 50
```
Смотреть на `preflight_failed` и ошибки подбора аккаунта.

---

## 17:49 UTC — Фикс unic-worker на 91.98.180.103 от разработчика

**Git:** `3352057` в GenGo2/delivery-contenthunter  
**Сервер:** 91.98.180.103 (внешний, не основной)

### Что было:
- Задачи уникализации зависали в статусе `processing` навсегда
- **Причина:** `mark_task_error` падала с `ValueError` — asyncpg возвращал JSONB-колонку `meta` как строку, `dict(строка)` → исключение → задача не сбрасывалась в `error`

### Что исправлено:
- Безопасный парсинг `meta`: строка → `json.loads`, dict → как есть, None → `{}`

### Диагностика если задачи снова зависнут в `processing`:
```bash
sshpass -p 'MNcwMPCiyiYtM5' ssh root@91.98.180.103 "pm2 logs unic-worker --lines 30 --nostream"
```

### Сброс зависших задач (экстренно):
```sql
-- PGPASSWORD=openclaw123 psql -U openclaw -h localhost -p 5432 -d openclaw
UPDATE unic_tasks SET current_status='pending', schemes_done=0, schemes_error=0, error_message=NULL
WHERE current_status='processing';
```

### Текущий статус: ✅ online, работает нормально

---

## 21:00 UTC — Рефлексия за 21-23.03

### Что хорошо:
- 23.03: три обновления от Генри зафиксированы сразу с командами диагностики и SQL — ничего не потеряно
- 22.03: рефлексия сделана в срок, learning обновлён

### Ошибки:
1. **Google мониторинг устарел:** 4-й раз подряд алертит об "истёкших OAuth токенах" — но мы уже на Service Account с 11.03. Мониторинг смотрит на старые `token.json`. Рекомендация "запустить google_token_refresh.py" — скрипт удалён. Нужно обновить `daily_infrastructure_check.py`.
2. **telegram_messages** — 2 дня в КРИТИЧНО, ни одного действия к решению.
3. **Паттерн повторяется третью рефлексию подряд:** диагностирую → документирую → не делаю следующего шага.

### Добавлено в learning:
- approved: мгновенная фиксация обновлений от разработчиков
- anti: рекомендации в отчёте указывают на удалённый скрипт = устаревший мониторинг

### Действия на завтра:
1. Обновить `daily_infrastructure_check.py` — убрать проверку token.json, заменить на SA API call
2. Выяснить статус telegram_messages — есть ли бэкап, можно ли восстановить
