# 2026-03-20 — Дневной лог

## 09:00 UTC — Обновление списка чатов парсинга

Роман попросил добавить новые чаты в парсинг. Через Telethon аккаунт @textTracker нашёл все чаты.

**Действия:**
- Обновлены `telegram_parse_incremental.sh` и `telegram_parse_batch.sh`
- Было: 36/39 чатов → стало: 102 уникальных чата
- Дублей нет, проверено

**Новые категории чатов:**
- GenGo (сотрудники, корпоративный, обучалки, HR, бухгалтерия)
- УК проекты (Relisme, Mimizombie, QUINGI, Бустер Кап, Yavo, PusherSnack)
- Контент-заводы (Booster Cup, Relisme, Sip&Shine, Chinatoday)
- Финансы и оплаты (ОПЛАТЫ, ин.карта, Зуфар, ОАЭ, BSC, Казахстан)
- Ферма Казахстан, Support GnG
- Новые клиентские чаты Content Hunter (Wanttopay, Септизим, Эль-косметик, Anecole, Age/ess, Pimble, Контактмастерс, Ritual, Глобал Виза)

---

## 07:34 UTC — Изменена логика дедупликации выкладки в autowarm

**Запись от межагентного сообщения:**

В `workspace-genri/autowarm/server.js` изменена логика защиты от повторной публикации.

- **Было:** блокировка по `(content_id, pack_id)` — одно видео нельзя дважды в один пак (даже на другую платформу)
- **Стало:** блокировка по `(content_id, account_username, platform)` — одно видео нельзя дважды на один аккаунт одной платформы, но можно на разные платформы
- Затронуто: крон `assignUnicResultsToQueue` + эндпоинт `POST /api/publish/queue`
- Код запушен на GitHub, PM2 autowarm перезапущен

---

## 07:29 UTC — Обновление autowarm (от разработчика)

**Запись в память от межагентного сообщения:**

В autowarm добавлена видеозапись экрана для задач фарминга (`warmer.py`).

- Новая колонка: `autowarm_tasks.screen_record_url`
- Видео пишется на устройстве через `screenrecord`
- Загружается в S3 бакет: `1cabe906ea6e-gengo`
- Env var управления: `FARM_SCREEN_RECORD` (по умолчанию `true`)
- PM2 autowarm перезапущен

**Что нужно добавить в мониторинг:**
- Проверять что PM2 autowarm online
- Опционально: проверять доступность S3 бакета `1cabe906ea6e-gengo`
