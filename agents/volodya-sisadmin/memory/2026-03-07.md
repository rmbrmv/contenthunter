# 2026-03-07 — Ежедневная проверка инфраструктуры

## ✅ Нормально
- Dashboard, Office, HR, Validator, Producer, Delivery, Tasks — все 200 OK
- PostgreSQL Docker 16.12 — connected ✅
- Telegram MTProto — Up 8 days ✅
- Все Google токены (Calendar, Docs, Sheets, Vision) — существуют ✅
- Zoom скрипты (x4 аккаунта) — синхронизируются каждые 30 мин ✅
- Agent-office, producer-copilot, caddy — all running ✅
- Ресурсы: Диск 52%, RAM 29% — в норме ✅

## ⚠️ ПРОБЛЕМЫ

### 1. **carousel-maker.service** — КРИТИЧНО
- **Статус:** `inactive (dead)` но localhost:3851 отвечает 200
- **Проблема:** Restart counter = **90723** и растет каждые 5 сек
- **Причина:** Процесс стартует → инициализирует API → выходит с exit(0) → systemd перезапускает
- **Логи показывают:**
  ```
  node[2385906]: 🎠 Carousel Maker on http://127.0.0.1:3851
  node[2385906]: Imagen 4 Fast: ✅
  node[2385906]: Anthropic:     ✅
  systemd[1]: carousel-maker.service: Deactivated successfully.
  systemd[1]: Scheduled restart job, restart counter is at 90723.
  ```
- **Это БАГ в коде** — нужно сообщить разработчику Genri
- **Текущий статус:** Локально работает (200), но systemd отдает конвульсии

### 2. **carousel.contenthunter.ru** — DNS проблема
- **Статус:** HTTP 000 (curl ошибка)
- **DNS запрос:** NXDOMAIN — домен не резолвится
- **Это может быть:**
  - Домен не зарегистрирован
  - Caddy конфиг не правильный
  - DNS пока не обновился
- **Действие:** Проверить Caddy конфиг, может быть домен еще не добавлен

## 📊 Запись в backlog
- Отправил алерт Роману о carousel-maker (need Genri to fix)
- Отправил алерт о carousel.contenthunter.ru (DNS/Caddy issue)

## 🔧 Попытанное
- Попробовал перезапустить carousel-maker через systemctl stop/start
- Команда упала (SIGTERM), но сервис остановился правильно (теперь inactive)
