# Memory — 2026-03-09

## ADB Host исправление (от Volodya/Richard, 06.03.2026 ~20:17 UTC)

### Правильный маппинг хостов малинок:
| Pi | ADB-порт | ADB-хост |
|----|----------|----------|
| 1  | 15037    | **147.45.251.85** |
| 2  | 15017    | 147.45.251.85 |
| 3  | 15028    | 147.45.251.85 |
| 4  | 15038    | 147.45.251.85 |
| 5  | 15048    | 147.45.251.85 |
| 6  | 15058    | 147.45.251.85 |
| 7  | 15068    | 147.45.251.85 |
| 8  | 15088    | 147.45.251.85 |
| 9  | 15098    | **82.115.54.26** (исключение!) |
| 10 | 15108    | 147.45.251.85 |

Подключение: `adb -H {host} -P {adb_port} -s {device_serial} shell "..."`

### Что изменено:
1. **БД `raspberry_port`**: Pi 1–8, 10 → `147.45.251.85`; Pi 9 → `82.115.54.26` ✅
2. **`publish_tasks`**: добавлена колонка `adb_host VARCHAR(64)` — заполняется при создании из `raspberry_port`
3. **`publisher.py`**:
   - `ADB_HOST = '147.45.251.85'` (новый default)
   - `DevicePublisher.__init__` принимает `adb_host` параметром
   - `run_publish_task()` читает `adb_host` из JOIN `publish_tasks + raspberry_port`
   - Все `adb -H` команды используют `self.adb_host`
4. **`server.js`**:
   - Оба INSERT в publish_tasks смотрят `raspberry_port` и пишут `adb_host`
   - Battery check cron: хардкод `82.115.54.26` → `${dev.host || '147.45.251.85'}`

### ⚠️ Важно:
Factory DB по-прежнему показывает `82.115.54.26` для всех малинок (устаревшие данные).
`factory_sync.py` при синхронизации ЗАТИРАЕТ `host` в raspberry_port → нужно либо:
- Убрать `host` из upsert при синхронизации raspberry_port
- Или обновить factory DB на правильные значения

## Итоги дня 06.03.2026

### Реализовано за день:
1. ✅ Фикс project_id в unic_tasks (передавался null при создании через UI)
2. ✅ "Живая справка" — контекстная панель ❓ на всех экранах Delivery
3. ✅ Резюме созвона Даниил+Наталья по тестированию платформы
4. ✅ Фиксы по итогам созвона:
   - Явные требования к файлу в DropZone + UploadModal
   - Разделение ❌ (критично) / ⚠️ (рекомендация) в ValidationDetails
   - Стрелки сортировки unic-results — indigo ▲/▼ при активной
5. ✅ Сводный планировщик (все проекты) для менеджеров/админов
6. ✅ Убраны разделы "Загрузить" и "Календарь" из сайдбара валидатора
7. ✅ Требования к файлу добавлены в модалку загрузки планировщика
8. ✅ ADB host исправлен везде (publisher.py, server.js, БД)

### Пользователи/доступы:
- Наталья Шигаева: validator admin, login=natalya_shigaeva
- admin/admin123 — сброшен пароль 06.03 для восстановления доступа

### Контакт: Richard (agent:richard:main) — передаёт технические данные от Volodya-sysadmin
