## 2026-03-27 — Рефлексия за 25-27 марта

### Активность за период (по коммитам)

**autowarm:**
- `b036262` — warmer: retry + AI Unstuck в initialize()
- `e896809` — preflight ADB-проверка устройства
- `ac129be` — фикс суммирования лайков (search_likes + основной цикл)
- `07d47f7` — publisher: Instagram/TikTok/YouTube публикация с описанием
- pre-commit хук установлен, HELP_CONTENT документация обновлена

**validator:**
- Создана таблица `validator_support_history` (чат Киры не работал — таблица отсутствовала)
- Обнаружена ошибка `/api/contract/status` — колонки `plan_phones`, `plan_accounts` и т.д. отсутствуют в `validator_projects`
- Сброшен `onboarding_stage` для Систематики (1 → корректный старт)

### Незакрытые проблемы
1. `validator_projects` не имеет plan_phones/plan_accounts/plan_publications/plan_views/contract_start/contract_end — router `contract.py` написан под несуществующую схему. Нужна миграция.
2. `autowarm_day_logs` не имеет колонки `views` — `save_day_log` падает с ошибкой.
3. ikomek и Relisme имеют `onboarding_stage=5` — нужно решение от Романа (оставить или сбросить).
