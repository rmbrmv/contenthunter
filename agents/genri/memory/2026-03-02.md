# 2026-03-02 — Рабочий лог

## Snapshot 12:33 UTC

### Сделано:
- **Virtual Office v3.0 — полная изометрия** реализована
  - Файл: `/root/.openclaw/workspace-genri/agent-office/public/index.html`
  - Старый плоский офис заменён на настоящую изометрию (TW=96, TH=48, 16×12 тайлов)
  - Референс: `/root/.openclaw/workspace/avatars/office-concepts/variant-3-v2-final.png`
  - Бэкап плоской версии: `index-flat-backup.html`
  
- **Зоны офиса (изометрические):**
  - Переговорная (gx:1-4, gy:0-3) — стеклянные стены, овальный стол #9B7D48
  - Серверные стойки (gx:5-6, gy:1-2) — 4 чёрных шкафа с LED
  - Центр мониторинга (gx:6-8, gy:2-3) — 3 монитора полукругом, синее свечение
  - Рабочая зона — 10 столов #3E2E1A с мониторами #4488CC
  - Большой стол совещаний (gx:4-9, gy:7-9) — тёмный #2A2218, ноутбуки
  - Лаунж (gx:9-12, gy:4-8) — диваны #2D8B78 (teal), ковёр
  - Кухня (gx:11-13, gy:1-3) — шкафчики, кофемашина

- **Архитектура JS:**
  - `iso(gx,gy)` → `{sx,sy}` — изометрическая проекция
  - Рендер back-to-front (сортировка по gx+gy)
  - `drawFrame(t)` — главная функция отрисовки
  - `drawAgent()` — PNG аватарка в круглой маске + кольцо статуса + бейдж
  - `drawMurzik()` — рыжий анимированный кот с хвостом/усами
  - `loadAvatar(id)` — кэш PNG из `/{agentId}.png`
  - `hitAreas[]` — клики на агентов с попапом
  - День/ночь — окна меняют цвет по `getDayPhase()`

- **Сохранены из старой версии:**
  - API fetch: `/api/agents`, `/api/taskboard`, `/api/timeline`, `/api/agent-tasks`
  - `renderTaskboard()`, `renderTimeline()`
  - Popup логика (click/mousemove)
  - CSS/HTML структура без изменений
  - setInterval обновление данных каждые 30 сек

- **pm2 restart office** — сервис online на порту 3847

### Решения:
- Unicode-минусы (−) в массивах → заменить на ASCII (-) через `sed -i`
- Claude Code не работает под root через su-nobody (not logged in)
- ACP runtime не настроен — пишем код напрямую
- Синтаксис проверять через `node --input-type=module` (ошибка `document not defined` = нормально)

### Статус проекта:
- **office.contenthunter.ru** = изометрия v3.0 ✅
- **Открытые задачи**: webhook-server, IPRoyal UAE, Zoom headless, первый тест делегации агентов

## Snapshot 19:09 — Virtual Office v5 (Pixi.js + AI Background)

### Сделано:
- Изучили репозиторий `maximgalson/ai-office-constructor` (Next.js + Pixi.js)
- Ключевой инсайт: Galson использует статичный PNG-фон (`office-bg.png`) без людей + Pixi.js персонажи поверх
- Попытка нарисовать всё через Pixi.js (`office_js_v4.js`, `office_pixi.js`) — пользователь сказал "ерундистика", слишком далеко от референса
- Правильный подход: AI-сгенерированный фон + HTML лейблы + Pixi.js персонажи поверх
- Сгенерировали фон через `gpt-image-1` (LaoZhang) — изометрический офис без людей, 1536x1024
  - Файл: `/root/.openclaw/workspace-genri/agent-office/public/office-bg-gen.png`
  - DALL-E 3 не хватило квоты (~18K осталось, нужно 20K+); gpt-image-1 дешевле
- Архитектура финальная:
  - `index.html` — фон `office-bg-gen.png` + HTML лейблы агентов (position:absolute, left/top в %)
  - `characters.js` — Pixi.js ES module, прозрачный canvas поверх, pixel-art человечки (S=1.5)
  - Лейблы: аватарка + имя + цветная точка статуса
  - Человечки: 20×30px pixel-art, цвета из хэша id, кружок статуса над головой, покачивание

### В процессе:
- Калибровка позиций персонажей — пользователь сказал "висят в воздухе"
- Последние правки: S=2.2→1.5, тень вытянутая, позиции опущены ниже

### Позиции агентов (текущие, % от экрана):
```
main:[26,34], plahov-operdir:[37,31], volodya-sisadmin:[51,30], elena:[59,25]
genri:[22,46], fyodor:[33,43], li-razvedchitsa:[43,43]
richard:[19,56], misha-metodolog:[29,54], oleg-marketolog:[42,56]
mark-dizayner:[72,46], pasha-novostnik:[79,51], dzheff:[72,56], marina:[82,53]
seryoga:[89,40], alfiya:[12,70], tolik-algoritmy:[19,73]
kira-pomoschnitsa-km:[45,62], dasha-smyslovik:[54,57]
```

### Технические детали:
- LaoZhang квота: ~18K кредитов остаток (gpt-image-1 потратил меньше чем DALL-E 3)
- `office_pixi.js` — старый файл, не используется (можно удалить)
- PM2 процесс: `office` port 3847
- Pixi.js v8.6.6 через CDN jsdelivr
- characters.js: async IIFE, PIXI.Application с backgroundAlpha:0, z-index:5 поверх фона

### Решения:
- `write` tool не работает для путей вне workspace → `exec + heredoc`
- Позиции задаются в % (left%, top%) — адаптируются к любому размеру экрана
- Один набор POSITIONS дублируется в index.html и characters.js (синхронизировать вручную)

## 19:13 — Instagram CLI (lupikovoleg/instagram-cli)

Роман показал репозиторий: https://github.com/lupikovoleg/instagram-cli

**Стек:** Python, HikerAPI (api.instagrapi.com), OpenRouter LLM, MCP-сервер

**Ключевое для Autowarm:**
- HikerAPI = Instagram API без официального доступа — альтернатива ADB-скрейпингу статистики
- viral_index формула: `(likes + 3*comments + 4*saves) / views * 100`
  - viral ≥10, strong ≥6, normal ≥3, weak ≥1
- MCP-сервер — можно подключить агентам напрямую
- Умеет: статистику Reel/профиля, скачивание контента, топ фолловеры, лайкеры

**Вопрос к Роману:** есть ли у нас HikerAPI ключ?
Если да — заменить ADB-скрейпинг Instagram аналитики на HikerAPI вызовы
