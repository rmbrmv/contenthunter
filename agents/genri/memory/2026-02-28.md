# Memory — 2026-02-28

## Autowarm 2.0 — TikTok & YouTube Shorts тесты (завершены)

### TikTok (com.zhiliaoapp.musically)
- **Лайк**: двойной тап на центр видео (~355, 680) — работает, счётчик 147→148 подтверждён
- **Скролл**: swipe up (540 1400 → 540 400, 800ms) — следующее видео загружается
- Одиночный тап на кнопку сердца — НЕ работал, двойной тап — работает

### YouTube Shorts (com.google.android.youtube)
- **Лайк**: одиночный тап на (990, 1128) — кнопка в правой панели, подтверждён счётчик "8 403 отметки Нравится"
- **Скролл**: swipe up — следующее видео загружается
- UI dump метод надёжен для поиска кнопок: `uiautomator dump` + парсинг XML
- Состояние "не лайкнут": content-desc содержит `поставить отметку "Нравится"`
- Состояние "лайкнут": content-desc содержит `X отметки "Нравится"` (без "поставить")
- Нельзя тапать в центр видео до лайка — вызывает паузу/оверлей

### ADB device
- Serial: RF8Y91F8EJH (Samsung SM-A175F, 1080×2340)
- ADB relay: `adb -H 147.45.251.85 -P 15037 -s RF8Y91F8EJH shell "<cmd>"`

### Autowarm статус на конец 27 февраля
- Web-приложение live: delivery.contenthunter.ru (port 3848)
- SLA Dashboard работает: 173 устройства, 172 online, avg battery 82%, avg temp 27.2°C
- PM2: autowarm(0), hr-payroll(1), producer(2), office(3), farm(4)
- Farm platform переехал с 3848 → 3853
- warmer.py требует обновления с правильными методами лайков по платформам

### Next: Iteration 1 (осталось)
- Обновить warmer.py: TikTok → adb_double_tap(), YouTube Shorts → adb_tap(990, 1128)
- Iteration 2: авто-цепочка дней 1→10, публикация видео из S3, прокси/гео

---

## Autowarm 2.0 — UI рефакторинг (2026-02-28, pre-compaction flush)

### Сделано в этой сессии
- Удалено поле "Тематика" из протоколов: из редактора (`proto-thematic` input заменён на info-label), из карточки рендера, из save/load логики
- Тематика теперь берётся только из Airtable — протокол = только механика
- Добавлен CSS для sticky headers: `.table-wrap` / `.table-wrap-lg` с `max-height` + `overflow-y: auto` + `thead th { position: sticky; top: 0; }`
- Добавлен CSS для пагинации: `.pagination`, `.page-btn`, `.page-btn.active`, `.page-info`

### В процессе (не завершено на момент compaction)
- Обновить HTML таблицы Devices (~строки 324–341): заменить `<div class="overflow-x-auto">` на `<div class="table-wrap">`, добавить `<div class="pagination" id="devices-pagination"></div>` после `</table></div>`
- Аналогично обновить таблицы: Raspberries (~345), SLA (~589), Tasks
- Добавить JS пагинацию: `devicesPage = 1`, `DEVICES_PER_PAGE = 20`, `renderDevicesPage()`, обновить `filterDevices()` и `renderDevicesTable()`
- Предыдущая попытка Edit не сработала из-за несовпадения whitespace/indentation — нужно читать точные строки перед редактированием

### Заблокировано
- ADBKeyBoard broadcast (result=0) — нет подтверждения работы Cyrillic input
- Claude API key невалиден (401) — нужен новый ключ от Roman

### Критические пути
- Файл UI: `/root/.openclaw/workspace-genri/autowarm/public/index.html`
- Warmer: `/root/.openclaw/workspace-genri/autowarm/warmer.py`
- Server: `/root/.openclaw/workspace-genri/autowarm/server.js`
- ENV: `/root/.openclaw/workspace-genri/autowarm/.env`
- PM2: autowarm(0), перезапуск: `pm2 restart autowarm`

### Следующие шаги (приоритет)
1. Дочитать точные строки devices/raspberries таблиц и сделать Edit правильно
2. Добавить JS пагинацию (20 строк/страница, prev/next)
3. Протестировать Cyrillic input через ADBKeyBoard на RF8Y91F8EJH
4. Получить новый Anthropic API key от Roman
5. Построить UI для audience stats (geo/age/gender charts из autowarm_day_logs)

---

## Правило: список сервисов

Когда появляется новый сервис (веб-приложение, порт, домен) — сразу скидывать обновлённое закреплённое сообщение со всеми сервисами в чат. Roman сам закрепляет.

**Текущий список сервисов:**
- 🔥 Autowarm 2.0 — https://delivery.contenthunter.ru (3848)
- 📊 Dashboard — https://dashboard.contenthunter.ru (3000)
- 🏢 Офис агентов — https://office.contenthunter.ru (3847)
- 👥 HR Payroll — https://delivery.contenthunter.ru:3849 (3849)

---

## TODO: Airtable поле factory_api_name

Нужно добавить поле `factory_api_name` (singleLineText) в Airtable таблицу `Table 1` (tblRdPNox1fzvwtey, base app7fFym01YjkYHB8).

**Зачем:** прямое сопоставление factory DB проекта с Airtable записью без нечёткого поиска.

**Что нужно от Roman:** токен с правами `schema:bases:write` на [airtable.com/create/tokens](https://airtable.com/create/tokens)

**После получения токена:**
1. Создать поле через API
2. Заполнить значения для всех 30 проектов из `autowarm_project_mapping`
3. В `warmer.py` убрать маппинг-таблицу, искать напрямую по полю `factory_api_name`

**Текущий workaround:** таблица `autowarm_project_mapping` в локальной БД — 30/32 проектов замаплено вручную.

---

## Autowarm 2.0 — Event Log, Preflight, Token Tracking (2026-02-28, вторая compaction)

### Что реализовано в этой сессии

#### DB миграция (`autowarm_tasks`)
- Добавлены колонки: `tokens_used INT`, `events JSONB`, `preflight_errors JSONB`

#### warmer.py
- `log_event(event_type, message, meta)` — append to `events` JSONB column в БД
- `add_tokens(input_tokens, output_tokens)` — инкрементирует `tokens_used`
- `preflight_check()` — валидирует: account, airtable_record_id, keywords (поле 'Ключевые фразы'), days_config
- `run()` вызывает `preflight_check()` первым; при ошибке → `status='preflight_failed'`, пишет `preflight_errors`, ранний выход
- `add_tokens()` подключён ко всем 3 вызовам Claude API
- `log_event()` добавлен для: ad detected, like skipped, like placed, comment generated, audience analyzed, day completed

#### public/index.html
- `statusLabels`/`statusClasses` расширены: добавлен `preflight_failed`
- Колонка "🪙 Токены" добавлена в таблицу задач (colspan 9→10)
- Кнопки в строке задачи: 📋 (openEventLog), ⚠️ (showPreflight, только при preflight_failed)

### В процессе (не завершено, cut off)
- Добавление HTML модала `modal-event-log` и JS функций `openEventLog(taskId, account)` и `showPreflight(taskId)`
- Код был прерван на середине файла после `modal-screenshot` div (~строка 671)

### Следующие шаги
1. Дочитать конец `public/index.html` (от modal-screenshot до конца)
2. Добавить modal-event-log HTML после modal-screenshot div
3. Добавить JS функции `openEventLog` и `showPreflight`
4. Добавить API endpoint `GET /api/tasks/:id/events` в server.js
5. Добавить dashboard chart (success vs failed tasks)
6. Исправить ошибку `hashtag` column в warmer.py keywords query
7. Тестировать preflight с задачей без Airtable маппинга

### Ключевые решения
- `events` как JSONB массив: `{ts, type, msg, meta?}` — типы: info/warning/error/like/comment/ad/skip/subscribe/day_done/audience/start/status
- `preflight_failed` — отдельный статус, не `failed`, чтобы UI мог показать ⚠️
- Token tracking: input+output суммируются в одно целое число (не разбивается по вызовам)
- Для openEventLog — нужен endpoint или включить events+preflight_errors в ответ существующего task detail

### Заблокировано
- Claude API key 401 — нужен новый от Roman
- ADBKeyBoard Cyrillic (result=0) — без подтверждения

---

## Autowarm 2.0 — Account Verification + Bug Fixes (2026-02-28, третья compaction)

### Баги исправлены
- **keywords query**: `hashtag` → `name`, добавлен JOIN с `factory_projects` по `api_name`; ключевые слова теперь загружаются (LaserCube: 10+)
- **tasks-chart endpoint**: `created_at` → `updated_at` (колонки `created_at` нет в таблице)

### Account verification (warmer.py)
Новые методы:
- `get_current_instagram_account()` — UI dump профиля, content-desc/resource-id
- `switch_instagram_account(target)` — через Instagram switcher, ищет bounds по username
- `get_current_tiktok_account()` — text="@username" в профиле
- `get_current_youtube_account()` — email/имя канала из меню
- `verify_and_switch_account()` — главный: сравнивает, для Instagram переключает, для TikTok/YouTube останавливает задачу если не совпадает

Вызов: в `run()` после `initialize()` — если False → `status='failed'`

### Архитектурное решение: три модуля
- 🔥 Фарминг (текущий)
- 📤 Выкладка (итерация 2, отдельный тип задачи)
- 🎨 Уникализация (итерация 3)
- Субагент запущен на UI редизайн (горизонтальные табы над sidebar)

### Выкладка (архитектура, не реализована)
- Контент: из S3 + БД (детали позже)
- Типы: Instagram пост/Reel, TikTok видео, YouTube Short (YouTube пост — нет)
- Фазы: pre_publish_warm (3-5 мин) → publish → post_publish_warm (3-5 мин)
- Нужны новые типы протоколов: pre_publish_warm, post_publish_warm

### Важный факт о паках
На одном телефоне может быть 2+ паков разных клиентов. Один пак = один Instagram + один TikTok + один YouTube. Account verification критически важна!

---

## Autowarm 2.0 — Publisher, Scheduler, Analytics, UI (2026-02-28, четвёртая compaction)

### Publisher (publisher.py)
- Создан `/root/.openclaw/workspace-genri/autowarm/publisher.py` (~400 строк)
- Фазы: `pre_publish_warm` → `publish` → `post_publish_warm`
- `preflight_check()`, `add_tokens()`, `capture_post_screenshot()`, `try_get_post_url()`, `save_post_url()`, `save_screenshot()`
- БД таблица `publish_tasks`: все колонки включая tokens_used, preflight_errors, post_url, screenshot_url

### Scheduler (scheduler.js)
- Единый планировщик вместо Task Runner
- Временное окно 09:00–21:00 МСК, max 2 задачи на Малинку, 30 мин cooldown на пак
- Publish > Farm по приоритету
- `PI_BY_PORT` хардкод: `{15037:1, 15017:2, 15028:3, 15038:4, 15048:5, 15058:6, 15068:7, 15088:8, 15098:9, 15108:10}`
- API: `GET /api/runner/status`, `PUT /api/runner/kill/:type/:id`

### Analytics (analytics_collector.py)
- Instagram + TikTok + YouTube: views, likes, comments, saves, shares per post (последние 30 постов, 14 дней)
- Audience: `collect_audience_instagram()` → скриншот Insights → Claude Haiku → geo, age_groups, gender, followers, reach_7d
- БД: `account_post_snapshots` (с saves/shares), `account_daily_delta`, `account_audience_snapshots`
- Крон: daily at 00:00 UTC

### UI (public/index.html)
- 4 таба: 🔥 Фарминг / 📤 Выкладка / 🎨 Уникализация / 📊 Аналитика
- Dashboard: виджет статуса планировщика
- Settings: active_hours, max_per_pi, cooldown
- Publishing: таблица задач + форма создания; Bulk create farming tasks modal
- Analytics: summary cards, views chart, delta table с фильтром по проекту, manual collect

### Тест устройства (сессия ~2026-02-28 09:50 UTC)
- ADB connectivity подтверждена: `echo ok` → `ok`
- Instagram запущен, но показал форму опроса (недвижимость) — нужно было навигировать на Profile tab
- Субагент запущен на добавление Audience widget в Analytics UI
- Текущий статус: субагент не завершён, тест устройства прерван

### Текущие блокеры
- Claude API 401 — нужен новый ключ ANTHROPIC_API_KEY от Roman
- ADBKeyBoard Cyrillic result=0 — не подтверждено
- Airtable schema:bases:write токен — нужен от Roman для поля factory_api_name

### Task #5
- Device RF8Y91F8EJH, adb_port 15037, pack Laser Cube_166
- Day 1 завершён (39 видео, 9 лайков, ad detection OK)
- Day 2 запланирован на 2026-03-01 06:33 UTC (resume_at)

---

## Autowarm 2.0 — Telegram auth + Account Factory (2026-02-28, пятая compaction)

### Account Factory — статус
- `account_factory.py` создан (~400 строк), полный pipeline Gmail→Instagram→TikTok→YouTube
- Тест на RF8Y91F8EJH: Lisa Wilson, `lisa.wilson3835@gmail.com`, DOB 1993-08-15, Female, `account_id=1`
- Google WebView Birthday form заполнили Day=15, Year=1993, но Month (август) и Gender (Female) не встали — WebView тапы не персистируют
- **Решение**: использовали уже существующий аккаунт устройства `kolb1666next@gmail.com`

### Telegram авторизация на RF8Y91F8EJH
- Telegram установлен из Play Market: `org.telegram.messenger` (официальный, FZ-LLC)
- `TG_PKG = 'org.telegram.messenger'` обновлено в `telegram_warmer.py`
- Открыли Telegram, перешли на Email login
- Ввели `kolb1666next@gmail.com` в поле email, отправили KEYCODE_ENTER
- Telegram отправил код верификации на email
- Открыли Gmail (`com.google.android.gm`), нашли письмо Telegram, прочитали код **429215**
- Вернулись в Telegram, ввели код 429215 через KEYCODE digits
- Статус: неизвестен — нужен свежий скриншот для проверки

### Telegram warmer
- Режимы: auth/warm/browse, --scan-phones, --run-pairs
- БД таблицы: `tg_accounts`, `tg_warm_log`, `tg_warm_tasks`
- Цель: генерация screen time на телефонах, НЕ прогрев аккаунтов

### ADB команды
- `adb -H 147.45.251.85 -P 15037 -s RF8Y91F8EJH shell "<cmd>"`
- Display: оригинал 2340x1080, отображение 2000x923 → коэф. 1.17

### Блокеры (на момент compaction)
- Telegram verification code 429215 — мог истечь к моменту ввода
- Gmail WebView dropdowns — Month/Gender не встают через тапы
- Claude API key 401 — нужен новый от Roman

### Следующие шаги
1. Скриншот текущего состояния Telegram на устройстве
2. Если код принят → profile setup (name, username)
3. Если нет → re-request code
4. Получить новый Anthropic API key от Roman
5. Починить Google WebView Month dropdown (scroll + delay подход)

---

## Autowarm 2.0 — UI баг с боковым меню (2026-02-28, шестая compaction)

### Баг: исчезает боковое меню в разделе Фарминг
- Нажатие некоторых кнопок в farming section скрывает `sidebar-farming`
- Исследование: поиск всех `onclick="nav(...)"` и `switchModule(...)` в index.html
- Подозрительные функции: `nav()`, `switchModule()` — содержат логику скрытия sidebar
- Расследование прервалось на этапе grep анализа

### Следующий шаг
- Найти определения функций `nav()` и `switchModule()` в JS части index.html
- Проверить список sidebars в них (не включает ли 'devices', 'accounts' и пр.)
- Найти конкретную кнопку, при нажатии которой исчезает sidebar

### Состояние кода
- `sidebarMap` в функции `nav()` обновлён с включением: `devices`, `sla`, `phone-warm`, `publishing-tokens`, `accounts-tokens`
- Был баг: `switchModule('farming')` не показывал `section-dashboard` и `sidebar-farming` явно
- Был баг: `openEditor()` для протоколов скрывал sidebar

---

## Instagram Reels navigation fix (28 Feb 2026, вечер)

### Проблема
- `initialize()` открывал Instagram через `MainTabActivity` — попадал на обычную ленту (фото)
- Нужно попадать на Reels (короткие видео с алгоритмом по ключевым словам)

### Решение
- `initialize()` теперь открывает Instagram через deep link `https://www.instagram.com/reels/`
- Добавлен метод `_navigate_to_feed()` вызывается из `_wait_for_app()` после загрузки приложения:
  - Instagram: тапает 3-ю иконку нижней навигации (X≈540, Y≈2150), проверяет UI dump, fallback — deep link
  - TikTok: логирует (For You feed уже правильный)
  - YouTube: логирует (Shorts уже открывается через deep link в initialize)
- Синтаксис проверен: `python3 -c "import warmer; print('✅ OK')"` — ОК

### Координаты (Samsung SM-A175F, 1080×2340)
- Нижний nav bar Y≈2140-2150
- Reels icon — 3-я из 5 иконок → X≈540
- Profile icon — 5-я → X≈972, Y≈2140 (используется в get_current_instagram_account)

### Статус
- Код изменён, синтаксис OK
- Реальный тест на устройстве ещё НЕ проведён — координаты предположительны

### Следующие шаги
1. Запустить задачу Instagram на RF8Y91F8EJH, проверить что попадает на Reels
2. При необходимости скорректировать Y-координату (2070 / 2140 / 2150)
3. Мониторить авто-запуск day 2 для task #5 (Instagram) и #6 (TikTok)

---

## Autowarm 2.0 — Claude API fix + End-to-end тесты + WhatsApp (2026-02-28, седьмая compaction)

### Claude API 404 → 200 fix
- Модели обновлены: `claude-3-haiku-20240307` → `claude-haiku-4-5`, `claude-3-sonnet` → `claude-sonnet-4-5`
- Обновлено в коде (`warmer.py`) И в DB (`autowarm_settings`)
- OAuth: `Authorization: Bearer <token>` + `anthropic-beta: oauth-2025-04-20` + `anthropic-version: 2023-06-01`
- PM2 рестарт с `--update-env` для прокидки env vars

### End-to-end тесты (все ✅)
- **Task #5 (Instagram, day 1)**: 39 видео, 9/9 лайков, watch phase ✅, smart scrolling работает
- **Task #6 (TikTok, day 1)**: 12/12 лайков, Claude audience analysis confirmed ✅
- **Task #7 (YouTube, day 1)**: 4/4 лайка ✅
- Day 2 auto-chain: task #5 resume_at ~2026-03-01 14:27 UTC, task #6 ~13:26 UTC

### UI table redesign
- "Устройство" → "№ девайса" (Pi number from piByPort mapping)
- "Протокол" → "Соцсеть" (platform icon+name: 📸 Instagram / 🎵 TikTok / ▶️ YouTube)
- Добавлена колонка "⏱ Мин." (elapsed minutes since started_at)
- colspan 10→11
- Bottom mobile nav hidden: `display: none !important`

### WhatsApp warmer (PAUSED)
- WhatsApp установлен на RF8Y91F8EJH, язык сменён на Қазақ тілі
- Проблема: страна "Ресей" (Russia), нужна "Қазақстан" для номера +77002184076
- ВСЕ ADB input методы заблокированы WhatsApp: input text, keyevent, ADBKeyBoard, Yosemite, sendevent
- UIAutomator2 server крашит устройство
- **User paused** — нужен root или ручная смена страны

### Token cost bug fix
- `/api/tokens/summary` — cost SQL query не имел `${srcFilter}` → farming и publishing показывали одинаковые суммы
- Добавлен `${srcFilter}` в WHERE clause cost-запроса (строка ~655 server.js)
- PM2 restarted, fix confirmed
