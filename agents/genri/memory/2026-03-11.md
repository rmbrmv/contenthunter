# 2026-03-11 — Дневной лог Генри

## Сессия с Даниилом Павловым (242574724) — раздел Публикации

### Документация
- Сформирован полный MD-документ по процессу публикации → `reports/publishing-process.md`
  - 5 фаз: уникализация → assign-queue → dispatch-queue → publisher.py → получение URL поста
  - Описаны все таблицы БД, API, крон-задачи, статусы

---

### Раздел Паков (#devices/packages)

#### Изменения в таблице паков
- Переставлены колонки: ID → Проект → Устройство → Название пакета → Соц.сети → Аккаунты → Даты → Действия
- Устройство теперь показывает **порядковый номер** (`#11`) вместо серийника
- Детект дублей только среди **активных** аккаунтов (inactive — OK)
- Визуальная подсветка дублей красным

#### Разбивка паков с дублями (⚡ Split)
- Реализованы 3 API: `GET /api/packages/audit/duplicates`, `GET /api/packages/:id/split-preview`, `POST /api/packages/:id/split`
- Алгоритм: активные аккаунты распределяются round-robin по паку a/b/c, неактивные остаются в оригинале
- Имена: `Проект_11` → `Проект_11a` + `Проект_11b`
- **Баг**: таблицы `factory_pack_accounts` и `factory_inst_accounts` без sequence → id надо передавать явно через `MAX(id)+1`
- UI: кнопки ⚡ в строках, аудит-модал, "Разбить все"

#### Защита от factory_sync.py
- Создана таблица `factory_sync_exclusions (table_name, row_id, reason)`
- После split → записываем защищённые строки
- `factory_sync.py` обновлён: фильтрует exclusions перед UPSERT
- **Проблема**: синк успел накатить обратно до добавления защиты → 28 дублей в родительских паках
- **Решение**: `POST /api/packages/repair-splits` — удаляет дубли из parent-паков (те что есть в child-паках)
  - Прогнали через psql напрямую: удалено 28 дублей
  - Подключились к Factory DB, получили оригинальные ID → 28 аккаунтов + 18 паков в exclusions

#### Валидация при добавлении аккаунта
- Бэкенд: проверка дубля по username+platform (всегда блок) + активного дубля по платформе (409 + флаг)
- Фронт: dropdown показывает все платформы с маркировкой `⚠️ активный есть` / `(есть откл.)`
- При конфликте — модал предупреждения вместо toast, кнопка "Добавить как неактивный"

---

### Раздел Публикации (#publishing)

#### Зависшая задача-зомби
- Задача #19 (TikTok, device RF8YA0W57EP) висела в `running` с 06.03 — 116 часов
- Блокировала dispatch для всего устройства (все 15 задач откладывались)
- Задачи 44-46 (Тест 3) запланированы на 12.03 — это норма
- **Исправлено**: сброшена в `failed`, добавлен авто-сброс зомби каждые 30 мин (`resetZombieTasks`)

---

### Технические детали
- Репо autowarm: `GenGo2/delivery-contenthunter` (push после каждого изменения)
- Репо factory_sync: `GenGo2/openclaw` (workspace-genri)
- factory_sync_exclusions: 28 account exclusions + 18 pack exclusions на 11.03

---

## Статус на конец блока (10:12 UTC)
- Паки: дубли устранены, защита от синка работает
- Публикация: устройство RF8YA0W57EP разблокировано, 15 задач пошли в работу
- Тест 3: задачи 44-46 ждут 12.03 05:00 UTC

---

## Блок 2 (10:18–14:27 UTC) — Publisher, Validator, TikTok

### Zombie-задачи в publish_queue
- При рестарте PM2 очищены 5 зомби-задач: queue#1,2,5 (instagram/inakent06), queue#4 (youtube/@Инакент-т2щ), queue#6 (tiktok/user70415121188138)
- `resetZombieTasks` расширен: покрывает `publish_tasks` (running/delegated >2ч, processing >6ч) + `publish_queue` (running/dispatched >2ч)

### Две вкладки в разделе Публикации
- Split UI на «📋 Запланировано» (publish_queue) и «📊 Результаты» (publish_tasks)
- Раздельные данные, раздельная загрузка, раздельная сортировка
- commit `6cbd291`

### awaiting_url — новый статус YouTube
- YouTube после загрузки устанавливает `awaiting_url` (вместо `processing`)
- Фоновый поллер (каждые 10 мин) ищет URL через `yt-dlp`
- Найден URL → `done`. 48 часов без URL → `failed`
- Zombie-reset НЕ трогает `awaiting_url`
- UI бейдж: `🔗 Ожидает URL` (фиолетовый)
- commit `3f3f1bb`

### YouTube URL scrape fix
- `axios_fetch` кодировал Cyrillic URL → YouTube редиректил на consent.youtube.com (GDPR) → скрейп давал null
- Переключили YouTube scrape на `yt-dlp`: `yt-dlp --flat-playlist --playlist-end 1 ...`
- 3 зависших задачи (#31, #22, #18) закрыты автоматически после деплоя
- commit `8184392`

### TikTok URL — UIAutomator-based grid detection
- `_auto_get_tiktok_url` тапал по фиксированным координатам (180, 1100–1200) → попадал на второй ряд → старое видео
- Заменено на `_find_tiktok_first_grid_cell()` — читает XML dump, находит верхний-левый элемент сетки
- commit `f026ef4`

### Лог событий (publish_tasks)
- API возвращал объект `{events, log, status, ...}`, фронт делал `Array.isArray(events)` → false → «Нет событий»
- Формат событий в БД: `{ts, msg, type}`, фронт смотрел на `timestamp/message` — не находил
- Исправлено: правильный парсинг + красивый timeline UI с иконками по типу события
- commit `4a44d8e`

### Validator — NameError: func
- `POST /schedule/slots` падал с `NameError: name 'func' is not defined`
- Причина: `func` из SQLAlchemy не был импортирован в `schedule.py`
- Фикс: `from sqlalchemy import select, func` — одна строка
- commit `0064ac9` (validator-contenthunter repo)

### Validator — удаление контента
- Добавлена кнопка «🗑 Удалить видео» на страницу `/content/:id`
- Модал подтверждения (Нет/Да, удалить)
- Бэкенд `DELETE /api/content/:id`: удаляет S3-файл, отвязывает слоты (→ empty), удаляет запись из БД
- commit `afc36c4` (validator-contenthunter repo)

### TikTok publish — geolocation fix
- Задача #36 (TikTok/user70415121188138): статус `done`, но публикации нет
- Причина: fallback координаты `(249, 1669)` для «Поделиться» попали на кнопку «Местоположение»
- TikTok открыл экран «Добавить местоположение» → ошибка «Функция недоступна в вашем регионе»
- Скрипт не детектировал проблему, сделал скриншот ЭТОГО экрана и вернул True
- **Три фикса:**
  1. Детект геолокации на всех шагах (редактор, описание, ожидание) + `KEYCODE_BACK`
  2. «Поделиться» только через UIAutomator текст/XML bounds — fallback-координаты убраны
  3. Верификация загрузки: 45 сек ждём `'Загружается'`/`'Uploading'`/`'Опубликовано'` → иначе `return False`
- commit `18ad39e`

## Статус на 14:27 UTC
- Autowarm: деплоено и перезапущено, все баги закрыты
- Validator: функция добавления слота работает, удаление контента работает
- TikTok: геолокация больше не ломает публикацию, есть верификация загрузки

## Сессия 17:01–17:10 UTC

### PM2 daemon crash (второй за день)
- Около 17:01 UTC PM2 daemon упал — все процессы были мертвы
- Поднято через `pm2 resurrect`, все 13 сервисов online
- Delivery доступен, validator-api в норме

### Validator admin users — fixes
- **Белый экран на /admin/users**: `/api/projects` возвращает массив напрямую `[...]`, а код делал `.data.projects` → undefined → Vue crash
  - commit `8ce4306`: парсинг с защитой (array или object.projects)
- **Алфавитная сортировка проектов**: `computed sortedProjects` через `localeCompare('ru')` для обоих списков (dropdown и checkbox)
  - commit `a3af9b4`
- **Поле логина в модале редактирования**: добавлено readonly поле «Логин» с подписью «Логин используется для входа в систему»
  - commit `d702d5e`

### Instagram publish — диагностический патч
- 100% отказ (8/8 задач), падение через ~3 мин, причина неизвестна
- В коде не было промежуточных log_event между "старт" и "ошибка"
- **Добавлено:**
  - Подробное логирование на каждый шаг (камера, REELS, галерея, редактор)
  - `_save_debug_screenshot(label)` — сохраняет скриншот устройства в задачу при ошибке
  - UI-дамп в лог при каждом неудачном шаге
  - REELS tab: 4 попытки с fallback, скриншот если не найден
  - Галерея: fallback tap + скриншот + log_event('error')
  - Редактор: каждый шаг логируется, при timeout — скриншот + log_event
  - commit `d06d968` (delivery-contenthunter)
- **Следующий шаг**: запустить Instagram задачу и прислать лог — узнаем точный шаг падения

### Аудит платформ (11.03.2026)
- Instagram: 8/8 failed, ~3 min после старта, точная причина unknown (нет логов шагов)
- YouTube: 2/3 done (задачи 21, 29), задача 37 — zombie-reset после 2ч (OK)
- TikTok: задачи 30, 33 — done с URL; задача 36 — false done (геолокация, починено); задача 39 — running

---

## Продолжение сессии (18:00–18:48 UTC)

### Проблема: "Создать задачу" давало 0 создано, 3 ошибки
- Кнопка «Создать задачу» отправляла запрос в `/api/publish/tasks` (прямой INSERT, минуя очередь)
- Правильный endpoint — `/api/publish/queue/manual` (создаёт pq-запись → dispatcher скачивает медиа → запускает publisher)
- Дополнительно: баг в `/api/publish/videos` — `uc.original_filename` не существует → 500 ошибка → "Нет доступных видео"
- Починено: endpoint переключён, колонка заменена на `ut.input_video_name`
- commit `b6c435e` (delivery-contenthunter)

### Проблема: Tasks table — пустые поля "Пак аккаунтов" и "Старт"
- Мусорный INSERT (task 44 — мой debug) создал запись без pack_name → удалён
- API `/api/publish/tasks` теперь: `COALESCE(pq.pack_name, subquery по device_serial → factory_pack_accounts)` для pack_name
- Для pending задач "Старт" показывает `pq.scheduled_at` со значком 📅
- commit `7a5c55b` (delivery-contenthunter)

### Критический баг: Планировщик не запускает pending задачи вечером
- **Причина**: `scheduler_active_hours_end = 21` (МСК), а сейчас 21:30 МСК → `isWorkingHours()` = false
- **Дедлок**: `loadSettings()` внутри `tick()` вызывается ПОСЛЕ `isWorkingHours()` → кэш настроек не обновлялся
- **Решение**: Обновил `scheduler_active_hours_end = 23` в DB + `pm2 restart autowarm` (чтобы `init()` перечитал настройки)
- Учесть: при следующем рестарте вечером — нужно убедиться что setting = 23

### Instagram диагностика (задача #45, 18:35–18:43 UTC)
**Подробный лог шагов:**
1. ✅ Pre-warm 5 видео (2 мин)
2. ✅ Камера открыта (попытка 0)
3. ✅ REELS таб найден и нажат
4. ✅ Галерея открыта
5. ⚠️ UIAutomator dump_ui — возвращает пустой XML (5 попыток, 7 секунд) → fallback тап (181,1000)
6. ✅ Редактор Reels открылся (fallback сработал)
7. ❌ Редактор: тапал Далее в (933,2021) — нижний правый угол, а НЕ правый верхний!
   - 20 шагов — всё время один экран → timeout → failed
8. **Диагноз**: неверные координаты кнопки «Далее» в редакторе Reels
9. **Фикс**: изменил на `(1050,80)` — верхний правый угол; ротация 4 вариантов; лог UI-дамп на шаге 0
   - commit `e6d0359` (delivery-contenthunter)
10. **Нужно**: дождаться следующей Instagram задачи и проверить что (1050,80) работает
    - Если нет — в логе будет UI-дамп с реальными элементами страницы → найти точные bounds кнопки

### Состояние очереди (18:48 UTC)
- pq=59 (instagram), 60 (tiktok), 61 (youtube) — pending, scheduled ~18:27 МСК
- pq=62 → task 45 → failed (instagram, неверные координаты редактора)
- pq=63, 64, 56–58 — pending, все ждут освобождения устройства RF8YA0W57EP
- Следующий dispatch: должен был сработать но устройство то занято то не занято
- pq=55 (youtube task 40): reset в failed вручную (PM2 restart убил процесс в 18:07)

### Ключевые ID для продолжения
- Device serial: RF8YA0W57EP, adb_port=15068, raspberry=7, host=82.115.54.26
- Последняя Instagram задача: #45 (failed: неверные координаты Далее)
- Следующая попытка Instagram: ждёт dispatch из pq=63 или pq=59
