# 2026-03-03 — Дневной лог

## 06:36 UTC — Рефлексия по итогам 2026-03-02

### Ключевые события дня:
- Virtual Office v5: AI-фон (gpt-image-1) + Pixi.js персонажи + HTML лейблы — архитектура утверждена
- Изучен репозиторий `maximgalson/ai-office-constructor` (Next.js + Pixi.js v8.6.6)
- HikerAPI рассмотрен как альтернатива ADB для Instagram аналитики — решено не регистрировать пока
- LaoZhang баланс ~18K кредитов (после генерации фона)

### Открытые задачи:
- Калибровка позиций персонажей в офисе (висят в воздухе — нужно опустить на столы)
- POSITIONS дублируются в index.html и characters.js — вынести в один источник
- Удалить неиспользуемый office_pixi.js
- Роман недосказал мысль ("Я такое придумал") — узнать о чём речь

### Состояние сервисов:
- office.contenthunter.ru (порт 3847) — работает, фон office-bg-gen.png
- Все остальные PM2 процессы без изменений

## 06:51 UTC — Zoom Playwright бот (Кира)

### Сделано:
- Написан `zoom_playwright.py` — Кира заходит в Zoom через Playwright + PulseAudio
- Архитектура: Xvfb (виртуальный дисплей) + Chromium headless + PulseAudio virtual sink/source
- Пайплайн: захват аудио с kira_out.monitor → STT (gpt-4o-transcribe) → LLM (claude-sonnet) → TTS (gpt-4o-mini-tts) → paplay → kira_out → Zoom mic
- Добавлены endpoints в server.py: POST /join, POST /leave, GET /status
- PulseAudio установлен (v16.1), Playwright + Chromium работает (протестировано)
- kira-voice PM2 перезапущен, отвечает на :4501/health

### Использование:
```
POST http://localhost:4501/join {"url": "https://zoom.us/j/ID?pwd=PASS"}
GET  http://localhost:4501/status
POST http://localhost:4501/leave
```

### Ждём:
- Zoom ссылку от Романа для реального теста

---

## Snapshot 07:30 UTC — Zoom Voice Agent (Кира)

### Сессия отладки Zoom бота
- Роман открыл Zoom звонок: `us05web.zoom.us/j/5681070814?pwd=73vLhQBi2783bYkzwkgW8Jxt6UPSoU.1`
- Кира физически видна в звонке (скриншот подтверждён — 3 участника: Danil Pavlov, Роман, Кира)

### Баги найдены и исправлены:
1. **SIGTERM убивал бот** — причина: `signal.signal()` + asyncio конфликт. Решение: убрали signal handlers полностью
2. **Cookie banner** перекрывал форму входа — добавили `OptanonConsent` cookie до навигации
3. **Selector для имени** — `#input-for-name` не работает, работает `input[placeholder*="Name"]`
4. **`.preview-join-button`** — не находится после cookie redirect; работает `button:has-text("Join")`
5. **`play_in_browser` зависал** — AudioContext в headless без user gesture; убрали аудио полностью
6. **send_chat selector** — `[placeholder*="To:"]` сломан в новом Zoom UI
7. **preexec_fn=os.setsid** — добавлено в server_pw.py чтобы бот не получал SIGTERM от aiohttp

### Текущий статус zoom_playwright.py v3:
- Заходит стабильно ✅
- Имя "Кира" вводит ✅ (sel: `input[placeholder*="Name"]`)
- Ждёт Admit в waiting loop (each 3s, max 200 iter) ✅
- Детект впуска: ищет слова mute/leave/chat в body текста
- После впуска: `try_send_chat()` + `get_chat()` loop (chat-only, без аудио)
- chat send selectors: `[aria-label="Type message here"]`, `div[contenteditable="true"][role="textbox"]`

### Что НЕ проверено:
- Реальный ответ Киры в чат (не дошли до этого шага — Roman не нажал Admit)
- Детект впуска через body text (ещё не сработал на реальном звонке)

### Ключевые файлы:
- `/root/.openclaw/workspace-genri/zoom-voice-agent/zoom_playwright.py` — v3, текущий
- `/root/.openclaw/workspace-genri/zoom-voice-agent/server_pw.py` — aiohttp сервер порт 4501
- PM2: `kira-voice` (id=13) → запускает `server_pw.py`

### Следующие шаги:
1. Роман нажимает Admit → проверяем детект впуска
2. Роман пишет "Кира, привет" → проверяем ответ в чат
3. Если send_chat не работает — искать реальный selector в живом DOM после впуска

---

## Snapshot 07:40 UTC — Kira Voice Bot + delivery.contenthunter.ru

### Kira статус (07:40)
- Бот работает стабильно: 36+ итераций цикла без падений ✅
- Основной фикс: убрали `signal.signal()` — именно он вызывал STOP=True из-за конфликта с asyncio
- `preexec_fn=os.setsid` добавлен в server_pw.py
- Вход: cookie preset + `input[placeholder*="Name"]` + `button:has-text("Join")`
- После впуска: loopback AudioContext (TTS → Zoom mic), читает captions + чат
- **Слышать**: только через Zoom Live Captions (CC) — DOM scraping. Без CC — только чат.
- Roman спросил зачем субтитры → объяснено: headless браузер не захватывает аудио напрямую

### Kira архитектурное решение (финальное):
- **Говорит**: TTS (gpt-4o-mini-tts shimmer) → base64 → AudioContext loopback → Zoom mic ✅
- **Слышит вариант 1**: Live Captions (CC) → DOM scraping → LLM → ответ
- **Слышит вариант 2**: Zoom чат → DOM scraping → LLM → голосовой ответ
- Roman предпочтительный вариант: не выбран ещё

### delivery.contenthunter.ru — сессия доработки начата
- Roman хочет довести сервис до работоспособного состояния
- Дана полная архитектура: 17 UI секций, 9 python-воркеров, 47 таблиц в БД
- Конкретный модуль для доработки Roman ещё не назвал (сессия прервалась)
- Следующий шаг: дождаться от Roman что именно доработать

### Дублирование ответов
- Замечено дублирование сообщений (отправлял один ответ дважды)
- Причина неясна — возможно глюк Telegram или OpenClaw
- Roman написал "???" — заметил дублирование

---

## Snapshot 07:56 UTC — Kira архитектура (финальное решение)

### Roman's требование (жёсткое):
- "Как человек с человеком" — живой голос ↔ живой голос
- Никаких субтитров, чатов, костылей
- Всё остальное выбросить

### Правильная архитектура для реального голоса:
```
Zoom аудио → PulseAudio (виртуальная карта)
           → Python захватывает → Whisper STT
           → LLM → TTS → PulseAudio
           → обратно в Zoom как микрофон
```
- Нужен отдельный Linux-пользователь `kira`
- PulseAudio под non-root (root не работает — известный блокер)
- Chromium (non-headless) + Xvfb под тем же пользователем
- ALSA loopback (`snd-aloop`) для захвата аудио
- Оценка: 2-3 дня работы

### Приоритет:
- Roman не ответил на вопрос "что сначала — Кира или delivery?"
- Сессия прервалась на этом месте
- Следующий шаг: уточнить приоритет у Roman

### Текущий статус Kira bot:
- Работает как чат-бот в Zoom (читает чат, отвечает голосом через loopback)
- Голосовой ввод НЕ работает без CC
- Для полноценного голоса нужна переработка архитектуры (PulseAudio + non-root)

---

## Snapshot 08:13 UTC — БД доступ открыт

### PostgreSQL внешний доступ
- БД в Docker-контейнере `openclaw-postgres` (pgvector/pgvector:pg16)
- Порт 5432 уже был проброшен наружу через Docker (`0.0.0.0:5432->5432/tcp`)
- Публичный IP сервера: `72.56.107.157`
- Roman подключается через DBeaver

### Реквизиты (публичные):
- Host: 72.56.107.157 / Port: 5432
- DB: openclaw / User: openclaw / Pass: openclaw123

### Kira приоритет
- Roman сказал "в порядке очереди просто сделай"
- Значит сначала delivery.contenthunter.ru, потом Кира с голосом
- Для голоса нужна архитектура: PulseAudio non-root + ALSA loopback (2-3 дня)

### Планы на сессию
- Доработка delivery.contenthunter.ru до работоспособного состояния
- Roman подключился к БД через DBeaver — будет смотреть данные

---

## Snapshot 08:35 UTC — GitHub репозиторий

### Новый GitHub аккаунт
- Аккаунт: **GenGo2**
- Токен: `[REDACTED-GITHUB-TOKEN]`
- Репозиторий: **https://github.com/GenGo2/delivery-contenthunter** (приватный)
- Запушен initial commit: 21 файл, вся кодовая база autowarm
- Remote настроен в `/root/.openclaw/workspace-genri/autowarm/.git/config`

### Следующие шаги по delivery.contenthunter.ru
- Roman подключился к БД через DBeaver ✅
- Код на GitHub ✅
- Ждём от Roman что конкретно доработать

---

## Snapshot 08:41 UTC — Доступы к сервисам

### Таблица доступов (актуальная)
| Домен | Порт | Логин | Пароль |
|-------|------|-------|--------|
| delivery.contenthunter.ru | 3848 | rmbrmv@gmail.com | HMqsAEc33U!D8uB |
| validator.contenthunter.ru | 8000 | admin | hunter2025 |
| producer.contenthunter.ru | 3850 | rmbrmv@gmail.com | HMqsAEc33U!D8uB |
| carousel.contenthunter.ru | 3851 | master pass | Bo37H#Kla8dl0chQnL0@3jSlcY |
| hr.contenthunter.ru | 3852 | неизвестен | нужно уточнить |
| office.contenthunter.ru | 3847 | без авторизации | — |
| tasks.contenthunter.ru | 3849 | без авторизации | — |
| dashboard.contenthunter.ru | 3000 | сервис не запущен | — |

### GitHub GenGo2
- Токен: [REDACTED-GITHUB-TOKEN]
- Repo: https://github.com/GenGo2/delivery-contenthunter (приватный)

---

## Snapshot 08:56 UTC — Validator репозиторий + сессия

### Validator репозиторий
- https://github.com/GenGo2/validator-contenthunter (приватный)
- Структура: backend/ (FastAPI, Python, uvicorn port 8000) + frontend/ (Vue.js, /var/www/validator) + docker-compose.yml
- Код: /root/.openclaw/workspace-genri/validator/

### Текущая сессия с Roman (08:00-08:56)
- Подключился к БД через DBeaver ✅
- Код autowarm на GitHub GenGo2 ✅
- Код validator на GitHub GenGo2 ✅
- Получил доступы ко всем сервисам ✅
- Следующий шаг: доработка validator.contenthunter.ru

### HR сервис пользователи
- roman (admin), kirill (admin), dina (editor), tatiana (admin)
- Пароли bcrypt — оригиналы неизвестны
- Roman не просил сбрасывать пароль

---

## Snapshot 09:02 UTC — Validator: вкладка Клиенты

### Реализовано
- Таблица `validator_projects` в openclaw DB: 39 проектов скопированы из factory DB
- Поля: id, project, api_name, active, logo_url, manager
- Backend: `/root/.openclaw/workspace-genri/validator/backend/src/routers/projects.py` (SQLAlchemy async)
- Frontend: `/root/.openclaw/workspace-genri/validator/frontend/src/pages/admin/ProjectsPage.vue`
- Роут: `/clients` (roles: manager, producer, admin)
- Sidebar: NavItem 🏢 Клиенты после Аккаунты
- Фронт пересобран и задеплоен в `/var/www/validator/`
- Запушено на GitHub GenGo2/validator-contenthunter

### Технические детали validator backend
- SQLAlchemy async (не asyncpg напрямую)
- get_db из `..database`, get_current_user из `..dependencies`
- `from sqlalchemy import text` для raw SQL запросов
- Роль проверяется через `current_user.role.value`
- Фронт: `import api from '@/api/client'` (default export, не named)
- build команда: `npm run build` в frontend/ — автоматически деплоит в /var/www/validator/

### Factory DB структура (для справки)
- factory_projects: id, project, api_name, active
- factory_project_logos: id, project_id, file_name, file_path
- logo_url формат: https://app.contenthunter.ru/media/{file_path}
- Всего 39 проектов, активных ~28

---

## Snapshot 10:10 UTC — Validator: разделы Клиенты + Контент Уник

### Реализовано за сессию (09:00-10:10)
1. **Раздел Клиенты** (`/clients`) — 39 проектов из factory_projects
2. **Раздел Контент Уник** (`/unic-content`) — 429 записей из factory_unic_content
3. **Паттерн таблиц** — утверждён Roman, записан в approved-patterns.md

### Контент Уник технические детали
- Таблица: `validator_unic_content` в openclaw DB
- Поля: id, content_type, label, usage_type, project_id, duration, size, file_path, chromakey_color
- JOIN с validator_projects для project_name
- Backend: `/root/.openclaw/workspace-genri/validator/backend/src/routers/unic_content.py`
- Frontend: `/root/.openclaw/workspace-genri/validator/frontend/src/pages/admin/UnicContentPage.vue`
- Роут: `/unic-content` (roles: manager, producer, admin)

### Утверждённый паттерн таблиц (обязательный для всех таблиц)
1. Строка поиска сверху (max-w-md)
2. Заголовки с сортировкой: asc → desc → off (компонент SortIcon)
3. Строка фильтров в thead (bg-indigo-50/50): инпуты + select из реальных значений
4. Кнопка "Сбросить" если есть активные фильтры
5. Счётчик "Показано X из Y" в футере
6. Кнопка "Добавить" вверху справа (bg-indigo-600)
7. Колонки действий: ✏️ редактировать + 🗑️ удалить
8. Модал редактирования с предзаполнением
9. Модал удаления с подтверждением и именем записи

### Validator backend паттерн
- SQLAlchemy async + `from sqlalchemy import text` для raw SQL
- get_db из `..database`, get_current_user из `..dependencies`
- Роль: `current_user.role.value != "admin"`
- Регистрация в main.py: import + app.include_router()
- Frontend API: `api.get('/endpoint')` (без /api/ префикса — baseURL уже /api)

### Следующие шаги
- Продолжаем доработку validator.contenthunter.ru
- Roman ещё не указал что именно доработать дальше

---

## Snapshot 10:40 UTC — Миграция factory DB → openclaw

### Выполнено
- Создана схема `factory` в openclaw DB (localhost:5432)
- Скопировано 34 таблицы из factory DB (193.124.112.222:49002) в factory.* схему
- Autowarm server.js переключён с factory DB на localhost (distPool)
- Таблицы с JSONB скопированы отдельно через psycopg2 Json адаптер

### Что скопировано (ключевые)
- factory.device_state (32594 rows), factory.raspberry_port (10 rows)
- factory.factory_inst_accounts (1126), factory.factory_inst_reels (62371)
- factory.factory_unic_queue (5502), factory.factory_unic_results (10836)
- factory.factory_unic_schemes (30), factory.factory_unic_tasks (1317)
- factory.creator_anket (2121), factory.contentlab_videos_upload (1381)
- + 24 более мелкие таблицы

### Пропущено (ждём решения Roman)
- factory_inst_reels_stats (816 MB) — нужна/нет?
- factory_inst_reels_stats_old (344 MB) — нужна/нет?
- factory_parsing_logs (27 MB) — нужна/нет?

### Пропущено (намеренно)
- backups, auth (users/roles/refresh_tokens), alembic_version, schedule
- validator_* таблицы из factory (пустые, наши есть)

### Паттерн
- Все factory таблицы в схеме `factory.` нашей БД
- В SQL запросах: `FROM factory.device_state` и т.д.
- Для JSONB: psycopg2.extras.Json() адаптер при insert

---

## Snapshot 12:03 UTC — Доработки delivery.contenthunter.ru

### Контент Уник (в Устройствах)
- Перенесён из validator в delivery.contenthunter.ru → вкладка Устройства → пункт «🎬 Контент Уник»
- API в autowarm/server.js: GET/PUT/DELETE /api/unic-content, GET /api/projects-list
- Таблица на весь экран (убран max-w-7xl), sticky headers, фильтры, сортировка
- Колонка Действия: ✏️ редактировать + 🗑️ удалить с модалами
- Поле "Проект" в модале — дропдаун с названиями из validator_projects (не ID)
- Поле "Путь к файлу" — иконка 🔗 слева, кликабельная, открывает ссылку в новой вкладке

### Порядок вкладок (обновлён по требованию Roman)
Новый порядок: 📱Устройства → 👤Аккаунты → 🔥Фарминг → 🎨Уникализация → 📤Выкладка → 👥Управление → 📊Аналитика
- Дефолтная вкладка при открытии: Устройства (было: Фарминг)
- Мобильная навигация обновлена в том же порядке

### Паттерн: иконка-ссылка в поле файла
```html
<div class="flex items-center gap-2">
  <a id="link-id" href="#" target="_blank" onclick="return !!inputEl.value"
     oninput linked to input value>🔗</a>
  <input id="input-id" oninput="linkEl.href=this.value||'#'; linkEl.style.opacity=this.value?'1':'0.4'">
</div>
```

---

## Snapshot 12:33 UTC — Рефакторинг таблиц delivery.contenthunter.ru

### Статус
- coding agent упал (--dangerously-skip-permissions запрещён под root)
- Делаю вручную
- Резервная копия: autowarm/public/index.html.bak

### Что нужно сделать
Применить паттерн ко всем ~20 таблицам autowarm/public/index.html:
- overflow-auto + max-height calc(100vh-240px)
- sticky top-0 заголовки + sticky top-[45px] фильтры
- Убрать max-w-*xl из секций
- Поиск + сортировка + фильтры + счётчик

### Паттерн (эталон: section-unic-content)
- Секция: class="section px-4 py-6 fade-in" (без max-w)
- Контейнер таблицы: overflow-auto style="max-height: calc(100vh - 240px)"
- thead tr 1: sticky top-0 z-10 shadow-sm bg-gray-50
- thead tr 2 (фильтры): sticky top-[45px] z-10 bg-indigo-50
- Поиск: input над таблицей, max-w-md

### Таблицы для обновления
section-devices, section-packages, section-tasks, section-sla,
section-publishing*, section-whatsapp*, section-telegram*,
section-factory*, section-accounts-tokens, section-analytics,
section-uniqualization, section-admin

---

## Snapshot 13:14 UTC — Validator: Исходники + UI правки

### Раздел Исходники (validator.contenthunter.ru/sources)
- Данные: `factory.contentlab_videos_upload` (1381 записей)
- Колонки: ID, Дата, Загрузил, Файл (ссылка), Проект, Артикул, Геотег, Статус, Комментарий, Действия
- Права: client=свои проекты, manager=проекты своих клиентов, producer/admin=все
- Статус меняется через дропдаун, комментарий — кликом inline
- Backend: `/root/.openclaw/workspace-genri/validator/backend/src/routers/sources.py`
- Frontend: `/root/.openclaw/workspace-genri/validator/frontend/src/pages/client/SourcesPage.vue`
- Роут: `/sources` (roles: client, manager, producer, admin)
- Таблица factory_users создана (loader_id → имя загрузчика)

### Изменения в Sidebar валидатора
- Секция "Клиент" добавлена как заголовок (была безымянной)
- Пункт "Исходники" (📁) добавлен в секцию Клиент
- Пункт "Клиенты" перемещён в секцию Админ (только для admin)
- "Залетаемость" → "Виральность" переименовано

### Validator роутер паттерн
- Файл: `/backend/src/routers/<name>.py`
- Регистрация в main.py: import + app.include_router()
- Фронт: `/frontend/src/pages/<section>/<Page>.vue`
- Роут в router/index.ts + NavItem в AppSidebar.vue

### Delivery.contenthunter.ru изменения (сегодня)
- Порядок вкладок: Устройства→Аккаунты→Фарминг→Уникализация→Выкладка→Управление→Аналитика
- Раздел "Контент" (бывший "Контент Уник") — в вкладке Уникализация
- Все таблицы: max-height calc(100vh-240px), sticky headers, убраны max-w-*xl
- sidebarMap/sidebarMap2 обновлены для unic-content → uniqualization

---

## Snapshot 13:27 UTC — Синхронизация исходников

### Sync скрипт
- Путь: `/root/.openclaw/workspace-genri/validator/sync_sources.py`
- Инкрементальная синхронизация: factory DB → local openclaw DB
- Тянет: factory.contentlab_videos_upload (новые id + обновлённые статусы/комментарии)
- Также синхронизирует factory_users (теперь 15 записей)
- Крон: `*/15 * * * *` → `/var/log/validator_sync.log`

### Кнопки действий в Исходниках (зависят от статуса)
- Загружено → 🗑️ Удалить
- В обработке / В очереди → нет кнопок
- Сконвертировано → ↙ Утилизировать (SVG: circle→3circles) + 🗑️ Удалить
- Уникализировано → ↙ Уникализировать + 👁️ Проверить + 🗑️ Удалить
- DELETE /api/sources/:id эндпоинт добавлен в backend

---

## Snapshot 15:16 UTC — Syncthing + синхронизация

### Syncthing установлен на сервере
- Version: v1.27.2-ds4
- Systemd сервис: syncthing.service (enabled, running)
- Config: /root/.config/syncthing/config.xml
- GUI: http://72.56.107.157:8384 (порт открыт)
- Sync порт: 22000 tcp/udp (открыт)
- **Device ID сервера: YGR7WA5-IBR3YDW-IRFEBTW-XDYX4Z6-KD5REWP-B2T4DCJ-4LDDLQD-EVNI2QS**

### Папка синхронизации
- Path: /root/.openclaw
- Folder ID: openclaw-all
- Label: OpenClaw
- .stignore: node_modules, .git, __pycache__, logs, media/inbound, dist, .vite

### Что ждём от Roman
- Device ID его ПК → добавить удалённое устройство на сервере
- GitHub sync для workspace - ещё не настроен (второй этап)

### Исходники - синхронизация (cron)
- Скрипт: /root/.openclaw/workspace-genri/validator/sync_sources.py
- Крон: */15 * * * * → /var/log/validator_sync.log

---

## Snapshot 15:50 UTC — GitHub backup + Syncthing

### GitHub repo GenGo2/openclaw
- URL: https://github.com/GenGo2/openclaw (private)
- Первый коммит: 3822 файла, 2026-03-03 15:23
- Крон пуша: `0 3 * * *` → /var/log/openclaw_git_backup.log
- Скрипт: /root/.openclaw/workspace-genri/scripts/git_backup.sh
- .git инициализирован в /root/.openclaw/
- .gitignore: node_modules, __pycache__, media/inbound, *.mp4/mp3/png/jpg, dist, *.log, .git вложенные

### Syncthing
- Roman PC Device ID: PFOIFZL-MM4A2IM-5U3BWNR-LVAECAJ-M2OIWSN-DJGKITR-O4C4ZDW-DQU5WQQ
- Папка: /root/.openclaw (всё, без исключений)
- 116638 файлов, 16.36 GB
- Порты: 22000 tcp/udp (sync), 8384 tcp (GUI) — открыты
- GUI: http://72.56.107.157:8384

### Итог системы синхронизации
- Syncthing = реалтайм P2P (сервер ↔ ПК Roman)
- GitHub = ежедневный бэкап в 03:00 UTC

---

## Snapshot 16:41 UTC — Изучение репозиториев GenGo2

### cpm_hunter_backend
- FastAPI бэкенд, Python, Celery + Redis, Poetry
- Роуты: autowarm, devices, projects, auto_public, users
- Старый/параллельный сервис для автоварма — не связан с delivery напрямую
- Path на сервере: не задеплоен

### arm_cpm_hunter
- Полноценное приложение: Vue 3 + Vite + Tailwind (тёмная тема) + FastAPI
- JWT + cookie auth, S3, slowapi rate limiting, SQLAlchemy sync
- Работает с: factory_unic_results, upload_ordes, contentlab_videos_upload, factory_projects
- Разделы: ContentLab (загрузка/журнал), Автовыкладка, Уникализация, Фарминг, Устройства, AI Чат, График
- Предшественник/параллельная версия validator — многое уже реализовано
- Path на сервере: не задеплоен, только в GitHub

### Статус
- Роман изучает репозитории — решение о дальнейшем использовании не принято

---

## Snapshot 17:14 UTC — arm_cpm_hunter DeviceModal доработка

### Задача от Roman
Доработать DeviceModal в arm_cpm_hunter:
1. Индикатор активности аккаунта (✅ жирный / □ обычный) — в списке и в форме
2. Переключение active в модале редактирования → сохранять в БД
3. Отображение аккаунтов пака → таблица с колонками:
   - Платформа, Username, user_id (readonly + копировать), Активность, Дата последнего парсинга
4. Кнопка "Добавить новый аккаунт" → новая строка → сохранить (✅) / удалить (❌)
5. Кнопка карандаша для редактирования существующих строк

### Структура БД factory_inst_accounts
- id, instagram_id, username, platform, active, pack_id
- date_last_parsing — есть в реальной БД, но НЕТ в ORM модели
- instagram_id заполняется асинхронно через n8n webhook

### Уточняющие вопросы заданы Roman (ждём ответа)
1. date_last_parsing — read-only или редактируемое?
2. Переключение active: через PATCH устройства ИЛИ отдельный PATCH /accounts/{id}/toggle-active?
3. Где задеплоен app.contenthunter.ru — это arm_cpm_hunter или другой проект?
4. instagram_id при создании аккаунта всегда пустое (заполняется через n8n) — верно?

### cpm_hunter_backend (отдельный сервис)
- FastAPI + Celery + Redis + Poetry
- Роуты: autowarm, devices, projects, auto_public, users
- После insert в factory_inst_accounts → send_instagram_creds() → вебхук n8n → instagram_id callback
- НЕ задеплоен на сервере

### arm_cpm_hunter backend models
- Device → PackAccount (cascade) → FactoryInstAccount (cascade)
- AccountChangeReason — причины замены аккаунта
- Эндпоинты: POST/GET/PUT/PATCH/DELETE devices, DELETE packs, POST account-change-reasons
- PATCH = только существующие сущности; PUT = синхронизация новых + существующих

### Пути репозиториев
- /tmp/cpm_hunter_backend/ — клонирован
- /tmp/arm_cpm_hunter/ — клонирован

---

## Snapshot 17:40 UTC — DeviceModal & accounts table

### Что сделано
- cpm_hunter_backend: добавлены 4 новых эндпоинта для аккаунтов
  - PATCH /accounts/{id}/toggle-active
  - POST /accounts?pack_id=X
  - PATCH /accounts/{id}
  - DELETE /accounts/{id}
- FactoryInstAccount модель: добавлен date_last_parsing, BigInteger для instagram_id
- AccountResponse схема: добавлены active, instagram_id, date_last_parsing
- arm_cpm_hunter DeviceModal.vue: полностью переписан — аккаунты теперь таблица
  - Колонки: Платформа | Username | User ID (copy) | Активность | Дата парсинга | Действия
  - ✅ + жирный = активный; ☐ + обычный = неактивный
  - Инлайн добавление/редактирование/удаление строк
- DevicesPage.vue: список аккаунтов показывает ✅/@username vs ☐/@username
- api/index.js: добавлены toggleAccountActive, createAccount, updateAccount, deleteAccount

### Ветки
- cpm_hunter_backend → создана ветка main (была master), PR нужен
- arm_cpm_hunter → main (прямой push)

### Статус деплоя
- Ждём ручного деплоя от Roman на app.contenthunter.ru (нет SSH доступа)

### Ключевые решения
- date_last_parsing — read-only (заполняется парсерами), отображается только
- instagram_id — заполняется асинхронно n8n-скриптом после создания аккаунта
- Переключение active — отдельный PATCH /accounts/{id}/toggle-active (атомарный)
- Доступ к серверу app.contenthunter.ru — НЕТ (другие сервера), только GitHub

---

## Snapshot 18:01 UTC — cpm_hunter fixes

### Реальная схема factory_inst_accounts (193.124.112.222:49002)
- id: integer (identity)
- username: text
- instagram_id: TEXT (не integer! хранит числа + YouTube UCxxx IDs)
- platform: text
- active: boolean, default true
- pack_id: integer → FK pack_accounts.id ON DELETE RESTRICT
- date_last_parsing: DATE (не timestamp!)
- Итого: 1126 аккаунтов, 1116 с instagram_id, 609 с date_last_parsing

### Исправления в cpm_hunter_backend
- instagram_id: BigInteger → String
- date_last_parsing: DateTime → Date
- AccountResponse: instagram_id: Optional[str], date_last_parsing: Optional[date]

### Исправления в arm_cpm_hunter
- Убран ✅/☐ из колонки Username (только в колонке Активность)
- Формат даты упрощён до ДД.ММ.ГГ

### Архитектура app.contenthunter.ru
- arm_cpm_hunter = фронт (Vue) + часть бэкенда (auth, content, upload)
- cpm_hunter_backend = отдельный сервис (autowarm, devices, projects)
- Фронт обращается к cpm_hunter_backend через VITE_AUTOWARM_API_URL
- Оба деплоятся на серверах Roman (нет SSH доступа, только GitHub)

### Все коммиты запушены в main
- cpm_hunter_backend: master → HEAD:main
- arm_cpm_hunter: main

---

## Snapshot 18:42 UTC — DeviceModal финальные правки

### Проблема с GET /devices/{id}/accounts
- Новый эндпоинт возвращал 404 — cpm_hunter_backend не задеплоился с новым кодом
- URL деплоя: dev.contenthunter.ru (не app, а dev окружение)

### Итоговое решение (без нового эндпоинта)
- openEditModal в DevicesPage: перезагружает ВСЕ устройства через существующий GET /devices, находит нужное по id
- DeviceModal: убран отдельный refreshAccountsData (был 404)
- Данные instagram_id и date_last_parsing приходят через device prop (из свежего GET /devices)
- Это работает только если cpm_hunter_backend задеплоен с новым AccountResponse (включает оба поля)

### Checkbox активности
- View mode: disabled + pointer-events-none (только отображение)
- Edit mode (карандаш): кликабельный checkbox, при сохранении вызывает toggleAccountActive если значение изменилось

### Статус commitов
- arm_cpm_hunter: main ✅ (545fa83)
- cpm_hunter_backend: master/HEAD:main ✅ (9a85110) — новый эндпоинт добавлен но не критичен

---

## Snapshot 18:59 UTC — КРИТИЧЕСКАЯ ОШИБКА исправлена

### Root cause: неправильная ветка для деплоя
- cpm_hunter_backend деплоится с ветки **master** (origin/HEAD → origin/master)
- Я пушил в **origin/main** — все мои изменения НЕ попадали на сервер
- Исправил: git push origin master
- Теперь origin/master содержит все 4 коммита с исправлениями

### Урок (в anti-patterns.md нужно записать)
- Всегда проверять дефолтную ветку репозитория ДО первого пуша!
- git branch -a → смотреть "remotes/origin/HEAD ->"
- cpm_hunter_backend: дефолтная ветка = master (не main)
- arm_cpm_hunter: дефолтная ветка = main ✅

### Что должно заработать после деплоя cpm_hunter_backend с master
- GET /devices возвращает instagram_id (String) и date_last_parsing (Date) в AccountResponse
- GET /accounts/{id} — свежие данные аккаунта
- GET /devices/{id}/accounts — bulk endpoint
- PATCH /accounts/{id}/toggle-active — переключение активности
- POST /accounts — создание аккаунта
- PATCH /accounts/{id} — обновление аккаунта
- DELETE /accounts/{id} — удаление аккаунта
