# 2026-03-13 — Дневной лог Генри

## Сессия 06:00–06:34 UTC — Отладка публикаций после вчерашних падений

### Анализ провалов 12.03 (tasks #111–#120)

**Instagram #111, #112, #116, #120** — "редактор не дошёл до подписи (timeout 20 шагов)"
- Причина: `REELS_EDITOR_MARKERS` не содержал реальных маркеров редактора
- Маркеры `['Предпросмотр видео Reels', 'воспроизводится', 'Добавить аудио']` никогда не срабатывали
- Фикс `3617d60` (12.03 вечером): добавлены `'Выйти из редактора', 'Обрезка', 'Фильтры', 'Стикеры'`

**YouTube #113, #115, #118** — тихий timeout 25 шагов
- Editor loop ни разу не логировал что на экране → неизвестно что происходит
- Фикс `da8b866`: `log_event` каждые 3 шага + агрессивный тап top-right clickable + fallback `(1030,120)`

**TikTok #117** — все fallback coords промахнулись
- Попробованы: `(980,110)`, `(980,65)`, `(980,150)` — ни одна не попала
- Скриншоты debug оказались 0 байт — баг в `_save_debug_screenshot`
- Фикс `da8b866`: `screencap -p file` вместо `exec-out screencap -p > file`

**Instagram #119** — Поделиться нажата, но confirmation loop 30 итераций не нашёл MainTabActivity
- Confirmation loop не вызывал `dismiss_overlay_dialogs()` → overlay мог блокировать
- Не было диагностики (что на экране, какой activity)
- Фикс `da8b866`: dismiss overlays в loop + `log_event` каждые 5 итераций

### Новый баг обнаружен (06:30 UTC)
**run_mini_warm открывал редактор вместо ленты** — приложение возобновлялось с предыдущего состояния
- Не было `am force-stop` перед стартом в `run_mini_warm()`
- Фикс `0747c12`: добавлен `am force-stop + KEYCODE_WAKEUP` перед `am start` в преварме

### Результаты запуска
- task #121 Instagram (pq #116) → **awaiting_url** ✅ 06:31:39 UTC
- Очередь pq #116–#124 сброшена в pending, пойдёт последовательно

### Коммиты за сессию
- `da8b866` — fix: screencap, YouTube диагностика, Instagram confirmation overlays
- `0747c12` — fix: run_mini_warm force-stop перед стартом

### TikTok кнопка публикации — TODO
- Координаты `(980,65/110/150)` не работают
- Нужен ручной прогон с observer для точных координат
- `/tmp/obs_tiktok.py` — готов для запуска

### Очередь на 13.03
- pq #116–124: Instagram/TikTok/YouTube тройки @inakent06/@user70415121188138/@Инакент-т2щ
- task #121 Instagram — awaiting_url (06:31)
- остальные в pending, будут обработаны последовательно

---

## Сессия 13.03.2026 — Покадровый анализ видео (09:00–09:49 UTC)

### Анализ видео ручной публикации
Пользователь загрузил три видео записи экрана через Google Drive (gdown):
- tiktok.mp4 (34.3MB) — оказалась запись Instagram-публикации
- instagram.mp4 (44.2MB) — оказалась запись TikTok-публикации
- youtube.mp4 (30.1MB) — запись YouTube-публикации
Разрешение всех трёх видео ~588x1278 (scale 1.83x к 1080x2340 устройству)

### ✅ Критические находки — правильные координаты кнопок

**Все предыдущие fallback-координаты были НЕВЕРНЫМИ!**
Старый (1030,120) — это правый верхний угол, там нет кнопок публикации.

| Платформа | Кнопка | Координаты на 1080×2340 |
|---|---|---|
| TikTok | Опубликовать | **(816, 2130)** — самый низ, правая половина |
| YouTube | Загрузить | **(791, 2051)** — самый низ, правая половина |
| Instagram | Поделиться | **(808, 1907)** — нижняя треть |

Подтверждено двумя источниками для TikTok:
- Видео (588x1278): (440,1152) → устройство: (808,2109)
- Screencap (720x1560): (550,1430) → устройство: (825,2145)

### Маркеры успеха (из видео)
- **Instagram**: тост «Публикация в Reels...» → в ленте кнопки «Статистика» + «Продвигать видео Reels»
- **YouTube**: после «Загрузить» → переход на канал → «Ваши видео» → видео «7 секунд назад»
- **TikTok**: индикатор загрузки ~95% в левом верхнем углу ленты (загрузка в фоне)

### YouTube flow (из видео)
1. Главная → «+» (540,2160)
2. Trim screen → «Готово» (923,2109)
3. Editor screen → «Далее» (791,2109)
4. «Добавьте информацию» → «Загрузить» (791,2051) — рядом «Сохранить черновик» (282,2051)
5. После загрузки → канал Инакент → «Ваши видео»

### TikTok flow (из видео)
1. Лента → галерея → выбор видео → «Далее» (450,1090 video → ~825,1996 device)
2. Description screen: поле текста, хэштеги, кнопка «Опубликовать» (816,2130)
3. После нажатия → лента с индикатором прогресса 95% в левом верхнем углу
4. Samsung AddItemActivity может появиться — тап «Отмена» (фикс уже есть)

### Фикс в publisher.py
Коммит `03cfae9`:
- TikTok FALLBACK_COORDS: [(816,2130), (808,2109), (825,2145)]
- TikTok retap fallback: (816,2130)
- YouTube fallback "Загрузить": (791,2051)
- YouTube fallback "Далее/пустой UI": (791,2051)
- Заменены все (1030,120) в YouTube секции

### Статус задач на момент заморозки
- tasks #122–#129: все failed/stopped (комбо: gallery bug + неверные coords)
- pq #116–#124: 9 задач в paused (autowarm остановлен)
- pq #122 Instagram: pending
- Instagram gallery bug: пофикшен (debug screenshots → /sdcard/debug_screenshots/.nomedia)
- TikTok coords: пофикшены (03cfae9)
- YouTube coords: пофикшены (03cfae9)

### Очередь готова к запуску
Для возобновления:
```sql
UPDATE publish_queue SET status='pending' WHERE status='paused';
```
Затем: `pm2 start autowarm`

---

## Сессия 10:09–11:24 UTC — Ревью алгоритма + запуск очереди + фиксы

### Правки по ревью пользователя (коммит 12bb337)
1. **Удаление видео с устройства** после успешной публикации (до post-warm): `adb shell rm` + MediaStore notify
2. **KEYCODE_HOME после post-warm** — возврат на лаунчер перед следующей задачей
3. **Instagram галерея**: изначально попытка переключить на папку "Видео" через dropdown
4. **YouTube URL через UI** (новый метод `_get_youtube_url_via_ui()`): вкладка "Вы" → "Ваши видео" → ⋮ → "Поделиться видео" → "Копировать ссылку" → clipboard

### Запуск очереди (10:18 UTC)
- 9 задач pq#116–124 переведены pending → очередь стартовала
- autowarm запущен: `pm2 start autowarm`

### Новые падения после запуска

**Instagram #131, #132 — провалы editor loop**
- #131: `ModalActivity` с кнопкой "Закрыть" появлялась поверх ленты после "Поделиться" — wait_upload не распознавал как SUCCESS → timeout
  - Фикс `6afe69c`: в `_wait_instagram_upload` тапаем "Закрыть" в ModalActivity
- #132: Переключение на папку "Видео" через dropdown ломало flow — после тапа видео → возврат на главный экран Reels (не в редактор)
  - Фикс `9f345ea`: откат, работаем с папкой "Недавние" (debug screenshots в .nomedia → наш файл первый)

**TikTok #133 — ✅ УСПЕХ (10:52 UTC)**
- Кнопка публикации найдена по XML: pos=(798,2103) — подтверждение что (816,2130) верные coords
- URL получен: `https://www.tiktok.com/@user70415121188138/video/7584363851353719863` ✅

**TikTok #135 — опубликовано, URL не получен**
- Причина: share sheet TikTok показывает "Ссылка" (иконка цепочки), а код искал "Копировать ссылку"
- Видео-анализ instagram.mp4 кадр f0067: TikTok share sheet → "Ссылка" на позиции (274,65) в видео
- "Поделиться" кнопка на видео в ленте: (535,980) в видео = (979,1794) на устройстве
- Фикс `2dbb9de`: добавлены паттерны 'Ссылка', 'Ссылку', 'link', 'ссылк' + fallback coord (979,1794)

**YouTube #134 — ПАДЕНИЕ (stuck-counter KEYCODE_BACK)**
- Проблема: после stuck-counter срабатывал KEYCODE_BACK → вылет ИЗ YouTube на рабочий стол Samsung
- UI показывал погодный виджет, "Страница 1 из 2" — лаунчер Samsung
- Фикс `b6694dc`: перед KEYCODE_BACK проверяем topResumedActivity — если не YouTube → HOME + рестарт Shell_UploadActivity

### Статус на 11:24 UTC
- task #133 TikTok: ✅ done, URL получен
- task #134 YouTube: ❌ failed (stuck-counter), pq#118 вернулся pending
- task #135 TikTok: ✅ опубликовано, URL=профиль (не video URL — старый код)
- task #136 YouTube: pending (следующий в очереди)
- Фиксы `2dbb9de` и `b6694dc` активны для новых задач

### Ключевые открытия
- **TikTok share sheet**: кнопка называется "Ссылка" (цепочка), НЕ "Копировать ссылку"
- **TikTok "Поделиться" на видео**: (979,1794) на устройстве
- **YouTube stuck-counter**: KEYCODE_BACK без проверки пакета = выход из приложения на лаунчер
- **Instagram папка "Видео"**: переключение через dropdown ломает editor flow — не использовать

### Коммиты сессии
- `12bb337` — 4 правки по ревью (удаление файла, HOME, Instagram Видео, YouTube URL via UI)
- `6afe69c` — Instagram ModalActivity fix
- `9f345ea` — откат папки Видео в Instagram галерее
- `2dbb9de` — TikTok URL: "Ссылка" вместо "Копировать ссылку"
- `b6694dc` — YouTube stuck-counter: проверка topResumedActivity

---

## Сессия 11:24–12:52 UTC — Диагностика падений после ревью + фиксы

### Задачи в очереди при старте сессии
- pq#116–#124 (9 задач) переведены pending, autowarm запущен
- Задача #131 (Instagram) — ModalActivity "Закрыть" не закрывалась (fix: `6afe69c`)
- Задача #132 (Instagram) — галерея: переключение на вкладку "Видео" не работало
  - Вкладка "Видео" переключалась, но editor_loop уходил обратно в галерею
  - Fix: убрано принудительное переключение вкладки, оставлен поиск по content-desc (fix в editor select)

### TikTok URL-поллер: исправлен
- tasks #133, #135 (TikTok done) — URL-поллер брал СТАРОЕ видео через HTML-скрейпинг
- Fix `543215e`: заменён HTML-скрейпинг на yt-dlp с фильтром по дате (как YouTube)
- URL вручную исправлены в БД: #133 → 7616692008232553758, #135 → 7616696877513215262

### YouTube CreationModesActivity (trim screen) — КРИТИЧЕСКИЙ БАГ
- tasks #136, #137, #140: YouTue editor loop тапал (625,1410) 25 раз → fail
- Причина: координата была от AI-анализа скриншота с масштабированием
- UIAutomator dump подтвердил реальные bounds: Далее=[831,2053][1035,2154] → centre (933,2103)
- Fix `c6eede8`: детектируем CreationModesActivity → tap Далее UIAutomator/fallback (933,2103)
- Дополнительно: после fail теперь принудительно жмём KEYCODE_HOME (fix `9c60c9f`)
  - Причина: YouTube оставался в foreground → следующая TikTok-задача видела YouTube вместо своего приложения

### TikTok видео pq#120 — КРИТИЧЕСКИЙ БАГ
- tasks #138, #139: SystemShareActivity крашилась с любым video URI → топActivity = Launcher
- Диагностика: `am start SystemShareActivity` без URI → OK, с URI → crash
- Причина: видео pq#120 = 2270×3950px, 60fps, 50MB (vs успешные: 820x1380 30fps 6.6MB)
- TikTok крашится на обработке нестандартного большого файла
- Fix `250999b`: автотранскод перед adb push: если >20MB или >1920px или >30fps → ffmpeg scale до 1080×1920 CRF=23 30fps

### Instagram ModalActivity "Готово" (выбор обложки) — БАГ
- task #141: после "Поделиться" Instagram показал ModalActivity с кнопкой "Готово" (cover photo selector)
- Наш обработчик искал только "Закрыть" → игнорировал модалку 30 итераций → timeout
- Fix `985019f`: добавлен "Готово"/"Done" в список кнопок ModalActivity обработчика

### Коммиты этой сессии
- `b6694dc` — YouTube editor stuck counter
- `543215e` — TikTok url-poller yt-dlp
- `c6eede8` — YouTube CreationModesActivity: tap Далее coords (933,2103)
- `9c60c9f` — KEYCODE_HOME при любом завершении задачи (success/fail/error)
- `250999b` — transcode >20MB/>1920px + YouTube Далее (933,2103) правильные coords
- `985019f` — Instagram ModalActivity "Готово" handler

### Статус на 12:52 UTC
- pq#128 Instagram: pending (следующая в очереди)
- pq#129 TikTok: pending
- pq#130 YouTube: pending
- Все фиксы закоммичены и запушены в main

### Ключевые выводы / уроки
- Координаты от AI-анализа скриншотов — ВСЕГДА проверять через UIAutomator dump!
  - AI ошибся на 300px по X и 700px по Y (625,1410 vs 933,2103)
- Нестандартные видео (>20MB, >1920px) нужно транскодировать ПЕРЕД отправкой на устройство
- Instagram после "Поделиться" может показывать ModalActivity с разными кнопками:
  - "Закрыть" — простое уведомление после публикации
  - "Готово" — выбор обложки (cover photo selector) — нужно подтвердить
- KEYCODE_HOME после каждого завершения задачи — обязательно для чистого старта следующей

---

## Сессия 12:52–13:10 UTC — Screen Recording для отладки

### Решение
- Добавлена автозапись экрана во время публикации (коммит `c37d4c8`)
- Управление: env var `SCREEN_RECORD=false pm2 restart autowarm`
- S3 endpoint: `https://s3.ru1.storage.beget.cloud` (beget, не s3.gengo.io!)
- Путь в S3: `autowarm/screenrecords/{platform}/task{id}_{ok|fail}_{filename}.mp4`
- URL хранится в `publish_tasks.screen_record_url`

### Критерий отключения (договорились с Даниилом)
**10+ успешных задач подряд по КАЖДОЙ платформе** (Instagram, TikTok, YouTube)
После достижения → `SCREEN_RECORD=false pm2 restart autowarm`

---

## 13 марта 2026 (продолжение — 15:34 UTC)

### Исправленные баги в publisher.py (вторая половина дня)

#### Fix: adb() deadlock — subprocess.run + shell=True + timeout зависал навсегда
- Симптом: task #146 hung после "Pre-warm: видео 5/5" — даже heartbeat-поток замер
- Причина: subprocess.run(shell=True, capture_output=True, timeout=N) — при таймауте
  `/bin/sh` убивался, но дочерний `adb` держал pipe открытым → communicate() зависал
- Fix: Popen + `preexec_fn=os.setsid` + `os.killpg` на таймауте (убивает всю группу)
- Коммит: `61d07f7`

#### Fix: Instagram Cover Edit screen (экран выбора обложки Reel)
- Симптом: задача #147 — 29 повторных тапов "Поделиться" без результата
- Причина: "Изменить обложку" был в CAPTION_MARKERS — срабатывал на Cover Edit screen
  (ModalActivity с "Добавить из фотопленки" — это экран ДО экрана подписи)
- Fix: убрали "Изменить обложку" из CAPTION_MARKERS; добавили Cover Edit детектор
  (маркер "Добавить из фотопленки") → тап "Готово" (UIAutomator / fallback 667,120)
- Обработка в двух местах: editor_loop и _wait_instagram_upload
- Коммит: `c1bc9f2`

#### Fix: Instagram "Поделиться" fallback координата
- Старая coord: (799,2087) — неверная оценка
- Правильная из видео-анализа: (808,1907)
- В _wait_instagram_upload добавлен retry при caption-screen ModalActivity
- Коммит: `1adb1a8`

#### Fix: YouTube URL — awaiting_url вместо профиля
- Симптом: задачи #149 и #151 — post_url = "youtube.com/@Инакент-т2щ/shorts" (раздел, не видео)
- Причина: _get_youtube_url_via_ui тихо возвращал fallback профиль, сохранялся как done
- Fix: новый хелпер _save_youtube_url_or_await() — если URL без /shorts/VIDEO_ID → awaiting_url
  (yt-dlp поллер на сервере найдёт реальный URL)
- Вручную исправлены: #149 → 4V0ox1wUvgs, #151 → L9BpzbyWDEo
- Коммит: `a8763d3`

### Validator — исправлен вход (hunter2025)
- Проблема: admin не мог войти на validator.contenthunter.ru
- Причина: .env обновили на ADMIN_PASSWORD=hunter2025, но хэш в БД остался от старого пароля
- Fix: обновили password_hash в validator_users для login='admin' → bcrypt(hunter2025)
- Логин: admin / hunter2025

### Статус задач на конец дня
- #143 TikTok ✅ | #144 YouTube ❌ | #145-147 Instagram ❌ (фиксы применены)
- #148 TikTok ✅ | #149 YouTube ✅ (URL исправлен) | #150 TikTok awaiting_url
- #151 YouTube ✅ (URL исправлен)
- Очередь pq#120-124+ продолжает работать

### Коммиты дня (хронология)
`1adb1a8` → `00864f9` → `3131500` → `61d07f7` → `c1bc9f2` → `a8763d3`
