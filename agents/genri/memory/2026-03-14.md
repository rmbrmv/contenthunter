
## 08:37 UTC — Instagram URL поллер: цепочка фиксов

### Проблема
Instagram `awaiting_url` задачи (#161, #165, #167, #170) зависали и уходили в `failed`.

### Причины (по порядку обнаружения)
1. `edge_felix_video_timeline` в `web_profile_info` возвращал **пустой список** — Instagram изменил API
2. `/api/v1/feed/user/{user_id}/?count=6` — с сервера возвращает **401** (работает только с правильными cookies)
3. `processing → 2ч → failed` — задача #170 попала под этот таймаут (статус был `processing`, а не `awaiting_url`)

### Решение (коммит 9bc1576)
- Поллер теперь использует `edge_owner_to_timeline_media` с фильтром `GraphVideo`
- endpoint: `web_profile_info?username=inakent06` (без авторизации, без 429)
- `edge_owner_to_timeline_media` содержит shortcode + taken_at_timestamp — достаточно для дедупликации

### Вручную исправлены
- #161 → DV1UD5YCAOE (17:29 UTC)
- #165 → DV1aJgpCCM0 (18:22 UTC)
- #167 → DV1cGOCDkQG (18:39 UTC)
- #170 → DV1gpo4DkSj (19:19 UTC)

### Таблица: Публикации 13.03.2026
| ID | Platform | Status | URL |
|----|----------|--------|-----|
| 161 | Instagram | done | DV1UD5YCAOE |
| 165 | Instagram | done | DV1aJgpCCM0 |
| 167 | Instagram | done | DV1cGOCDkQG |
| 170 | Instagram | done | DV1gpo4DkSj |
| 163,164,169,171 | TikTok | done | ✅ |
| 162,166,168,172 | YouTube | done | ✅ |

## 08:14 UTC — Запуск мониторинга 14.03.2026

Пользователь зарядил 10 задач на каждую платформу (publish_queue #146-160).
Задача: мониторить каждые 5 минут, разбираться с ошибками, отписывать в чат.
- Текущая: #173 Instagram (running)
- Очередь: pq #147-160 pending (TikTok, YouTube, Instagram x10 на платформу)

## 21:01 UTC — Рефлексия дня

### Сделано хорошо
- Instagram URL поллер: нашёл рабочий endpoint (edge_owner_to_timeline_media) после цепочки 429→401→пустой список
- Publisher: прямой API-поллинг после публикации (15-30с вместо 10мин)
- needs_verification статус: отдельный от failed, жёлтый бейдж, запись в лог
- syncQueueStatuses: устранил рассинхрон publish_queue ↔ publish_tasks
- Диагностика YouTube "7 видео не загружено" через скриншот — нашёл реальную причину (лимит телефона)

### Ошибки дня
- os.path.exists() для S3 URL — элементарная ошибка, задачи #185-187 упали трижды
- adb_output vs adb() — опечатка в методе, clipboard не читался весь день
- 5 итераций фикса бейджа (4 разных объекта статусов в HTML) — нужна была диагностика сначала
- YouTube задачи не детектировали лимит телефона → 7 зависших задач

### Паттерн дня: "не тот рендерер"
Один и тот же визуальный элемент может рендериться 4 разными кодовыми путями в большом HTML. Всегда трассировать: экран → id → функция → объект.
