## Snapshot 15:01–15:40 UTC — "Живая справка" реализована

### Задача
На каждом экране delivery.contenthunter.ru добавить контекстную справку.

### Реализация (index.html)

**1. CDN:** `marked.min.js` добавлен в `<head>` (рядом с chart.js)

**2. CSS (`#help-panel`, `#help-trigger`, `#help-backdrop`):**
- Панель: `position: fixed`, `width: 440px`, `max-width: 50vw`, `min-width: 320px`
- Анимация: `transform: translateX(100%)` → `.open { translateX(0) }`, `transition: 0.28s`
- `.wide` класс = `width: 50vw` (по кнопке ⇔)
- Backdrop: `rgba(0,0,0,0.18)`, клик закрывает
- Кнопка ?: `position: fixed; bottom: 24px; right: 24px`, круглая, indigo

**3. HTML:**
- `#help-backdrop` + `#help-panel` (с `#help-resize-handle`)
- Шапка: title + кнопки ⇔ и ✕
- `#help-content-body` — скроллируемый контент с prose-стилями

**4. JS объект `HELP_CONTENT`** (разделы → markdown):
- dashboard, publishing, publishing-queue, uniqualization
- unic-results, unic-sources, unic-settings, unic-content
- devices, tasks, protocols, sla, analytics
- factory, publish-queue, settings, admin, tokens, archive, packages, validator-plan

**5. Функции:**
- `helpOpen(section?)` — открыть, рендерит marked.parse(md)
- `helpClose()` — закрыть
- `helpToggleWidth()` — toggle .wide класс
- `helpSetSection(section)` — обновляет `_helpCurrentSection`; если панель открыта — обновляет контент

**6. Drag-to-resize:**
```js
handle.addEventListener('mousedown', ...) → mousemove → mouseup
// Диапазон: 320px–75vw
```

**7. nav() подключён:**
```js
function nav(section) {
  helpSetSection(section); // ← добавлено первой строкой
  ...
}
```

### Результат
✅ Деплой: `pm2 restart autowarm`
✅ Скриншот отправлен в Telegram
✅ Панель открывается, таблицы и заголовки рендерятся корректно

### Правило сопровождения
🚨 При добавлении новой фичи → обновить `HELP_CONTENT['section']` в index.html

---

## Snapshot 15:40–16:18 UTC — Созвон Даниил + Наталья (тестирование платформы)

### Контекст
Даниил показал Наталье (менеджер) новую платформу Валидатор + Delivery.

### Договорённости
1. Наталья самостоятельно проходит флоу: загрузка → планировщик → уникализация
2. Тестировать на тестовом проекте
3. Когда Instagram-выкладка заработает → Даниил пишет → Наталья тестирует (в т.ч. выходные)
4. Наталья собирает баги и UX-пожелания
5. Справка на каждый экран Delivery — **уже сделана** (выполнено до созвона)

### Замечания от Натальи
- Лимит веса файла не отображается в валидаторе — нужно вывести явно
- Стрелки сортировки в таблице результатов не видны — сделать заметнее
- Уточнить: какие параметры критичны (блокируют), а какие — просто предупреждения

### Доступы
- Наталья: аккаунт admin в Delivery создан Даниилом во время созвона
- Валидатор: доступ от Ромы, нужно уточнить

### Ключевые наблюдения о платформе (из демо)
- Клиент сам загружает видео + описание + хэштеги → снимает нагрузку с менеджеров
- Уникализация медленнее старой — оптимизация в планах
- TikTok и YouTube выкладка — ещё в доработке
- Синхронизация с factory DB раз в час
- Интервал между публикациями одного аккаунта: 30 минут

---

## Snapshot 16:18–16:29 UTC — Фиксы по итогам созвона с Натальей

### Реализовано

**П.1+П.3 — Валидатор: явные требования и разделение блокеров/предупреждений**

1. `DropZone.vue` — добавлена сетка требований ДО выбора файла:
   - ❌ критично (блокируют загрузку): формат MP4/MOV, размер ≤200 МБ, длительность 3–180 сек, вертикальное 9:16, аудио обязательно
   - ⚠️ рекомендации: 20–45 сек, 1080p, динамика

2. `ValidationDetails.vue` — добавлены два новых блока:
   - `techBlockers` → красная карточка "❌ Критические ошибки"
   - `techWarnings` → жёлтая карточка "⚠️ Технические рекомендации"
   - computed: `techBlockers = content.tech_blockers ?? []`, `techWarnings = content.tech_warnings ?? []`

3. `UploadPage.vue` — обработка 422 ошибки с blockers:
   - если `detail.blockers` → result='rejected', показываем ValidationDetails с tech_blockers
   - иначе → обычный error.value = текст

**П.2 — Стрелки сортировки в Delivery (unic-results)**
- Все заголовки: добавлен `⇅` с `class="text-gray-300"` по умолчанию
- `urSort()` обновлён: все колонки сбрасываются в серый ⇅; активная — `text-indigo-500 font-bold` + ▲/▼

**Деплой:** `npm run build` (postbuild → /var/www/validator/), `pm2 restart autowarm`

### Ожидает ответа пользователя
**П.4 — Сводный вид планировщика** (менеджер/admin):
- Вопрос: отдельный раздел в сайдбаре ИЛИ переключатель на экране планировщика?
- Логика: менеджер видит все свои проекты, admin — все активные проекты
- В слотах указывать проект

---

## Snapshot 16:29–16:47 UTC — П.4 Сводный планировщик (все проекты)

### Реализация

**Бэкенд: `GET /api/schedule/all`** (новый endpoint в schedule.py)
- manager → слоты по `current_user.project_ids`
- admin/producer → все активные проекты из `validator_projects WHERE active=true`
- client → 403
- Возвращает `project_name` в каждом слоте
- Сортировка: slot_date, project_id, slot_position

**Фронт: ClientDashboard.vue**
- Новые состояния: `viewMode: 'single'|'all'`, `isAdmin`, `setViewMode()`
- Новая функция: `loadSlotsAll()` → `GET /schedule/all` → фильтрует только `status='filled'`
- `prevWeek`/`nextWeek` → теперь вызывают `loadSlotsAll()` или `loadSlots()` по режиму
- Переключатель `[📁 Проект] [🗂 Все проекты]` в строке выбора проекта
- В режиме 'all': бейдж `📁 project_name` на каждой заполненной карточке
- В режиме 'all': скрыты пустые слоты, кнопки "Загрузить", "+ Слот"

**Деплой:** `npm run build` + `pm2 restart validator-api`

### Итого за день (все 4 пункта из созвона с Натальей):
1. ✅ Явные требования к файлу в DropZone + разделение ❌/⚠️ в ValidationDetails
2. ✅ Стрелки сортировки в unic-results — серый ⇅ по умолчанию, активная = indigo ▲/▼
3. ✅ Критичные блокеры vs предупреждения — явно показываются после загрузки
4. ✅ Сводный планировщик для менеджеров/админов (все проекты, с бейджем проекта)

---

## Snapshot 16:47–17:22 UTC — Убраны "Загрузка" и "Календарь", требования в модалке

### Задача
- Убрать разделы "Загрузить" и "Календарь" из сайдбара валидатора
- Добавить требования к файлу в модальное окно загрузки в планировщике

### Реализация

**AppSidebar.vue:**
- Удалены две строки: `NavItem to="/upload"` и `NavItem to="/calendar"` (десктоп)
- Удалены два mob-tab блока для /upload и /calendar (мобильный)

**UploadModal.vue — блок требований внутри дропзоны:**
- Добавлена сетка 2×3 с требованиями (только когда файл ещё не выбран):
  - ❌ Формат: только MP4 или MOV
  - ❌ Размер: не более 200 МБ
  - ❌ Длительность: 3–180 сек
  - ❌ Ориентация: вертикальное 9:16
  - ❌ Аудио: обязательно
  - ⚠️ Рекомендуется: 20–45 сек, 1080p, динамика
- Легенда: "❌ — критично, блокирует загрузку · ⚠️ — рекомендация"

**Деплой:** `npm run build` (postbuild → /var/www/validator/)
