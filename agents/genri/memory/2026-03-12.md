# 2026-03-12 — Дневной лог Генри

## Сессия с Даниилом Павловым (242574724) — ночная сессия (00:00–04:12 UTC)

---

### Instagram — ИСПРАВЛЕНИЕ ПОЛНОГО ФЛОУ (главный прорыв!)

**Корневые причины всех предыдущих падений Instagram:**

1. **Gallery screen с "Добавление к видео Reels"** содержит слово 'Reels' → код входил в editor loop прямо с экрана галереи (ложный триггер)
2. **Fallback tap `(181,1000)`** попадал на кнопку "Открыть камеру" (bounds `[5,408][358,1035]`) — не на видео!
3. **Gallery не auto-navigates** — после tap видео надо нажать отдельную кнопку "Далее" в заголовке галереи
4. **`dump_ui()` возвращал "UI hierchary dumped..." prefix** → `ET.fromstring()` падал (починено коммитом `230ab97`, но до 12.03 не тестировалось)

**Фиксы (коммиты в delivery-contenthunter):**
- `6332a20` — правильный fallback tap `(540,721)` (первый видео-слот, не камера) + GALLERY_MARKERS для детекции
- `b8abcfa` — после выбора видео ищем и тапаем "Далее" в заголовке галереи (без этого нет перехода в редактор)
- Задачи #52, #53 — failed (pm2 restart во время работы)
- **Task #54 — ✅ done** — первая успешная Instagram публикация!
- **Task #56 — awaiting_url** — вторая успешная Instagram публикация (04:01 UTC)

---

### Фиксы check constraint publish_tasks_status_check

- `awaiting_url` **не был** в check constraint → `_save_post_url` падал с ошибкой → URL не сохранялся
- ALTER TABLE: добавлен `awaiting_url` в список разрешённых статусов
- Теперь task #56 корректно имеет status `awaiting_url` с profile URL

---

### Pre-warm таймаут (3 минуты)

- Task #55 завис на pre-warm 8+ минут (ADB команда `am start` зависла внутри `run_mini_warm`)
- **Фикс `25be19c`**: добавлен `MAX_WARM_SECS=180` в `run_mini_warm` — прерывает по таймеру, не зависает
- Task #55 сброшен вручную → failed, pq=80 сброшен в pending
- Task #56 (повтор) — pre-warm прошёл за 1.5 мин ✅

---

### Instagram URL capture — незакрытая проблема

- `_auto_get_instagram_url` находит Reels таб ✅ но не находит grid cells
- Причина: `self.adb('shell input swipe ...')` — неправильный двойной `shell` префикс (баг в коде)
- UIAutomator grid cell search может не находить элементы до полной загрузки
- **Server-side**: `scrapePostUrl('Instagram', ...)` использует `?__a=1&__d=dis` — Instagram заблокировал этот API
- **Статус**: `awaiting_url`, сервер-side poller будет retry, но Instagram API не работает → нужна доработка

---

### TikTok URL capture — улучшения

- Коммит `f0656f9`: после публикации ищем Share/Поделиться через UIAutomator (не хардкод coords)
- Ищем "Копировать ссылку" в открытом sheet
- Task #57 — TikTok запущена, мониторинг в процессе

---

### Очередь publish_queue — состояние на 04:12 UTC

| ID | Platform | Account | Status | Task |
|----|----------|---------|--------|------|
| 80 | instagram | inakent06 | running | #56 awaiting_url |
| 69 | tiktok | user70415121188138 | running | #57 running |
| 70-79 | youtube/ig/tt | Тестовый проект_19 | pending | — |

---

### Ключевые коммиты (12.03.2026)

| Hash | Описание |
|------|----------|
| `6332a20` | Instagram gallery: GALLERY_MARKERS + correct fallback tap (540,721) |
| `b8abcfa` | Instagram gallery: tap "Далее" button after video selection |
| `f0656f9` | URL capture: awaiting_url constraint + Instagram UIAutomator Reels nav + TikTok Share via UIAutomator |
| `25be19c` | run_mini_warm: 3-min hard timeout |

---

### Решения (Key Decisions)

- **Галерея Instagram не auto-navigates**: нужен тап "Далее" после выбора видео (было неизвестно)
- **Gallery markers** (gallery_grid_item_thumbnail, gallery_recycler_view, Недавние): при наличии — это НЕ редактор
- **Pre-warm timeout**: 180s hard limit, адаптивное время просмотра
- **`awaiting_url`** теперь в check constraint — можно использовать для всех платформ
- **Instagram server-side scrape**: `?__a=1` не работает, нужен альтернативный подход
- **`shell` prefix bug в `adb()`**: `self.adb('shell cmd')` = `adb shell "shell cmd"` — НЕПРАВИЛЬНО

---

### Незакрытые задачи

- [ ] Instagram URL capture: убрать `shell` prefix из swipe команды, добавить больше wait после Reels tab
- [ ] Instagram server-side URL: заменить `?__a=1` на workable подход (yt-dlp? playwright? mobile API?)
- [ ] TikTok task #57: мониторинг результата
- [ ] YouTube pq=70,73,76,79: ещё не запускались
- [ ] Много pending pq (69-82): все для "Тестовый проект_19" — почему так много?

---

## Продолжение сессии — утренняя часть (04:12–05:02 UTC)

### YouTube — первая реальная публикация + диагностика трёх багов

**Корневые причины провала YouTube task #47 (вчера):**
1. Кнопка "Создать" не найдена → fallback `(1150,1012)` за экраном (1080px шириной)
2. Парсинг XML галереи падал из-за мусора в начале UIAutomator dump (уже пофикшено в `230ab97`)
3. Подтверждение загрузки не получено, но задача ставилась в done — false positive

**Баг `ensure_unlocked()`:**
- `dumpsys window | grep screenState` — ОЧЕНЬ медленная команда, вешала все задачи
- Task #58 (YouTube) завис на 20+ минут именно на этом шаге (0 events в DB!)
- **Фикс**: заменил на `dumpsys power | grep -m1 mWakefulness` с timeout=8s
- Коммит: `3b7b81a`

**Баг TikTok chooser:**
- На Samsung, в системном chooser все иконки приложений имеют `clickable=false`!
- Код искал TikTok как clickable element → никогда не находил
- **Фикс**: сканировать ВСЕ узлы (не только clickable), найти text="TikTok", вычислить центр bounds, тапнуть координаты
- Также: `Только сейчас` button нужно тапать после выбора иконки
- Полный XML chooser: apps отрендерены как non-clickable nodes с text1/text2, clickable только "Только сейчас" / "Всегда"
- Коммит: `bef483f`

**YouTube — первая успешная публикация:**
- Task #61 (YouTube, @Инакент-т2щ) = `awaiting_url` ✅ (04:59 UTC)
- Логи: "Создание видео нажато" → "Галерея открыта" → "Fallback тап (181,600)" → "Ждём загрузки" → awaiting_url
- Fallback для выбора видео: тап по `(181,600)` когда UIAutomator не нашёл видео по имени

### Текущий статус задач (05:00 UTC)
- Task #56 Instagram: `awaiting_url` (post_url = профиль, не реальный URL)
- Task #61 YouTube: `awaiting_url` → yt-dlp poller ищет реальный URL
- TikTok: chooser fix задеплоен в `bef483f`, следующая задача покажет результат

### Ключевые паттерны (добавить в approved-patterns.md)
- `dumpsys power | grep -m1 mWakefulness` = быстрая проверка экрана (vs медленный `dumpsys window`)
- Samsung chooser: иконки `clickable=false`, tap по координатам bounds текста
- После выбора иконки в chooser → искать "Только сейчас" и тапать

---

## Сессия 2 (08:00–09:00 UTC)

### Исправления деплоя

#### YouTube: Shell_UploadActivity (КРИТИЧНО)
- **Старый код**: запускал YouTube через лаунчер → тапал "Создание видео" → открывал галерею → fallback тап (181,600)
- **Новый код**: `am start -n com.google.android.youtube/.../Shell_UploadActivity -a android.intent.action.SEND -t video/mp4 --eu android.intent.extra.STREAM <content_uri>`
- Полностью обходит галерею, открывает редактор Shorts напрямую
- Task #75 завершился с `failed` (запущен ещё со старым кодом)

#### TikTok: дедупликация URL
- **Проблема**: scrapePostUrl брал `videoIds[0]` без проверки → tasks #69, #71, #74 получили одинаковый URL
- **Фикс**: перебираем первые 5 видео, для каждого делаем `SELECT FROM publish_tasks WHERE post_url = $1 AND updated_at > NOW() - INTERVAL '24 hours'`
- Если URL уже занят — пропускаем, берём следующий

#### Instagram: новый API endpoint
- **Старый**: `?__a=1&__d=dis` → возвращает HTML (Meta закрыла в 2024)
- **Новый**: `api/v1/users/web_profile_info/?username=<acct>` с заголовками `X-IG-App-ID: 936619743392459` + mobile User-Agent
- + дедупликация + проверка даты (только сегодня/вчера)

#### Геолокационный диалог
- `dismiss_location_dialog()` — 11 мест во всех циклах TikTok/Instagram/YouTube
- Ключевые слова: "местоположени", "Точно", "Приблизительно" → нажимает "Запретить"

### Коммиты
- `6eb63fb` — dismiss location dialog (11 мест)
- `ed24cb4` — PUBLISH-NOTES.md документация
- `205b350` — YouTube Shell_UploadActivity + TikTok dedup + Instagram web_profile_info

### Архитектура очереди (пояснение)
- Одно устройство (`RF8YA0W57EP`) = одна задача одновременно
- Scheduler блокирует `device_serial` на время выполнения
- `max_tasks_per_pi = 2` — только если к малинке подключено несколько устройств

### Состояние задач на конец сессии
- task #75: YouTube — failed (старый код)
- task #76: Instagram inakent06 — pending (следующая в очереди)
- PM2 autowarm перезапущен с новым кодом

---

## Сессия 11:00–11:14 UTC (продолжение)

### Исправления publisher.py (публикация)

#### YouTube direct upload — clip trim экран
- **Проблема**: после `Shell_UploadActivity` YouTube показывает экран обрезки видео (timeline + "Далее") перед редактором
- **Причина**: в editor loop `tap_element(['ОК','Allow'...])` проверялся ДО `tap_element(['Далее'...])` и тапал что-то в интерфейсе без логирования — 20 шагов × 5с = 2:14 тишины
- **Фикс** (коммит `df41002`): порядок изменён — сначала `Загрузить`, потом `Далее`, потом `ОК/Allow` (все с `clickable_only=True`); лимит шагов 25; добавлено логирование каждого тапа

#### TikTok upload confirmation — false positive
- **Проблема**: `UPLOAD_OK` содержал `'Видео'` → матчился на вкладке "Видео/Фото" в редакторе TikTok сразу на шаге 0 (3 сек после нажатия "Поделиться") — ложная публикация
- **Фикс** (коммит `3d5778d`): убраны общие слова (`'Видео'`, `'Нравится'`, `'Ещё'`, `'Загрузка'`, `'Публикация'`); добавлен `STILL_ON_EDITOR` список для детекции "всё ещё в редакторе"; повторный тап "Поделиться" если застряли >5 шагов
- **task #84** TikTok: отмечен `failed`, `pq=66` → retry

#### adb push timeout
- **Проблема**: файл `pq_65` весил 60MB, timeout 60с не хватало
- **Фикс** (коммит `3d9f72b`): динамический timeout = `max(120, 120 + int(size_mb / 10) * 60)`, макс 600с

### UI: колонка "Видео" в Результатах (коммит `bda6882`)
- API `/api/publish/tasks` добавлен JOIN: `unic_results` → `unic_tasks.input_video_name` → поле `video_name`
- Также приходит `s3_url` = `pq.media_url`
- Колонка после "Аккаунт": название видео (кликабельная ссылка на S3) + иконка 🎬
- Если `video_name` пустой — берёт имя файла из S3 URL
- Фильтр по колонке "видео" работает

### Git коммиты за сессию
- `df41002` — YouTube direct upload clip trim fix
- `3d5778d` — TikTok false positive confirmation fix
- `3d9f72b` — adb push dynamic timeout
- `bda6882` — UI: Video column in Results

### Состояние очереди на 11:14 UTC
- task #84 TikTok: failed (ложная публикация), pq=66 retry
- task #78 TikTok: awaiting_url 98+ мин (возможно дубль старого видео)
- pq=48,49,66,81,82,84,85 — pending, ждут dispatch
- Instagram URL-поллер: 429 rate limit от Meta

---

## Сессия 12:00–12:57 UTC — Фиксы, delivery упал, unic-worker разобрали

### Delivery упал (~12:18 UTC)
- `autowarm` исчез из PM2 — причина: запуск `pm2 start ecosystem.config.js` из папки unic-worker + `pm2 save` перезаписал dump без autowarm
- Восстановлен: `pm2 start server.js --name autowarm && pm2 save`
- Важно: **никогда не делать `pm2 save` из чужой папки с ecosystem.config.js** — перезаписывает глобальный dump

### TikTok публикация — фикс кнопки "Поделиться"
- Корневая причина: `tap_element(['Поделиться'...])` находил кнопку "Поделиться видео" (шеринг в другие соцсети), а не кнопку публикации
- "Поделиться в TikTok" bottomsheet (Android overlay) — НЕ признак успешной публикации, а наоборот — TikTok закрылся БЕЗ публикации
- **Фикс (коммит `8d034d1`)**:
  - Перед поиском кнопки — `KEYCODE_BACK` (скрыть клавиатуру)
  - Строгий поиск: `text == "Опубликовать" / "Post"` или `text == "Поделиться"` только если `y < 300`
  - Fallback: координата `(980, 65)` — топ-правый угол
- Откат ложного детектора Android share sheet (коммит `9eab811` → отменён в `8d034d1`)

### unic-worker — разобрались с архитектурой
- **unic-worker на 91.98.180.103 уже 10 дней работает** и подключается к нашей БД по `72.56.107.157:5432`
- Сегодня я случайно запустил ДУБЛИРУЮЩИЙ локальный воркер → конкуренция → задачи падали
- Задачи #17, #18, #19 (Тест 6/8/9) — всё время падали из-за этого
- **Исправлено**: удалён unic-worker из локального PM2, обновлён worker.py на удалённом сервере

### unic-worker на 91.98.180.103 — конфигурация
- DB: `postgresql://openclaw:openclaw123@72.56.107.157:5432/openclaw`
- S3: те же ключи (1cabe906ea6e-gengo, save.gengo.io)
- MAX_WORKERS=4, POLL_INTERVAL=3
- PM2 процесс id=0, ecosystem в `/root/unic-worker/ecosystem.config.js`
- Логи: `~/.pm2/logs/unic-worker-error.log`
- SSH: `sshpass -p 'MNcwMPCiyiYtM5' ssh root@91.98.180.103`

### PostgreSQL — Docker конфигурация
- Слушает на `0.0.0.0:5432` через docker-proxy
- pg_hba.conf в Docker: `host all all all scram-sha-256` — внешние коннекты разрешены
- Наш внешний IP: `72.56.107.157`

### Состояние задач на 12:57 UTC
- unic_tasks #17, #18, #19 — ✅ all done
- publish_tasks #90 YouTube — awaiting_url (yt-dlp поллит)
- Локальный PM2: autowarm (pid 41611), ch-auth (pid 40628), producer (pid 44590)
- Удалённый PM2 (91.98.180.103): unic-worker (pid 3979619) — online

### Коммиты за сессию
- `8d034d1` — TikTok post button strict search + revert Android sheet false success

---

## Продолжение — вторая половина дня (15:00 UTC)

### Проблемы и фиксы TikTok публикации

#### Root cause "мягкий успех"
- `KEYCODE_BACK` отправлялся БЕЗ проверки клавиатуры → уходили обратно в редактор вместо dismiss keyboard
- `ensure_unlocked()` в confirmation loop → swipe закрывал TikTok диалоги
- "Мягкий успех" после 3 retap → `awaiting_url` хотя видео не опубликовано
- **Фикс**: `_is_keyboard_shown()` перед KEYCODE_BACK, убрал `ensure_unlocked()`, честный `failed` вместо мягкого успеха
- **Коммит**: `0361105`

#### Root cause "Оценить Instagram" overlay
- После pre-warm в Instagram оставался диалог "Оценить Instagram" поверх экрана
- При запуске TikTok — диалог перекрывал кнопку публикации
- Нажимали в пустоту → TikTok закрывался → Android share sheet → confirmation loop timeout
- **Фикс**: `dismiss_overlay_dialogs()` — вызывается перед запуском TikTok и каждые 5 итераций в confirm loop
- Keywords триггер: `Оценить`, `Rate`, `Review`, `Do you enjoy`, `Update available`
- **Коммит**: `5f4180f`

### Heartbeat + Watchdog
- `set_step(text)` — обновляет `updated_at` + текущий шаг в `log` каждые 30 сек (фоновый поток)
- `stop_heartbeat()` — вызывается в конце `run()` и в finally
- Watchdog в server.js: если `updated_at` не обновлялся > **3 минут** → task=failed, pq=pending
- **Коммит**: `a9e6548` (и предыдущие в той же серии)

### PM2 systemd фикс
- **Root cause падений PM2**: OpenClaw Gateway (systemd `openclaw-gateway.service`) потребляет 7.5GB RAM, краш → убивает всю cgroup включая PM2
- Падения: 12:02, 12:52, 14:10 UTC — все совпадают с `[gateway] drain timeout reached`
- **Фикс 1**: `pm2-root.service` (systemd) поднят и запущен — PM2 теперь в отдельной cgroup `/system.slice/pm2-root.service`
- **Фикс 2**: `systemctl edit openclaw-gateway` → `MemoryMax=3G` — Gateway перезапускается сам до OOM
- **Команда восстановления**: `pm2 resurrect` (при ручном падении)

### Статус задач на 15:00 UTC
- task #90, #93: YouTube ✅ done
- task #91: Instagram ❌ failed (zombie watchdog)
- task #92, #95: TikTok ❌ failed (мягкий успех баг)
- task #94, #96: TikTok ❌ failed (watchdog / PM2 упал)
- task #97, #98: TikTok ❌ failed (overlay диалог "Оценить Instagram")
- pq #84: pending (перезапущен с новым фиксом)

### Коммиты за вторую половину
- `0361105` — TikTok: KEYCODE_BACK только при keyboard, убран мягкий успех, log_event
- `5f4180f` — fix: dismiss Rate/Review overlay dialogs before TikTok launch and in confirm loop

---

## Сессия 16:00–17:29 UTC — ручное наблюдение + критические фиксы

### Методология: ADB observer (pm2 observer)
- Создан `/tmp/obs2.py` — PM2 процесс, логирует каждую секунду:
  - Смена activity → скриншот
  - Изменения UI текстов (➕/➖)
- Запуск: `pm2 start /tmp/obs2.py --name observer --interpreter python3`
- Тачскрин: `/dev/input/event2` (sec_touchscreen, ABS_MT_POSITION_X max=1079)
- Важно: exec-сессии убиваются Gateway при рестарте → только PM2!

### Ручное наблюдение YouTube (17:07–17:14 UTC)
Реальный флоу публикации YouTube Short из галереи:
```
17:07:47  CreationModesActivity     ← YouTube открылся (Create экран)
17:08:58  CreationModesActivity     ← галерея / выбор альбома "Видео"
17:09:55    Редактор: Текст/Обрезка/Фильтры/Стикеры/"Выйти из редактора"
17:10:03    "Добавьте информацию"   ← промежуточный экран (скрипт не знал!)
17:10:10  UploadActivity            ← "Добавьте название" + "Загрузить"
17:10:36  InternalMainActivity      ← после Загрузить → "Ваши видео"
17:11:13    "Готово" ← видео опубликовано ✅
```

**Ключевые выводы для скрипта:**
- `"Выйти из редактора"` → нужно нажать "Далее" / тап (1030,120)
- `"Добавьте информацию"` → промежуточный экран, скрипт раньше его не обрабатывал
- После "Загрузить" → `InternalMainActivity` + `"Ваши видео"` = подтверждение загрузки
- `"Обработано 100%"` → `"Готово"` — уже после return True, не нужно ждать

**Фиксы (коммиты):**
- `07742f8` — YouTube флоу: Выйти из редактора, Добавьте информацию, подтверждение "Ваши видео"
- `a6452e2` — принудительный портрет в run(): accelerometer_rotation=0, user_rotation=0 для всех платформ

### Ручное наблюдение TikTok (17:19–17:24 UTC)
Реальный флоу публикации TikTok:
```
17:20:13  SAAActivity               ← TikTok открылся
17:20:37  SAAActivity               ← редактор: '3 мин', '60 с'
17:21:22    UI: "Далее" + "Настройки"   ← нажали Далее → к описанию
17:21:26    UI: "Добавьте описание", "Предпросмотр", "Изменить обложку"
17:21:36    description заполнено
17:21:50  AddItemActivity ⚠️         ← Samsung "Добавить виджет TikTok Камера на ГЭ"
            'Камера TikTok 1x1', 'Отмена'
17:21:58  SplashActivity            ← диалог закрыт (Отмена)
17:22:11    профиль: Инакент, @user70415121188138
17:22:18  DetailActivity ✅          ← видео опубликовано! "· 20 с. назад, 0 лайков"
17:22:42    Нижняя шторка: Ссылка   ← копирование ссылки
```

**КОРЕНЬ ВСЕХ ПАДЕНИЙ TIKTOK:**
`com.sec.android.app.launcher/.AddItemActivity` — Samsung диалог "Добавить виджет TikTok Камера на главный экран?" появляется **ПОСЛЕ** нажатия кнопки публикации. Наш overlay-детектор видел `launcher` → streak=3 → fail. А видео РЕАЛЬНО публиковалось!

**Фиксы (коммит `6c48165`):**
- `dismiss_overlay_dialogs()` — добавлено правило для `AddItemActivity`:
  триггеры: `['Добавить на главный экран', 'Камера TikTok', 'Add to Home screen']`
  кнопка: `['Отмена', 'Cancel', 'Нет']`
- Overlay-детектор: `DetailActivity` → сразу `upload_confirmed = True` (без streak)
- Overlay-детектор: профиль пользователя ('Фото профиля', 'Создать историю') → тоже успех
- UPLOAD_OK: добавлены 'Фото профиля', 'Создать историю', 'Оригинальный звук от'
- Принудительный портрет: `accelerometer_rotation=0`, `user_rotation=0` в самом начале `run()`

### Горизонтальная ориентация — важное замечание
- При ручной публикации телефон был горизонтально часть времени
- Fallback-координаты рассчитаны под портрет 1080×2340
- Фикс: в начале `run()` принудительно ставим portrait-режим

### Статус задач на 17:29 UTC
- task #107 YouTube: failed (watchdog убил за 8 мин без heartbeat — ensure_unlocked() bug был ещё из старого кода)
- TikTok задачи #100, #101: failed из-за AddItemActivity (теперь пофикшено)
- pq #84 TikTok, #107 Instagram, #108 TikTok, #109-115: pending, готовы к запуску
- Observer PM2 удалён (сделал своё дело)

### Коммиты 16:00–17:29
| Hash | Описание |
|------|----------|
| `07742f8` | YouTube: Выйти из редактора, Добавьте информацию, подтверждение Ваши видео |
| `a6452e2` | Принудительный портрет для всех платформ в run() |
| `6c48165` | TikTok: AddItemActivity overlay, DetailActivity success, профиль как успех |

---

## Сессия 17:00–18:20 UTC — Ручные наблюдения, фиксы всех платформ

### Ручное наблюдение YouTube (17:07–17:14 UTC)
Реальный флоу (ранее неизвестный скрипту):
- CreationModesActivity → галерея → редактор ('Выйти из редактора') → 'Добавьте информацию' → UploadActivity ('Загрузить')
- После Загрузить → InternalMainActivity + 'Ваши видео' + 'Обработано 100%' → 'Готово'
- **Фиксы**: добавлены шаги 'Выйти из редактора', 'Добавьте информацию'; подтверждение через MainTabActivity + 'Ваши видео'
- Коммит: `07742f8`

### Ручное наблюдение TikTok (17:19–17:24 UTC)
Реальный флоу:
- SAAActivity → редактор (Музыка/3мин/60с) → 'Далее' → description screen
- Description: 'Добавьте описание...', 'Предпросмотр', 'Изменить обложку', 'Хэштеги', 'Упомянуть'
- КНОПКА ПУБЛИКАЦИИ НЕ ВИДНА в UIAutomator (custom view TikTok)
- После публикации: **Samsung AddItemActivity** ("Добавить виджет TikTok Камера на главный экран?") — ГЛАВНАЯ ПРИЧИНА всех провалов!
- Dismissal: нажать 'Отмена' → TikTok возвращается в ленту/профиль
- Success: SplashActivity + 'Фото профиля'/'Подписки' / DetailActivity + '0 лайков'
- **Фиксы**: AddItemActivity → 'Отмена'; DetailActivity как success; 'Фото профиля' как UPLOAD_OK
- Коммит: `6c48165`

### Ручное наблюдение Instagram (17:30–17:35 UTC)
Реальный флоу:
- MainTabActivity (лента) → ModalActivity (камера) → caption screen (внутри ModalActivity!)
- Caption screen маркеры: 'Нажмите дважды, чтобы изменить фото обложки', 'Изменить обложку', 'Аудитория', 'Связать видео Reels'
- После 'Поделиться' → **СРАЗУ** MainTabActivity (лента) — никакого "Reel опубликован" нет!
- **Фиксы**: 6 новых маркеров caption screen; MainTabActivity как PRIMARY success indicator
- Коммит: `cd026c2`

### Баги найденные при запуске очереди
- **_is_keyboard_shown() без аргумента**: `_fill_instagram_caption_and_publish` вызывала без `ui` → TypeError → task #109 failed
  - Фикс: `c5f2111`
- **TikTok task #110**: FALLBACK (980,65) не сработал — попадал в статус-бар
  - Причина: y=65 = системный статус-бар Samsung, кнопка на y≈110
  - Фикс: множественные fallback coords (980,110)+(980,65)+(980,150); скриншоты при fallback
  - Коммит: `f5f41c7`
- **Принудительный портрет**: добавлен в `run()` для всех платформ (accelerometer_rotation=0, user_rotation=0)
  - Коммит: `a6452e2`

### TODO завтра (13.03.2026)
- [ ] Ручной прогон TikTok с новым observer — поймать точные координаты кнопки публикации
- [ ] Скриншоты при fallback будут сохранены в /sdcard — проверить после след. авто-прогона
- [ ] pq #117-124 (TikTok + YouTube + Instagram) — запустить очередь
- [ ] pq #116 (Instagram) — retry с фиксом _is_keyboard_shown
- Observer script сохранён: /tmp/obs_tiktok.py (детальный + скриншоты каждые 6 сек)
