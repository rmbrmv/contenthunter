## 2026-03-22 — Авторизация validator (admin)

### Проблема
Даниил Павлов (@Danil_Pavlov_123) сообщил что постоянно вылетает из системы и скидывается пароль на client.contenthunter.ru.

### Что сделано

**1. Пароль admin:** `hunter2025` (из `/root/.openclaw/workspace-genri/validator/backend/.env`)

**2. Привязка Telegram к admin-аккаунту:**
```sql
UPDATE validator_users SET telegram_id = '242574724', telegram_username = 'Danil_Pavlov_123' WHERE login = 'admin';
```
Теперь Даниил может заходить через кнопку Telegram на странице логина.

**3. JWT TTL увеличен с 24ч до 7 дней (168ч):**
- Файл: `validator/backend/src/config.py`
- Было: `jwt_expire_hours: int = 24`
- Стало: `jwt_expire_hours: int = 168`

**4. Бэкенд validator запущен через PM2:**
- Процесс не был запущен вообще (не числился в PM2)
- Запущен: `pm2 start "uvicorn src.main:app --host 0.0.0.0 --port 8000" --name validator --cwd /root/.openclaw/workspace-genri/validator/backend`
- PM2 save сделан
