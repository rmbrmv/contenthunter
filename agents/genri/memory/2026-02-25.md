# 2026-02-25 — Producer Copilot launch + Office fixes

## Producer Copilot (producer.contenthunter.ru:3850)
- **v1-v7** built in one session, deployed at producer.contenthunter.ru
- Стек: Express + static HTML (единый дизайн с dashboard — Tailwind, Inter font, bg-gray-100)
- Проекты из Airtable (app7fFym01YjkYHB8), 33 проекта с ключевиками
- UX: Tinder-style свайп видео (← скип, → лайк, клавиши + тач)
- Три платформы: YouTube (бесплатный API), TikTok (Apify), Instagram (Apify)
- Фильтры: язык (RU/EN/ES/PT), длительность (≤60с/≤3мин/все)
- Вкладки: Видео / Аккаунты / Понравилось
- Метрики: просмотры, подписчики, лайки, ER%, виральность%
- Транскрипция: yt-dlp + Groq Whisper
- Анализ смыслов + Адаптация: Claude API
- Prefetch: 200+ видео и 40+ каналов закачаны по всем проектам
- Видео в вертикальном формате 9:16
- Systemd: producer-copilot.service (enabled)

### API ключи (в systemd env):
- YOUTUBE_API_KEY=AIzaSyDHnPtJbhsc6yzjFuHVkWz-vALf1fWW9Vs
- GROQ_API_KEY (из /root/.openclaw/workspace/integrations/groq/api_key.txt)
- ANTHROPIC_API_KEY (из agent auth)
- APIFY_API_KEY=[REDACTED-APIFY-TOKEN] (от Даниила Павлова)

### Caddy
- producer.contenthunter.ru → localhost:3850
- DNS A-запись добавлена Романом в Reg.ru

## Agent Office fixes
- v33: No toast spam on first load
- v34: Security hardening (localhost bind, rate limit, sanitized API, admin token)
- v35: Removed task text from cards
- v36-39: Large tables, pathfinding, layout fixes
- v40: Fixed SOUL.md name parsing — heading regex priority over keyword match

## Обсуждения
- Уникализатор каруселей — AI-генерация вариантов постов (разные люди, интерьеры, текст). Русский текст лучше накладывать программно (Pillow), не генерить в картинке. Banana.dev как вариант для GPU. Пока не начато.
- ВСЕ СЕРВИСЫ в едином дизайне (dashboard, office, producer)

## Роман доволен скоростью разработки 🫡

---

## Обновления — вторая половина дня (12:00–19:00 UTC)

### Producer Copilot (продолжение)
- **v8-v18**: Серия фиксов и улучшений
- Виральность пересчитана: `max(0, views/subs - 1) * 100%` — 0% когда просмотры ≤ подписчиков
- Фильтр длительности: хранится `allVideos` unfiltered, фильтруется локально
- Фильтр языка: Apify fallback при исчерпанной YouTube API квоте, per-language кэш
- Кнопка "Подгрузить ещё" когда видео кончились
- **📥 Инбокс** на карточках проектов: показывает сколько видео ждёт в очереди
- Вертикальные видео 9:16 в лайкнутых
- Транскрибация: починён баг с `form-data` модулем
- Платформа аккаунтов: YouTube/TikTok/Instagram с Apify авто-поиском

### Agent Office
- v41: Звук починён — brown noise (0.08 gain), hum 220Hz (0.04 gain), keyboard clicks (0.07 gain)
- v42: Canvas `pointer-events: none` — кнопка 🔇 стала кликабельной (canvas перекрывал всё)
- Ping (C5-E5-G5) при появлении новой задачи агента

### Carousel Maker → интегрирован в Producer Copilot
- Отдельный сайт `carousel.contenthunter.ru` отменён (не нужен)
- **Уникализация** — второй раздел в Producer Copilot (боковое меню)
- Imagen 4 Fast (Google AI Studio): `imagen-4.0-fast-generate-001`
- Gemini API Key: `AIzaSyA6aChUo3g93p97zdLikPsv-AQ5YB6KpDk` (Tier 1, от GenGo HR)
- Флоу: загрузка слайдов → Claude Vision OCR → Claude перефразировки × 10 → Imagen 4 Fast → ZIP
- Без Gemini: Sharp fallback (цветовые тинты)

### Сайдбар в Producer Copilot (v18)
- Левое меню: 📹 Поиск трендов | 🎠 Уникализация
- Ссылки на Офис и Дашборд в нижней части сайдбара
- Все остальные функции сохранены

### Расходы
- Apify: ~$0.045 за поиск (1 запрос = 10-15 видео)
- Imagen 4 Fast: ~$X/gen (не замерили)
- YouTube API: 10K units/day (исчерпывается при 33 проектах × 3 запроса)

---

## Статус на 19:10 UTC

### Producer Copilot — объединение с Carousel Maker
- Роман попросил: НЕ делать отдельный домен для каруселей
- Всё объединить в producer.contenthunter.ru с левым сайдбаром
- **Сайдбар**: 📹 Поиск трендов (старое) | 🎠 Уникализация (новое)
- Carousel Maker логика перенесена в `/producer-copilot/public/index.html`
- Флоу уникализации: загрузить слайды → OCR → 10 текстовых вариантов → Imagen 4 Fast генерирует фоны → накладка текста → ZIP
- Эндпоинты в `producer-copilot/server.js`: `/api/uniqualize`, `/api/uniqualize/status/:jobId`
- carousel-maker.service — отдельный сервис (порт 3851) оставлен как есть, но фронт переехал

### Архитектурные решения зафиксированные сегодня
- Все 3 продукта (`office`, `producer`, `dashboard`) — единый дизайн Tailwind
- Используем Express + static HTML (NO Next.js / React / Zustand / SQLite)
- Canvas для офиса с `pointer-events: none` чтобы кнопки UI работали
- Imagen 4 Fast (NOT `imagen-4.0-flash-generate-001` — такой модели нет!)
- Русский текст в изображениях = через Pillow/Sharp overlay, не через промпт

### TODO (незавершённое)
- [ ] DNS для `carousel.contenthunter.ru` — добавить A-запись (если вообще нужен после объединения)
- [ ] Тест полного pipeline уникализации в браузере
- [ ] Mobile responsiveness Producer Copilot
- [ ] TikTok/Instagram видео через yt-dlp

### Ключевые файлы
- Office: `/root/.openclaw/workspace-genri/agent-office/` → port 3847
- Producer: `/root/.openclaw/workspace-genri/producer-copilot/` → port 3850
- Carousel (standalone): `/root/.openclaw/workspace-genri/carousel-maker/` → port 3851
- Caddy: `/etc/caddy/Caddyfile`

---

## Обновления — вечер (19:00–20:10 UTC)

### Carousel Maker — масштабные улучшения (вторая сессия)
- **Job-based async**: POST → jobId → poll `/api/carousel/status/:jobId` каждые 500мс
- **Прогресс-бар 0→100%**: 5% старт, +10% после OCR, +~(90/N)% за каждый вариант
- **Stop button**: POST `/api/carousel/stop/:jobId` → `job.cancelled=true`, проверяется в loop
- **Streaming variants**: каждый вариант добавляется в DOM сразу с fade-in и авто-скроллом
- **Два loading div**: `car-analyzeLoading` vs `car-loading` — фикс бага "0% навсегда"
- **Табы в Уникализации**: ✨ Генерация | 🗂 История | 💰 Расходы
- **История**: `data/carousel/history.json`, показывает дату/проект/кол-во вариантов/стоимость/превью
- **Кнопка удаления**: удаляет из JSON + диск; карточка убирается без перезагрузки
- **Rebuild from disk**: GET `/api/carousel/history/:sessionId` восстанавливает варианты из файлов
- **Выбор проекта**: дропдаун из 33 клиентов Airtable; `projectId` передаётся в генерацию
- **Brief из Airtable в промптах**: `brand: X, target audience: Y, niche: Z` в каждом Imagen промпте
- **Трекинг расходов**: `imagenCalls * $0.04` за job; хранится в истории; показывается в значке
- **Dashboard расходов**: `/api/carousel/spending` → бар-чарт по дням
- **Text overlay перписан**: `buildTextOverlaySvg()` с word wrap, `<tspan>`, semi-transparent rect
- **carReset()** скрывает оба loading div, очищает грид/кнопку скачивания
- **Кол-во вариантов 1-15** (было 5/10), default 10
- **Ссылка на Dashboard убрана** из сайдбара Producer Copilot (admin only)

### Agent Office — серьёзные фиксы
- **Privacy thought bubbles**: `parseSession()` читает `toolResult` entries (`msg.role==='toolResult'`); маппит `msg.toolName.toLowerCase()` в дружественные emoji-описания
- **Pathfinding**: `TOP_CORRIDOR_Y=152`, `TABLE_MID_Y=230`; новый `nearestV(x)`; `findPath()` — 4 случая
- **Ambient sound**: white noise + lowpass (600Hz, Q=0.8, gain=0.18); AC hum 60/120/180Hz

### Шрифты для каруселей — начата работа
- Цель: разные типографические пресеты для разных типов продуктов
- Директория: `/usr/local/share/fonts/carousel/`
- Скачаны TTF-файлы: BebasNeue, Montserrat-Bold, Oswald-Bold, Pacifico, Nunito-Variable
- **Proблема**: fc-query возвращает пустые имена для BebasNeue, Oswald, Montserrat TTFs
- Подтверждены fontconfig: Pacifico ("Pacifico"), Nunito ("Nunito")
- Montserrat .woff показывает неверные метаданные ("Montserrat Thin:style=Bold")
- Пипелайн: Sharp + SVG overlay через librsvg — нужны системные шрифты через fontconfig
- Пресеты запланированы: Мотивационный (Bebas), Бизнес (Montserrat), Редакционный (Oswald), Лайфстайл (Pacifico), Дружелюбный (Nunito)
- **Следующий шаг**: отладить почему fc-query пустой → подтвердить font-family имена → реализовать пресеты в SVG → UI дропдаун стилей


---

## Обновления — поздний вечер (21:00–22:00 UTC)

### Producer Copilot — добавление аутентификации (сессия)
- Задача: защитить `producer.contenthunter.ru` единым паролем
- Подход: `express-session` + single shared password
- Прогресс:
  - [x] `express-session` установлен (`npm install express-session`)
  - [x] Импорт добавлен в `server.js`
  - [x] `ACCESS_PASSWORD = process.env.PRODUCER_PASSWORD || 'contenthunter2024'` добавлен
  - [ ] Session middleware config (secret, resave, saveUninitialized)
  - [ ] Auth middleware function (`req.session.authenticated` check)
  - [ ] `GET /login` → login HTML page
  - [ ] `POST /login` → password check + set session
  - [ ] `GET /logout`
  - [ ] Apply middleware to all protected routes
  - [ ] Create login HTML page (unified design)
- **Пароль по умолчанию**: `contenthunter2024` (env: `PRODUCER_PASSWORD`)
- **Референс**: `/root/.openclaw/workspace/projects/openclaw-dashboard/server.js` — там bcrypt + express-session
- Компакция сессии до завершения реализации — нужно продолжить в следующей сессии

### Шрифты — статус
- BebasNeue, Oswald, Montserrat TTFs — пустой `fc-query`, проблема с метаданными шрифта
- Pacifico, Nunito — работают (`fc-list` подтверждает)
- Требует отладки в следующей сессии

### Зум фиксы (ранее сегодня)
- Баг дублирования встреч исправлен: ключ = UUID, не numeric meeting_id
- Авто-удаление cloud recordings с Zoom подтверждено
- Таблица `meetings.transcriptions` очищена: 6 чистых UUID-строк
- Caddy temp route для zoom OAuth реавторизации: `/zoom-auth → localhost:8080`

### Hall of Fame (Доска почёта)
- Эндпоинт `/api/hall-of-fame` — Factory DB, 30-day filter
- Кэш 30 мин, прогрев при старте (5с delay)
- Время запроса: ~7с прогрев, 13мс из кэша
- Join: `factory_inst_reels.account_id = factory_inst_accounts.instagram_id`

---

## Обновления — ночь (22:00–23:10 UTC)

### Carousel Generator (Генератор) — полная реализация
- **Задача**: Шаг 1 карусельного флоу — генерировать карусели с нуля (не уникализировать)
- **Tab**: `🎨 Генератор` добавлен в сайдбар Producer Copilot
- **Флоу**: выбрать клиента из Airtable → ввести тему → Gemini 2.5 Flash планирует слайды → Imagen 4 Fast генерит фоны → SVG overlay с текстом → ZIP
- **Эндпоинты**:
  - `POST /api/generator/generate` — старт job, возвращает `{ jobId }`
  - `GET /api/generator/status/:jobId` — polling, поля: `status`, `progress`, `slides[]`, `totalCost`
  - `POST /api/generator/stop/:jobId` — отмена
  - `POST /api/generator/regenerate-slide` — регенерация отдельного слайда
  - `GET /api/generator/download/:jobId` — ZIP скачивание
- **Модели**: Gemini 2.5 Flash для текста; Imagen 4 Fast для изображений ($0.04/шт)
- **Overlay функция**: `buildGeneratorSlideOverlay(width, height, title, body)` — SVG overlay с gradient id `gbg`, динамическая высота бара
- **Текст**: title ≤7 слов, body ≤15 слов (1 предложение) — не вылезает за overlay
- **Output dir**: `data/generator/output/{jobId}/slide_{n}.png`
- **Static route**: `/generator-output` → `data/generator/output`
- **JS globals**: `genCurrentJobId`, `genPollTimer`, `genSlideCount` (default 5), `genSlides[]`
- **AI review rating**: 8.5/10 — текст полностью виден, не обрезан, стабильный стиль

### Аутентификация Producer Copilot — ЗАВЕРШЕНА
- `express-session` + единый пароль `hunter2025`
- Env var: `PRODUCER_PASSWORD` в systemd
- Эндпоинты: `GET /login`, `POST /login`, `GET /logout`
- Auth middleware на всех защищённых маршрутах
- Сессионная cookie 7 дней

### Генератор — История, Расходы, Мост к Уникализации (незавершено)
- **Backend ГОТОВ**:
  - `GENERATOR_HISTORY_FILE = data/generator/history.json`, max 200 записей
  - `loadGeneratorHistory()` / `saveGeneratorHistory()` хелперы
  - `meta: { topic, projectId }` сохраняется в job
  - `saveGeneratorHistory(...)` вызывается при завершении job
  - `GET /api/generator/history` — отдаёт историю
  - `DELETE /api/generator/history/:jobId` — удаляет запись + файлы
  - `GET /api/generator/spending` — агрегация по дням, 30 дней
  - `POST /api/carousel/from-generator` — bridge: берёт jobId, OCR слайдов через Gemini, копирует PNG в carousel-uploads, возвращает формат как `/api/carousel/analyze`
- **Frontend ГОТОВ (HTML)**:
  - Табы в генераторе: ✨ Генерация | 🗂 История | 💰 Расходы
  - `genSwitchTab()` функция
  - `id="gen-uniq-btn"` кнопка "🎠 → Уникализация" (hidden до завершения job)
  - Divs для истории и расходов: `#gentab-history-content`, `#gentab-spending-content`
  - Stats cards: `gen-spend-total-cost`, `gen-spend-total-images`, `gen-spend-avg`
  - Bar chart container: `gen-spend-chart`
- **Frontend JS — НЕ ДОПИСАН** (сессия прервана компакцией):
  - [ ] `genLoadHistory()` — загрузить и отрендерить историю
  - [ ] `genLoadSpending()` — загрузить и отрендерить расходы
  - [ ] `genSendToUniq(jobId)` — мост к Уникализации

### Критические пути для продолжения
- Файл: `/root/.openclaw/workspace-genri/producer-copilot/server.js`
- Файл: `/root/.openclaw/workspace-genri/producer-copilot/public/index.html`
- Уникализатор JS entry point после analyze: найти где `carouselSlides` заполняется после `/api/carousel/analyze` — подключить оттуда `genSendToUniq()`
- Bridge endpoint: `/api/carousel/from-generator` — копирует в `CAROUSEL_DIR/uploads/gen_bridge_{jobId}_{n}.jpg`
- Формат ответа analyze: `{ slides: [{ id, previewUrl, originalPath, text, layout }] }`

---

## TODO — дизайн-консистентность Producer Copilot

Роман попросил сделать дизайн-пасс после завершения всех фич.

### Что неконсистентно:
- **Карточки форм**: Генератор `p-5 rounded-2xl`, дроп-зона Уникализации `p-6 sm:p-8` → унифицировать в `p-5`
- **Лейблы полей**: где-то `text-xs font-semibold uppercase tracking-wide text-gray-500`, где-то обычный текст → единый стиль
- **Табы**: Генератор/Уникализация используют `bg-gray-100 rounded-xl pill-style`, Видео использует `border-b tab-active` → выбрать один стиль
- **Заголовки секций**: иногда с подзаголовком, иногда без, разные размеры
- **Кнопки действий**: разные `rounded-xl` vs `rounded-lg`, разные padding

### Когда делать:
После закрытия всех фич (font presets, TikTok download, etc.)

---

## HR Payroll System — ЗАПУЩЕН (Feb 26, ~05:40 UTC)

### URL: hr.contenthunter.ru (порт 3852)
- Systemd: `hr-payroll.service` — active
- Директория: `/root/.openclaw/workspace-genri/hr-payroll/`
- Пароль: `hunter2025` (PRODUCER_PASSWORD env var)

### Схема БД
БД `openclaw`, схема `hr` — уже существовала до нас:
- `hr.persons` — PK=`id`, `gdrive_link` (не drive_folder_url!)
- `hr.employees` — PK=`id` (не staff_master!), `pay_type`/`pay_amount`
- employment_type: `tk_rf`/`freelance`/`self_employed`/`ip`/`gph` (EN enums)
- `hr.timesheet` — date (не work_date), day_type EN values: worked/paid_vacation/sick/etc
- `hr.events` — type (EN values), target_payment: advance/salary
- `hr.staff_changes` — change_type EN values: hire/transfer/salary_change/etc

### Разделы (sidebar)
1. 👥 Сотрудники — список, приём, редактирование, история изменений
2. 👤 Физлица — картотека, CRUD
3. 📅 Табель — матрица дней/сотрудников, клик по ячейке
4. 💰 Выплаты — расчёт аванса и зарплаты (с компенсацией 25р/день)
5. 📊 Начисления — бонусы, удержания, KPI
6. 🏢 Справочники — бренды, юрлица, способы выплат
7. 📋 Отчёты — по брендам, юрлицам, способам выплат, headcount

### API эндпоинты
- GET/POST/PUT/DELETE /api/persons, /api/brands, /api/legal-entities, /api/payment-methods
- GET/POST/PUT /api/staff, GET /api/staff/:id/history
- GET/POST/DELETE /api/timesheet
- GET/POST/DELETE /api/events
- POST /api/payroll/calculate { month, type: 'advance'|'salary' }
- GET /api/reports/headcount, /api/reports/by-brand, /api/reports/by-legal, /api/reports/by-payment

### Особенности
- Server.js переводит RU↔EN enums на API-слое (translateRow functions)
- Сид-данные: 2 бренда, 2 юрлица, 3 способа выплат, 2 физлица, 2 сотрудника
