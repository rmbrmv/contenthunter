# 2026-03-12 Memory Log

## Agent Office — monitor.html fixes

### "Доля" bug fix
- **File**: `/root/.openclaw/workspace-genri/agent-office/public/monitor.html`
- **Bug**: Line 962 used `maxCost` (max agent cost) instead of total — most expensive agent always showed 100%
- **Fix**: Replaced `maxCost` with `totalCostAll = agents.reduce((s,a) => s+(a.cost||0), 0)`
- **pct formula**: `totalCostAll > 0 ? Math.round((a.cost / totalCostAll) * 100) : 0`

### Model filter in Tokens tab (monitor.html)
- Added `tokensModel = 'all'` global variable
- Added `.model-filter-btn` CSS class (green active state)
- Backend: added `models: {}` breakdown to each agent in `/api/token-usage` response
- UI: model filter chips appear above period buttons in `#section-tokens`
- `buildModelFilter(agents)` — builds chips from aggregated model data
- `setTokensModel(m)` — filters agent table by selected model
- Period change resets model filter to 'all'

### Token stats panel added to index.html (sidebar)
- New endpoint `/api/token-stats` in server.js — hourly costs per agent + model breakdown, 24h window, 60s cache
- `#tokenboard` div + "Задачи/Токены" tab switcher in sidebar
- `fetchTokenStats()` called on load + every 60s
- Note: this was an error — user wanted monitor.html changes, not index.html. The index.html changes remain but are extra.

## PM2 state recovery
- **Pattern**: When PM2 daemon restarts fresh, `pm2 resurrect` restores from `/root/.pm2/dump.pm2`
- **office** (id=1, port=3847): `/root/.openclaw/workspace-genri/agent-office/server.js`
- **producer** (id=2, port=3850): `/root/.openclaw/workspace-genri/producer-copilot/server.js`
- **autowarm** (id=0): always survives
- After manual `pm2 start`, always run `pm2 save` to update dump

## Caddy outage
- Caddy stopped at ~09:47 UTC due to SSL cert renewal failure for `pixel.contenthunter.ru` and `carousel.contenthunter.ru`
- Fix: `systemctl restart caddy`
- Caddy config: `/etc/caddy/Caddyfile`
- office.contenthunter.ru → localhost:3847
- producer.contenthunter.ru → localhost:3850

## Key identifiers (updated)
- **monitor.html**: `/root/.openclaw/workspace-genri/agent-office/public/monitor.html`
- **index.html** (office canvas): `/root/.openclaw/workspace-genri/agent-office/public/index.html`
- **server.js** (office): `/root/.openclaw/workspace-genri/agent-office/server.js`
- **producer server**: `/root/.openclaw/workspace-genri/producer-copilot/server.js`
- PM2 dump: `/root/.pm2/dump.pm2`
