# Memory: 2026-03-17

## Большой день — Validator онбординг + Систематика

### Validator — онбординг-флоу
- Создал полный онбординг для клиентов: 5 шагов (Распаковка → Регистрация → Уникализация → Фарминг → Выкладка)
- Колонка `onboarding_stage` в `validator_projects`
- Бэкенд: `/api/onboarding/status` + `/api/onboarding/stage`
- Фронт: `OnboardingFlow.vue` + `appLoading` в App.vue
- Автосохранение бренда (onBlur + 800ms debounce)
- Прогресс-бар заполнения в sticky шапке (provide/inject из BrandPage)
- Спойлер "Почему важно заполнить бриф"
- Приветственный экран с дорожной картой
- Новые поля бренда: стоп-слова, воронка, формат аккаунтов
- ✨ Предложить ещё — генерация ключевых слов через Claude
- Иконки соцсетей SVG вместо эмодзи
- Мобильная адаптация
- Кнопки "Назад" и "Пропустить" для теста

### Кира — онбординг клиентов
- Обновлён SOUL.md: триггер "Кира начинай работу" → создаёт доступ → представляется → отправляет логин/пароль
- Скрипт `create_validator_access.py` — создаёт клиента по названию чата
- groupAllowFrom убран — Кира отвечает всем в группах
- dmPolicy = open — в личке перенаправляет в чат

### Сквозная история чата
- Таблица `validator_support_history` (project_id, role, content, source)
- Виджет поддержки сохраняет и загружает историю
- Кира получает контекст из истории при ответе

### Систематика — инфраструктура
- 3 Anthropic ключа: sys200 (token) + sys100 (oauth) + sys100-2 (oauth)
- auth.order настроен — ротация при overload
- Groq добавлен: whisper-large-v3 для транскрипции аудио
- Все 27 агентов получили shared/RULES.md и memory/

### Сессия Эдварда очищена
- 3.4MB сессия → rate limit
- Контекст сохранён в MEMORY.md и memory/2026-03-17.md
- Сессия сброшена

### Баги найдены и починены
- Дублирующий `default` токен Джони → 409 Conflict
- auth-profiles.json у агентов не содержал ключи → fallback не работал
- App.vue onMounted с 401 глотал ошибку → онбординг не показывался
- get_current_user импортировался из неправильного модуля
