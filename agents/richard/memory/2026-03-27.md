# Memory: 2026-03-27

## Агенты Систематики не отвечали — диагностика и фикс

### Причина 1: invalid config (garry)
- `"token"` вместо `"botToken"` → gateway валидировал конфиг и падал с status=1
- Исправил: `garry['botToken'] = garry.pop('token')`

### Причина 2: invalid config (garry subagents)
- `"allowlist"` вместо `"allowAgents"` → та же ошибка валидации
- Конфиг перезаписывался обратно между перезапусками gateway

### Диагностика
- `systemctl --user status openclaw-gateway` показал `status=1/FAILURE`
- Запустил gateway вручную: `node /usr/lib/node_modules/openclaw/dist/index.js gateway --port 18790`
- Вывод: `Config invalid: agents.list.40.subagents: Unrecognized key: "allowlist"`

### Итог
- Gateway запущен, агенты отвечают
- Garry — новый агент, добавлен с неверным форматом конфига

## Удалены агенты (протухшие боты) — 25 марта
- vseznayka, akaketo, redaktor, coordinator, apochemu, pisaka
- Причина: боты удалены через BotFather → 401 при старте

## Ротация API ключей — 25 марта  
- sys100-2 (OAuth) убран из ротации — ошибочно проверял как API key
- Возвращён обратно
- Рабочие: sys200 + sys100, sys100-2 требует обновления OAuth токена
