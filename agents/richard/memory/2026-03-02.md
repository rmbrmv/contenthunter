# 2026-03-02 — Дневной лог

## Модуль уникализации видео — Autowarm

### Источник
Изучен репо `GenGo2/unic-processing-service` (private) — Python-сервис уникализации видео через FFmpeg с привязкой к factory DB.

### Задача
Перенести уникализацию полностью к нам, отцепиться от factory DB.

### Что сделано

**БД (openclaw, localhost):**
- `unic_schemes` — 30 схем скопированы из factory_unic_schemes
- `unic_content` — 429 ассетов (оверлей видео, лого, аудио, паттерны)
- `unic_tasks` — задачи уникализации
- `unic_results` — готовые видео (ссылки S3)

**Python-воркер:** `/root/.openclaw/workspace-genri/autowarm/unic-worker/worker.py`
- PM2 процесс: `unic-worker` (id 10)
- Читает pending задачи → FFmpeg pipeline → S3 → сохраняет ссылки
- S3: бакет `1cabe906ea6e-gengo`, префикс `autowarm/unic/`
- MAX_WORKERS=2, POLL_INTERVAL=3s

**Валидация результата (после каждого FFmpeg):**
- Размер файла ≥ 100 КБ
- Длительность в пределах ±50% от ожидаемой (с учётом speed)
- Есть видео и аудио потоки
- Нет чёрного экрана (blackdetect в 3 точках)

**Retry логика:**
- До 3 попыток на схему при провале валидации
- Только после 3 неудач — схема идёт в ошибки

**API в Autowarm (server.js):**
- `GET /api/unic/schemes`
- `GET/POST/DELETE /api/unic/tasks`
- `GET /api/unic/tasks/:id`
- `GET /api/unic/stats`

**UI:** раздел "Уникализация" в delivery.contenthunter.ru (порт 3848)
- Статистика, форма создания задачи, список с прогрессом, модалка результатов с видеоплеером

### Тест
- Задача #1: 3 схемы (6, 10, 11) — все OK, ~40 сек/схема
- Задача #2: 1 схема + валидация — OK

### Следующие шаги (не обсуждались)
- Загрузка видео напрямую из UI (drag & drop → S3 → задача)
- Выбор проекта → своё лого из unic_content

---
## Продолжение сессии (09:30–09:46 UTC)

### Validator — баги от Генри (исправлены все 5)
1. ALLOWED_MIMES → только mp4/mov (убрали avi/webm/mkv)
2. /admin/projects — глотаем ошибку factory_projects
3. Лимит 80МБ→200МБ синхронизирован везде (Python + фронт)
4. resolution rule_config в БД: min_height:720, recommended_height:1080
5. generate-description промпты — явные запреты слов по нишам

### Validator — страница логина
- Два таба: 👤 Клиент / 🏢 Сотрудник
- Роутинг по роли: client→/dashboard, admin→/admin, manager→/manager, producer→/producer
- Светлый дизайн (gray-50 фон, white карточки, indigo акценты) — единый стиль платформы
- Пароль дефолтного admin сброшен Генри на Admin123!

### Validator — технические требования (чек-лист от менеджера)
- Длительность: 15–45 сек (идеал 20–35), блок <3с или >180с
- Формат: только MOV/MP4
- Размер: ≤200 МБ
- Разрешение: минимум 720p, рекомендуется 1080p
- Соотношение: вертикаль 9:16
- Аудиодорожка обязательна

### Уникализация — перенос воркера на внешний сервер
- Сервер: 91.98.180.103 (32 ядра, 124 ГБ RAM, 1.8 ТБ)
- Панель: Easypanel — ep.gengo.io
- PM2 unic-worker запущен на внешнем сервере
- Подключение к нашей БД: 72.56.107.157:5432 (docker-proxy)
- pg_hba.conf внутри контейнера openclaw-postgres: доступ для 91.98.180.103
- MAX_WORKERS=4 на внешнем сервере
- Локальный unic-worker остановлен и удалён

### Validator — текущее состояние
- validator.contenthunter.ru — live, 200 OK
- Caddy: /api/* → localhost:8000, static → /var/www/validator/
- postbuild hook: cp -r dist/* /var/www/validator/
- PM2: validator-api (id 11)
- Модерация: 9 ниш по гайду Жени
- Слоты: 3 по умолчанию, max 4
- AI описание: LaoZhang → claude-haiku-4-5-20251001

---
## Задача в ожидании (12:41 UTC)

### Кира — Zoom Voice Agent через Recall.ai
**Статус:** Ждём credentials от пользователя

**Что нужно получить:**
1. Recall.ai API key (recall.ai, бесплатно 5 часов)
2. ElevenLabs API key (Starter $5/мес, elevenlabs.io)
3. Запись голоса для клонирования (1-3 мин чистой речи)

**Что делать после получения:**
- Переписать /root/.openclaw/workspace-genri/zoom-voice-agent/zoom_bot.py
- Убрать симуляцию, подключить Recall.ai API (бот заходит в Zoom как участник)
- Подключить ElevenLabs TTS вместо gpt-4o-mini-tts shimmer
- Протестировать полный пайплайн: Zoom → STT → Claude → ElevenLabs → Zoom

**Текущий пайплайн (уже готов):**
- STT: gpt-4o-transcribe
- LLM: Claude Sonnet (через LaoZhang API)
- TTS: shimmer (временно) → заменить на ElevenLabs клонированный голос
- PM2: kira-voice (id 9), порт 4501, online 13h+

---
## Вечер (15:46–18:11 UTC)

### Обсуждение спецификации v3 (от Даниила Павлова)
- Получена полная техническая спецификация "Система автоматической уникализации видео v3"
- 5 модулей: VideoAnalyzer, SchemeGenerator, RenderPipeline, QualityAssurance, AssetGenerator
- Что стоит взять: Claude Vision анализ (free zones, face zone), QA уровень 2 (Whisper сравнение транскрипций), AI-генерация схем под клиента
- **AI реакт-видео** — решено делать через Kling via LaoZhang API (автоматически при онбординге нового клиента из Airtable)
- Триггер: Airtable "Бриф клиентов" (Table 1, base app7fFym01YjkYHB8)
- Кол-во реакт-видео = кол-во телефонов клиента (хранится в Google Sheets "расчёт CPM hunter")
- Google Sheets sync: /root/.openclaw/shared/scripts/sync_cpm_hunter.py (токен протух, SPREADSHEET_ID=1kMKgKgJKspoSny9rjZ5cKIyhXYe8IrD2Tstal4ZmpzY)
- В таблице CPM Hunter только финансы (контракты + оплаты), поля с кол-вом телефонов нет → нужно уточнить откуда брать

### Producer Copilot — критический баг (починен)
- Проблема: JS падал из-за `const carDZ = document.getElementById('car-dropZone')` на элементе в скрытой секции → null → всё JS не выполнялось включая loadProjects()
- Фикс: добавлен `if (carDZ) { ... }` guard
- Дополнительно: добавлена кнопка "🚪 Выйти" в сайдбар, функция doLogout()
- trust proxy + secure cookie для корректной работы сессий через Caddy HTTPS

### Producer Copilot — ПРОПАВШИЕ РАЗДЕЛЫ (НЕ ВОССТАНОВЛЕНЫ)
- Вчера Генри добавил разделы: ✍️ Тексты, ⚙️ Настройки, 📊 Расходы, 🎬 Видео (4 режима)
- Эти разделы НЕ были закоммичены и потерялись
- Последний коммит: v19 (e7df6e5) — только Поиск трендов + Уникализация
- **НУЖНО ВОССТАНОВИТЬ** все 4 раздела по памяти из /root/.openclaw/workspace-genri/memory/2026-03-01.md:
  - ✍️ Тексты: генерация постов через Claude Sonnet, типы пост/карусель/видео/stories/bio, платформы IG/TT/YT/VK/TG, 1–5 вариантов, история + расходы
  - ⚙️ Настройки: CRUD пользователей + матрица прав (viewer/editor/manager/admin), data/users.json + data/roles.json
  - 📊 Расходы: карточки Тренды(Claude/Gemini) + Тексты(Claude) + Карусели
  - 🎬 Видео: Text-to-Video / Image-to-Video / Редактор / Таймлапс через LaoZhang API
  - genSwitchTab() для История/Расходы генератора

### Ключи
- Anthropic direct key: [REDACTED-ANTHROPIC-KEY]
- ElevenLabs: sk_ae12428d7b19e8b7a064b8c46e38bcfaad103194992ee129 (бесплатный, кредиты исчерпаны)

### Состояние PM2
- producer (id 2): порт 3850, online — index.html починен, carDZ fix применён

---
## 18:11–18:18 UTC

### Producer Copilot — восстановление разделов (subagent запущен)
- Subagent runId: 8c321e9b-21bb-42f6-b1ca-3962f1c8a0d1
- Задача: добавить 4 раздела в сайдбар + секции в index.html
- Бэкенд API уже есть в server.js:
  - POST/GET /api/text/generate + /api/text/history
  - POST /api/video/generate + img2vid + edit + timelapse
  - GET /api/video/status/:jobId, /api/video/history
  - GET /api/spending
  - GET/POST/PUT/DELETE /api/users
- data/text_history.jsonl существует (бэкенд текстов работал раньше)

---
## 18:27–18:30 UTC — Контекст из переписки Генри

### Kira Zoom Voice Agent — итоговый статус
- **Zoom SDK credentials сохранены:** Client ID `suulVpnZTN6TpUA61t5xpQ`, Client Secret `1MHyq7xajRkJcD7wMXZKl2SPtzBSnj3P`
- **Zoom аккаунт для бота:** selenanavarro624@gmail.com / HMqsAEc33U!D8uB
- **Голос Натальи** записан (2:18), сохранён Генри как `kira_voice_full.mp3` в zoom-voice-agent/
- **ElevenLabs** sk_ae12428d7b19e8b7a064b8c46e38bcfaad103194992ee129 — бесплатный план исчерпан, нужен Starter $5/мес
- **Knowledge base** создана из 50 Zoom транскриптов → `knowledge_base.json`
- **Топ-10 кейсов** прописаны в промпте Киры (перебивание, тишина, агрессия, торг, прощание, etc.)
- **Streaming TTS** реализован — первый звук через ~4s
- **Ждёт:** оплата ElevenLabs Starter + Upload Natasha voice → получить Voice ID → подключить
- Zoom встреча 10:10 МСК уже прошла без Киры (не успели из-за ElevenLabs)

### Decodo прокси — статус
- **API ключ:** pSGt6dccf30ef039959ca5786318fc52d9bbac1d2e18ab96ad76d3cca32df8c45b402f360055f31487bea18acfa8decb3830af5edd1965783dc7f6a55dcec52012292fc38fc9ac2b5dcca357bbb99aca3b31c9ae75
- **Dashboard username:** spcbih7d10 (НЕ spmu3o8hyg — Генри перепутал)
- **Proxy password:** i5aRpgZj0P=eHe8p5r (спецсимвол = нужно URL-encode)
- **Host:** gate.decodo.com, порты 10001-10007
- **Прокси работает** (тест показал Саудовскую Аравию = Random)
- **Ждёт:** Купить UAE в дашборде (Proxy setup → Location → UAE → Generate) → скрин endpoint → настроить на телефоны
- proxy_manager.py написан Генри: `autowarm/proxy_manager.py`

### Hiddify на телефонах Celebration Station
- Установлен на 5/6 телефонов: 107, 108, 109, 113 (малинка 5), 116 (малинка 6)
- Телефон 112 — unauthorized (нужно подтвердить ADB на экране физически)
- Регион будет "неизвестен" пока не добавят UAE прокси

### Producer Copilot — незакрытый баг
- Генри фиксил Unauthorized в разделе Настройки (отсутствует `credentials: 'include'` в fetch запросах)
- Переписка оборвалась на этом — баг НЕ дофиксен
- Путь: /root/.openclaw/workspace-genri/producer-copilot/

### Прокси стратегия — масштаб
- Всего нужно 44 IP: UAE 35 + GE 6 + US 3
- Проекты с иностранным гео: Celebration Station (6 UAE), Symmetry (13 UAE), Flor (10 UAE), Ambassadori (6 GE+6 UAE), Trend Clone (3 US)
- Стоимость IPRoyal: ~$88/мес за весь парк
- Пилот: Celebration Station 6 IP = $12/мес

---

## [19:21 UTC] Producer Copilot — Генератор каруселей (продолжение работы)

### Проблема от Жени Киркицкого (forwarded)
- Баги в разделе "Генератор":
  1. Не даёт выбрать клиента
  2. Не даёт выбрать кол-во слайдов
  3. Кнопка "Создать сценарии" не работает
- Ещё баги: Image to Video "Ошибка запроса", уникализация с кривым результатом

### Диагностика
- **Секция `section-generator` вообще отсутствовала в HTML** — не была закоммичена, потерялась
- В JS были дубли функций: `carSwitchSource`, `carLoadGenHistory`, `carPickFromHistory`, `CAR_PRESETS` — каждая определена дважды → JS падал
- `LAOZHANG_BASE_URL` undefined в server.js (строки ~2135, 2151, 2237, 2255) — переменная называется `LAOZHANG_URL`
- **LaoZhang API quota exhausted** — `insufficient_quota` для ключа `sk-VHl2PVKV...` → все генерации падают

### Что исправлено
1. Восстановлена секция `section-generator` с нуля:
   - Дропдаун клиента, кнопки 3/5/7/10 слайдов, тема, референсы, "Создать сценарии"
   - Показ 3 вариантов сценария → выбор → "Генерировать слайды"
   - Прогресс-бар + превью слайдов в реалтайме
   - Кнопка "Отправить в уникализатор"
   - Вкладки: Генерация / История / Расходы
2. Удалены дублирующиеся функции (оставлена вторая, более полная копия)
3. Исправлен `LAOZHANG_BASE_URL` → `LAOZHANG_URL` (sed по всему server.js)
4. PM2 producer перезапущен
5. Добавлен `generator` в `switchSection()` массив
6. Добавлена кнопка "🎨 Генератор" в сайдбар (между Уникализацией и Текстами)

### Статус
- **Нужно пополнить баланс LaoZhang** — без этого генерация картинок не работает
- Технические баги исправлены
- Баг уникализации (кривой результат) — ждём уточнения от Жени

### Техническая карта файлов
- HTML: `/root/.openclaw/workspace-genri/producer-copilot/public/index.html` (~2500+ строк)
- Server: `/root/.openclaw/workspace-genri/producer-copilot/server.js`
- PM2: `producer` (id 2), порт 3850
- LaoZhang key в .env: `LAOZHANG_API_KEY=[REDACTED-API-KEY]` (EXHAUSTED)
- Generator output: `/generator-output/{jobId}/slide_N.png` (static served)

---

## [20:22 UTC] System Monitor — office.contenthunter.ru/monitor

### Задача
Роман (через main) запросил новый раздел на office.contenthunter.ru — визуализация работы агентов как "прозрачный корпус системы".

### Что сделано
**Файл:** `/root/.openclaw/workspace-genri/agent-office/public/monitor.html` (39KB)
**Route:** `GET /monitor` добавлен в `server.js` (`res.sendFile('public/monitor.html', { root: __dirname })`)

**3 новых API эндпоинта в server.js:**
- `GET /api/cron-status` — 32 крон-задачи с историей запусков (читает `/root/.openclaw/cron/jobs.json` + `/root/.openclaw/cron/runs/*.jsonl`)
- `GET /api/agent-metrics?period=day|week|month` — 19 агентов с токенами/задачами/временем ответа
- `GET /api/communications?period=day|week|month` — матрица коммуникаций (сканирует session JSONL на sessions_send tool calls)

**4 вкладки:**
1. 🕸 Граф агентов — D3.js force-directed, узлы=агенты, размер=токены, glow по статусу
2. 🌡 Матрица коммуникаций — heatmap 19×19, переключатель день/неделя/месяц
3. 📊 Метрики — карточки на каждый агент с bar нагрузки
4. ⏰ Крон-задачи — таблица 32 задач со статусами

**Дизайн (neon dark):**
- Фон: `#0a0a0f`, зелёный неон: `#00ff88`, ошибки: `#ff3366`, предупреждения: `#ffaa00`
- Шрифт метрик: JetBrains Mono
- CRT scanlines overlay, D3 glow filters, двойной слой ребер графа
- Обновление каждые 15 секунд

### Важные детали
- Office PM2: `office` (id 3), порт 3847
- `ADMIN_TOKEN` для полных данных: `7d8508a2570623516a8a35095dfcd68cb69e442776984530`
- Cron data: `/root/.openclaw/cron/jobs.json` (structure: `{version:1, jobs:[...]}`)
- Cron runs: `/root/.openclaw/cron/runs/{jobId}.jsonl` (fields: ts, jobId, status, durationMs, usage.total_tokens, nextRunAtMs)
- Agent sessions: `/root/.openclaw/agents/{agentId}/sessions/*.jsonl`
- Топ агент по токенам: Варенька (main) — 312,824 токенов
- Реальных коммуникационных линий сегодня: 65 (из сканирования sessions)

### Исправленный баг
- `LAOZHANG_BASE_URL` → `LAOZHANG_URL` в producer-copilot/server.js (4 места) — img2vid падал с `undefined/v1/videos`
