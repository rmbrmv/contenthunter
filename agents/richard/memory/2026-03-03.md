# 2026-03-03 — Дневной лог

## Перенос из вчера — незакрытые задачи

### 🔴 Заблокировано (ждём внешних действий)
- **LaoZhang квота исчерпана** (`insufficient_quota`, user 44333) — генератор карусели, уникализация, img2vid не работают. Нужно пополнить баланс на laozhang.ai
- **ElevenLabs Kira** — free key `sk_ae12428d7b19e8b7a064b8c46e38bcfaad103194992ee129` исчерпан. Нужен Starter план ($5/мес) → загрузить `kira_voice_full.mp3` → получить Voice ID → прописать в `zoom-voice-agent/config.py`
- **Decodo UAE прокси** — куплен Random geo (Саудовская Аравия). Нужно купить UAE IPs в дашборде (spcbih7d10) → Proxy setup → Location → UAE → Generate → настроить на Celebration Station

### 🟡 В процессе / не доделано
- **Producer Copilot — Настройки Unauthorized** — в разделе Настройки отсутствует `credentials: 'include'` в fetch-запросах. Баг известен, не зафиксирован
- **Уникализация преsets** — текущие пресеты (aggressive, fitness, dark) неправильные. Нужно заменить на визуальные правки: swap phone model, change clothing color, alter background
- **Zhenya's edits** — пользователь упоминал правки от Жени, файл так и не прислал
- **AI React Video** — запланировано, не начато. Airtable webhook → Kling через LaoZhang → сохранение в unic_content
- **Телефон 112 Hiddify** — нужно физически подтвердить ADB authorization на экране устройства

### ✅ Сдано вчера (2026-03-02)
- System Monitor: `office.contenthunter.ru/monitor` — D3 граф + heatmap + API эндпоинты
- Producer Copilot: восстановлена секция Генератор (`section-generator`)
- Producer Copilot: удалены дублирующиеся JS функции
- LAOZHANG_BASE_URL → LAOZHANG_URL в producer-copilot/server.js (img2vid fix)
- Daily reflection: обновлены learning/approved-patterns.md и anti-patterns.md

## Справочник быстрого доступа

| Сервис | Путь | PM2 | Порт |
|---|---|---|---|
| Autowarm | workspace-genri/autowarm/server.js | 0 | 3848 |
| Producer Copilot | workspace-genri/producer-copilot/ | 2 (producer) | 3850 |
| Agent Office | workspace-genri/agent-office/ | 3 (office) | 3847 |
| Validator API | workspace-genri/validator/ | 11 | 8000 |
| Unic Worker | root@91.98.180.103:/root/unic-worker/ | unic-worker | — |


---

## [07:45 UTC] Producer Copilot — Уникализация: добавлены вкладки

### Задача
Привести раздел Уникализация к той же структуре вкладок что и Генератор (Генерация / История / Расходы).

### Что сделано
- Добавлены 3 вкладки в `section-carousel`: 🎠 Уникализация / 📋 История / 💰 Расходы
- Весь существующий контент (загрузка, распознавание, генерация) обёрнут в `div#carcontent-generate`
- Добавлены `div#carcontent-history` и `div#carcontent-spending`
- Новые JS функции:
  - `carSwitchTab(tab)` — переключение вкладок
  - `carLoadUniqHistory()` — список прошлых сессий из `GET /api/carousel/history`
  - `carDownloadHistoryItem(sessionId)` — скачать ZIP варианта
  - `carDeleteHistoryItem(sessionId, btn)` — удалить из истории
  - `carLoadUniqSpending()` — расходы из `GET /api/carousel/spending`
- JS синтаксис проверен (`new Function()` — OK), PM2 producer перезапущен

### API (уже существовали в server.js)
- `GET /api/carousel/history` — список сессий
- `GET /api/carousel/history/:sessionId` — детали сессии
- `GET /api/carousel/download/:sessionId/:variantId` — скачать ZIP
- `DELETE /api/carousel/history/:sessionId` — удалить сессию
- `GET /api/carousel/spending` — расходы (из spending_log.jsonl, service='uniqualizer')

---

## [07:52 UTC] Producer Copilot — продолжение фиксов

### Исправления

**1. Настройки — Unauthorized (давний баг)**
- Причина: `fetch('/api/users')` без `credentials: 'include'` → браузер не отправлял сессионный куки
- Фикс: sed по всем 4 вхождениям fetch('/api/users...) добавил `credentials: 'include'`

**2. Уникализация — ошибка в Расходах**
- Причина: `GET /api/spending` не существовал → 404 → HTML → `JSON.parse` падал
- Фикс: добавлен endpoint `GET /api/spending` в server.js
  - Читает `SPENDING_LOG_FILE` (spending_log.jsonl)
  - Возвращает: `{ total, breakdown: {trends, carousels, texts, video}, rows[] }`
  - serviceMap: trends/producer-copilot/gemini-plan→trends, uniqualizer/carousel/generator→carousels, text→texts, video→video

**3. Генератор — добавлено поле промпта**
- Добавлен textarea `#gen-extra-prompt` между блоком Референсы и кнопкой "Создать сценарии"
- Label: "Дополнительные инструкции (опционально)"
- Placeholder: "Например: используй юмор, пиши от первого лица, добавь статистику..."
- В `genGetPlan()`: читает значение и передаёт как `extraPrompt` в POST /api/generator/plan
- В server.js: `extraPrompt` вставляется в промпт: `\nДополнительные инструкции: ${extraPrompt}\n`

**4. Уникализация — убран внутренний переключатель**
- Удалены кнопки "📤 Загрузить файлы / 📚 Из истории генератора" из car-step1
- Теперь сразу показывается зона загрузки без лишних вкладок

### Состояние файлов
- index.html: ~2600+ строк, JS чистый (проверен `new Function()`)
- server.js: добавлен `GET /api/spending` (~строка 2541)
- PM2 producer (id 2) перезапущен несколько раз, online

---

## [07:56 UTC] Producer Copilot — Доделки после генерации

### Задача
Добавить поле промпта для доделок на странице результатов генератора.

### Что сделано
**UI (`#gen-results`):**
- Блок "✏️ Доделки" добавлен после `#gen-results-grid`
- textarea `#gen-refine-prompt` — описание что изменить
- 3 кнопки: "📝 Переписать тексты" (`genRefine('text')`), "🖼 Перегенерить картинки" (`genRefine('images')`), "🔄 Всё сразу" (`genRefine('all')`)
- `#gen-refine-status` — спиннер во время обработки

**JS (`genRefine(mode)`):**
- Читает `genCurrentJobId` + промпт из `#gen-refine-prompt`
- `mode='text'` → POST /api/generator/refine → получает `{slides:[]}` → обновляет grid
- `mode='images'` → POST /api/generator/refine → получает `{jobId}` → запускает poll через `genPollStatus()`
- `mode='all'` → сначала тексты, потом images

**Server.js (`POST /api/generator/refine`):**
- `mode='text'`: переписывает через Gemini LLM, обновляет `job.slides[i].title/text`, возвращает `{slides}`
- `mode='images'`: создаёт новый jobId, запускает `runGeneratorJob()` с промптом добавленным к imagePrompt каждого слайда, возвращает `{jobId}`
- `mode='all'`: text + images

**Статус:** JS OK, PM2 producer перезапущен

---

## [09:08 UTC] Producer Copilot — Перекомпоновка формы генератора

### Задача
Сделать форму генератора удобнее — убрать большую пустую зону референсов, уместить всё без скролла.

### Новый layout (4 строки вместо 5):
1. **Клиент | Тема/ниша** — grid 2 cols
2. **Количество слайдов (flex) | Референсы (компактная строка)** — grid 2 cols
   - Референсы: inline строка с иконкой 🖼️ + текст "Добавить файлы (до 5)"
   - При выборе: показывает "N файла выбрано" + миниатюры 48×48
3. **Дополнительные инструкции** — textarea полная ширина
4. **Кнопка "🎯 Создать сценарии"** — right align, чуть крупнее (px-8 py-3)

### Изменения стиля:
- Labels: `text-xs font-semibold text-gray-500 uppercase tracking-wide` (вместо `text-sm font-medium`)
- Inputs/selects: `bg-gray-50` фон
- Ref drop zone: `border border-dashed` (не border-2), высота как у инпута
- `genHandleRefs()`: обновляет `#gen-refs-label` текст + миниатюры 48×48 (было 64×64)

---

## [09:10 UTC] Celebration Station — актуальная конфигурация телефонов

### Источник: Даниил Павлов (@Danil_Pavlov_123) подтвердил "порты те же"

| id | Pi # | ADB   | Host           | Scr   | Port  |
|----|------|-------|----------------|-------|-------|
| 1  | 1    | 15017 | 147.45.251.85  | 27000 | 27958 |
| 2  | 2    | 15037 | 147.45.251.85  | 27010 | 27998 |
| 3  | 3    | 15038 | 147.45.251.85  | 27040 | 27988 |
| 4  | 4    | 15028 | 147.45.251.85  | 27030 | 27968 |
| 5  | 5    | 15048 | 147.45.251.85  | 27050 | 27800 |
| 6  | 6    | 15058 | 147.45.251.85  | 27060 | 27810 |
| 7  | 7    | 15068 | 147.45.251.85  | 27080 | 27820 |
| 8  | 8    | 15088 | 147.45.251.85  | 27088 | 27830 |
| 9  | 9    | 15098 | 82.115.54.26   | 27098 | 27840 |
| 10 | 10   | 15108 | 147.45.251.85  | 27108 | 27850 |

- Основной хост: `147.45.251.85` (9 из 10 малинок)
- Pi #9 — другой хост: `82.115.54.26`
- ADB подключение: `adb connect {host}:{adb_port}`
- Телефоны 107, 108, 109, 113 (pi5), 116 (pi6) — Hiddify установлен ✓
- Телефон 112 — нужно физически подтвердить ADB авторизацию на экране

---

## [09:17 UTC] Celebration Station — ADB подключение разобрано

### Ключевое открытие
ADB Server для ВСЕХ телефонов: `147.45.251.85`
Поле `host` в таблице `raspberry_port` — только для **scrcpy** (стриминг экрана), НЕ для ADB!

### Правильный способ подключения (как Генри)
```bash
adb -H 147.45.251.85 -P {adb_port} -s {device_id} shell "{cmd}"
```

### Pi #9 — статус
- DB показывал `host=82.115.54.26` → этот IP недоступен (timeout на всех портах)
- Реальное подключение: `adb -H 147.45.251.85 -P 15098 -s RFGYA19DBAX shell ...`
- Устройство: **Samsung SM-A175F** (Galaxy A17), серийник `RFGYA19DBAX`
- Статус: **online** ✓

### Factory DB — таблицы телефонов
- `raspberry_port` (на 193.124.112.222:49002, db=factory, user=roman_ai_readonly, pass=Bo37H#Kla8dl0chQnL0@3jSlcY)
  - Поля: id, raspberry_number, adb, host, scr, port
- `device_numbers` — device_number, device_id, active, raspberry, warmup_end_date
  - Pi #9: device_number=12, device_id=RFGYA19DBAX, active=t, raspberry=9
- `device_state` — device_id, server, online, device_number, battery etc.

### Hardcoded в коде Генри
- `phone_warmer.py`: `ADB_HOST = '147.45.251.85'`
- `publisher.py`: `ADB_HOST = '147.45.251.85'`

---

## [09:58 UTC] ADB Pi #9 — уточнение

### Обновление
`82.115.54.26:15098` — **работает**! Ранее просто временно не отвечало (флакает).

### Итоговая схема подключения
- **Все Pi кроме #9**: `adb -H 147.45.251.85 -P {adb} -s {serial}`
- **Pi #9**: `adb -H 82.115.54.26 -P 15098 -s {serial}` (свой хост)

Поле `host` в `raspberry_port` = IP ADB сервера малинки (не scrcpy).
Т.е. для каждой малинки нужно брать `host` + `adb` из таблицы, а не hardcode `147.45.251.85`.

---

## [10:06 UTC] Celebration Station — screen/node API разобран

### Screen API (порт из колонки `scr` + `port` в raspberry_port)
- Pi #8: scr=27088, port=27830 → оба порта отвечают на TCP
- `http://147.45.251.85:27830/openapi.json` → FastAPI (uvicorn)
- Endpoints: GET /server, GET /devices, GET /device/{device_id}, **GET /device/{device_id}/screen**, GET /remove/{file_id}, POST /devices/reconnect, GET /node/sysmon, GET /node/reset
- Auth: **HTTPBearer** — нужен Bearer токен
- Токена у нас нет (нет в .env autowarm, нет в factory DB), принадлежит Даниилу

### Рабочий способ получить скриншот
```bash
adb -H {host} -P {adb} -s {device_id} shell screencap -p /sdcard/screen.png
adb -H {host} -P {adb} -s {device_id} pull /sdcard/screen.png /tmp/screen.png
```
Работает без токена через ADB.

### Метрики телефонов (замер сегодня)
| Pi | Serial | ADB latency | App cold start | CPU load | RAM avail | Battery |
|---|---|---|---|---|---|---|
| #9 | RFGYA19DBAX | ~780ms | 738ms | 22.58 | 3.3GB/7.8GB | 79% |
| #8 | RFGYB07Y5TJ | ~556ms | 1001ms | 22.61 | 4.3GB/7.8GB | 100% |

CPU load ~22 на всех малинках — по всей видимости норма.

---

## [11:39 UTC] Экосистема агентов — фарминг

### Агенты-фермеры
- **Альфия** (`agent:alfiya`) — Haiku 4.5, агент публикаций, "готова к публикациям"
- **Толик-алгоритмы** (`agent:tolik-algoritmy`) — Haiku 4.5, **персона Нурия**, выполняет прогрев: 31 видео/день, лайки; день 2 стартует 03.03

### Как работает делегирование
- Фарм-агенты работают **автономно** через cron — сами запускают `warmer.py` / `publisher.py` через ADB
- Генри и Ричард строят инструменты, агенты их используют напрямую
- Прямой `sessions_send` от нас к ним — не основной путь, задачи идут через cron/БД

### Все известные агенты (из sessions_list 03.03.2026)
| Agent key | Модель | Роль |
|---|---|---|
| richard | Sonnet 4.6 | Разработчик (я) |
| genri | Sonnet 4.6 | Разработчик |
| main (Варенька) | Opus 4.6 | Главный агент |
| plahov-operdir | Sonnet 4.6 | Операционный директор |
| pasha-novostnik | Sonnet 4.6 | Новостник (AI дайджест) |
| fyodor | Sonnet 4.6 | Аналитик |
| dasha-smyslovik | Sonnet 4.6 | Смысловик (Zoom транскрипты) |
| alfiya | Haiku 4.5 | Фермер/выкладчик |
| tolik-algoritmy | Haiku 4.5 | Фермер (персона: Нурия) |
| mark-dizayner | Sonnet 4.6 | Дизайнер |
| misha-metodolog | Opus 4.6 | Методолог |
| kira-pomoschnitsa-km | Sonnet 4.6 | Помощница КМ (клиент. менеджер) |
| elena | Sonnet 4.6 | HR |

---

## [11:43 UTC] Autowarm — сортировка задач не сохранялась

### Баг
Сортировка в таблице задач (`/tasks`) сбрасывалась каждые 10 секунд — `setInterval` вызывал `loadTasks()` без учёта текущей сортировки.

### Фикс
- Добавлена функция `_applyTasksSort(arr)` — применяет `_tasksSortCol` + `_tasksSortDir`
- `loadTasks()` теперь после получения данных проверяет `_tasksSortCol` и если задан — применяет сортировку перед `renderTasksTable()`
- `sortTasksTable(col)` вызывает `_applyTasksSort([..._tasksAll])` вместо inline sort
- PM2 autowarm (id 0) перезапущен

### Паттерн (записать в approved-patterns)
При автообновлении таблиц через setInterval — всегда сохранять и переприменять сортировку/фильтры после каждого рефреша данных.

---

## [13:13 UTC] Задачи Натальи не отображались — Юра использует ad-hoc скрипты

### Проблема
Пользователь видел что Наталья фармила "через Юру много раз", но задачи не появлялись в таблице autowarm.

### Расследование
- Ни в local `autowarm_tasks` (openclaw DB), ни в factory DB — записей от сегодня нет
- Оказалось: Юра (агент) создаёт одноразовые скрипты в `/tmp/yt_farm2.py` и запускает напрямую через ADB
- Пример: `python3 /tmp/yt_farm2.py` → телефон RFGYA19DBAX (Pi #9), YouTube Shorts, 20 видео
- Результаты пишутся в `/tmp/yt_results.json`, НЕ в БД
- В итоге: 20 просмотров, 0 лайков/подписок (все пропущены)

### Вывод
Агент Юра (`workspace-yura`) пишет ad-hoc скрипты без интеграции с `autowarm_tasks`. Для трекинга нужно либо заставить скрипты создавать записи в `autowarm_tasks`, либо сделать отдельную таблицу `ad_hoc_runs`.

### Контекст
- Pi #9: host=82.115.54.26, adb=15098, device RFGYA19DBAX (Samsung SM-A175F)
- Workspace Юры: `/root/.openclaw/workspace-yura/`

---

## [13:46 UTC] Ad-hoc runs — система логирования произвольных скриптов

### Что сделано
- Создана таблица `ad_hoc_runs` в openclaw DB (agent, device_serial, description, status, results, log, started_at, finished_at)
- Добавлены API эндпоинты в autowarm server.js:
  - POST /api/ad-hoc/start → { runId }
  - POST /api/ad-hoc/finish/:id
  - GET /api/ad-hoc/runs
- Создан Python-хелпер `/root/.openclaw/workspace-genri/autowarm/ad_hoc_logger.py` — context manager `AdHocRun`
- В UI (раздел Задачи прогрева) добавлен блок «🧪 Прогоны вручную» под основной таблицей

### Использование
```python
from ad_hoc_logger import AdHocRun
with AdHocRun(agent='yura', device='RFGYA19DBAX', description='YouTube Shorts') as run:
    # ... код ...
    run.results = {'viewed': 20}
```

### Producer Copilot — Generator История
- Переработан вид карточек: полная карусель слайдов (горизонтальный скролл), токены, стоимость, кнопка ⬇️ ZIP
- Убран текст темы из карточки (только мета-строка)
- Расходы переделаны с "последних генераций" на группировку по дням с прогресс-барами

---

## [13:56 UTC] Instagram Archiver — построена система архивации

### Задача
Ежедневно находить посты опубликованные ровно 3 дня назад с < 50 просмотров → архивировать.

### Что построено
- **БД**: `archive_tasks` + `archive_log` в openclaw DB
- **Python скрипт**: `autowarm/instagram_archiver.py` — ADB навигация по Instagram, парсинг дат/просмотров через uiautomator, антидетект (рандомные задержки 1.5–9с, джиттер координат)
- **Планировщик**: `autowarm/archive_scheduler.py` — берёт все active=true Instagram аккаунты из factory DB, запускает задачи
- **API**: `GET /api/archive/tasks`, `/api/archive/stats`, `/api/archive/tasks/:id/log`, `POST /api/archive/run`
- **UI**: новый раздел «🗄️ Архивация» в autowarm (delivery) с метриками, таблицей задач, детальным логом постов
- **Cron**: ежедневно 06:00 UTC (09:00 МСК), job id `fb3423ba-0f01-417b-a86f-62dd6dbe2eb6`

### Ключевые решения
- Только посты ровно 3 дня назад (не старше!) — чтобы не снести весь архив
- Лимит: 20 постов проверяем, 10 архивируем за один прогон
- Аккаунты из `factory_inst_accounts` WHERE active=true AND platform='instagram'
- Статус: ждёт первого теста на реальном устройстве

---

## [14:04 UTC] Архиваторы TikTok и YouTube — добавлены

### Что добавлено
- `archiver_base.py` — общий базовый класс: ADB хелперы, parse_days_ago(), parse_views(), BaseArchiver
- `tiktok_archiver.py` — TikTok: ⋮ → Privacy settings → Only me
- `youtube_archiver.py` — YouTube Studio: ⋮ → Edit video → Visibility → Private
- `archive_scheduler.py` — обновлён: запускает все 3 платформы (instagram/tiktok/youtube)
- API `POST /api/archive/run` — принимает параметр `platform`, роутит на нужный скрипт
- UI — фильтр по платформе, иконки 📸/🎵/▶️ в таблице

### Масштаб
- Instagram: 233 активных аккаунта
- TikTok: 182 активных аккаунта  
- YouTube: 180 активных аккаунтов
- Cron: ежедневно 06:00 UTC (09:00 МСК)

### Статус
Код написан, деплоен. Ждёт первого теста на реальном устройстве.

---

## [14:54 UTC] Ручной тест архиватора Instagram — lavka_radosti_store

### Что протестировано
- Устройство: RF8YA09SNNT, host 147.45.251.85, adb 15037, battery 100%, online
- Аккаунт: lavka_radosti_store

### Результаты
- Пост №1: 110 просмотров, 25 февраля (6 дней) → старше 3д, скрипт остановится
- Пост №2: 30 просмотров, 24 февраля (7 дней) → старше 3д
- Пост №3: 44 просмотра, 21 февраля (10 дней) → старше 3д
- У аккаунта нет поста от 28 февраля — скрипт правильно ничего не тронет

### Навигация работает
- Instagram открывается через am start
- Профиль находится через resource-id com.instagram.android:id/profile_tab, bounds [864,2070][1080,2205]
- Посты в сетке находятся как Button с content-desc "Видео Reels ... в строке N"
- Дата и просмотры читаются после скролла вниз внутри поста

### Ключевые координаты (для этого устройства)
- Профиль таб: tap 972 2137
- Посты сетки строка 1: y=1164–1639, x: 178 / 540 / 902
- Посты сетки строка 2: y=1645–2070

### Статус
Логика работает корректно. Для полного теста нужен аккаунт с постом от 28 февраля с < 50 просмотрами.

---

## [15:07 UTC] Producer CRM — добавлен в validator.contenthunter.ru

### Что построено
- **БД**: `producer_orders` + `producer_contractors` в openclaw DB
- **Backend**: `/root/.openclaw/workspace-genri/validator/backend/src/routers/producer.py`, prefix `/api/producer`
  - GET/POST/PATCH/DELETE /orders, /contractors
  - GET /stats (by_stage, finance месяц, workload исполнителей)
- **Frontend** (Vue 3, добавлено в validator):
  - `CrmOrders.vue` — таблица заказов, фильтры, смена этапа inline, модалка создания/редакт
  - `CrmKanban.vue` — drag&drop канбан доска (8 колонок = 8 этапов)
  - `CrmContractors.vue` — карточки исполнителей со статистикой
  - `CrmFinance.vue` — выручка/расходы/прибыль/маржа за месяц + нагрузка исполнителей
- **Роутер**: `/producer/crm`, `/producer/crm/kanban`, `/producer/crm/contractors`, `/producer/crm/finance`
- **Сайдбар**: 4 новые ссылки в секции Продюсер

### Этапы (stages)
task_received → finding_contractor → scripting → at_contractor → draft_received → ready → payment_pending → done

### Форматы
ugc, barter, montage, reel

### Тестовые исполнители (seeded)
Алексей (ugc), Марина (montage), Тимур (reel), Ольга (barter)

### Примечание
Пароль admin сброшен на Admin123! (был слетевший хеш после перезапуска)

---

## [18:26 UTC] Producer CRM — доработка формы заказа

### Изменения в CrmOrders.vue
- **Клиент** → dropdown из `validator_projects` (active=true), поле `project_id` вместо `client_name`
- **Ссылка на видео** → убрана из формы
- Добавлены поля: **quantity** (кол-во) + **price_per_unit** (цена/шт)
- **Сумма исполнителю** = расчётная (quantity × price_per_unit), вычисляется в SQL и показывается как голубая строка в форме
- В таблице при quantity > 1 показывается `N×₽цена`

### Изменения в БД
- `ALTER TABLE producer_orders ADD COLUMN quantity INTEGER DEFAULT 1, price_per_unit NUMERIC(12,2)`

### Изменения в бэкенде (producer.py)
- Добавлен `GET /api/producer/projects` → список активных проектов из validator_projects
- contractor_amount вычисляется в SELECT: `COALESCE(quantity,1)*COALESCE(price_per_unit,0)`
- JOIN с validator_projects для project_name

### Идея (не реализована)
Пользователь предложил: рабочий Telegram аккаунт продюсера + Telethon → парсить все DM с исполнителями → авто-матчинг к заказам по @username, авто-скачивание файлов в S3. Технически реализуемо через Telethon. Нужно: api_id + api_hash + телефон аккаунта.
