# Memory: 2026-03-20

## Account Factory — баги починены
- Gmail регистрация: добавлено ожидание загрузки страницы имени (до 15 сек, 5 попыток) + верификация что текст введён
- YouTube: обработка плашки "Обновите приложение" — 3 попытки Back/«Не сейчас», потом force stop + перезапуск, если не помогло → `yt_status='skipped_update_wall'`
- register_social.py: добавлены screencap доказательства после каждой платформы (`/sdcard/proof_ig_*.png`, `proof_tt_*.png`, `proof_yt_*.png`)
- Коммит: `b2aef82` "fix: YouTube update wall + Gmail name page wait + screenshots after social reg"

## Марк-дизайнер перенесён на Систематику
- workspace скопирован на 64.188.63.84
- Добавлен в openclaw.json Систематики (agents + telegram account + binding)
- Удалён с CH: workspace удалён, из конфига убран

## office.systematika.pro — большой ремонт
- Проблема: Динэш переключил office.systematika.pro на свой client-office (порт 3858), уронив основной офис
- Основная причина пустого canvas: WebSocket `ws://` блокировался браузером при HTTPS (нужно `wss://`)
- Основная причина зависания: `execSync('openclaw sessions...')` блокировал event loop на 20+ сек
- Решение: запущен `agent-office-sys` (правильный офис, 2029 строк) на порту 3857 из `/root/agent-office-sys-3857/`
- Caddy: `office.systematika.pro` → 3857 (основной офис)
- PM2: `office-3857` online

## Мультитенантный клиентский офис
### Архитектура
- `office.systematika.pro/` → основной офис Систематики (port 3857)
- `office.systematika.pro/{32-hex-token}` → клиентский офис (тот же сервер, роут `/:token`)
- Токен читается из `window.location.pathname` через regex `^[0-9a-f]{32}$`
- БД: `systematika_clients` (через SSH туннель localhost:5432 → CH сервер)
- SSH к VPS клиента для получения статусов агентов

### Что добавлено в server.js (agent-office-sys-3857)
- `getClientByToken(token)` — ищет клиента в систематика_clients по токену
- `getClientAgents(token)` — SSH на VPS клиента, читает mtime сессий агентов
- `GET /:token` — отдаёт наш HTML, токен читается из pathname
- `GET /api/agents/client?token=` — агенты клиента с русскими именами из AGENT_META
- `GET /api/taskboard?token=` — пустой (не показываем наши задачи Джони/Динэша)
- Аналогично для `/api/agent-tasks`, `/api/token-stats`, `/api/timeline`, `/api/connections`, `/api/completions`
- SVG аватарки для всех фреймворк-агентов (marketer, copywriter, critic1/2, scenarist + mg-* + f1-* + elena-hr)

### Фреймворки и агенты
```
redakciya:      marketer, copywriter, critic1, critic2, scenarist
marketing-guru: mg-admin, mg-gtm, mg-scout, mg-content, mg-economist, mg-focus
elena-hr:       elena, administrator, analyst, sociologist, coach
biz-lab:        f1-admin, f1-analyst, f1-financier, f1-marketer, f1-negotiator, f1-board-cis, f1-board-global
```

### Тестовые данные
- Алёна Батунова: IP=37.27.219.55, pass=`Sys417551fde744e0b4!`, token=`96aef5f9d57bd7c24593066f30015eea`
- URL: office.systematika.pro/96aef5f9d57bd7c24593066f30015eea
- Hetzner API токен (аккаунт Алёны): `UHjtDFZ3qQUti76AqmiBQ89qjVt3qRgih85CecIn3k4mLR6pwqIKhQUhYEKfRj1a`
- Сервер Алёны: Hetzner ID=124319381, name=sys-alena-batunova
- SSH ключ нашего сервера добавлен в Hetzner аккаунт Алёны (ID=109437683)

### Проблемы которые решили
1. WebSocket ws:// → wss:// при HTTPS (патч в public/index.html)
2. execSync заменён на spawn (асинхронный) — сервер не зависает
3. Токен читается из pathname а не query (pathToken regex)
4. Caddy: regex `@client path_regexp ^/[0-9a-f]{32}` убран — всё на 3857, сервер сам роутит
5. SSH ключ изменился на 45.82.14.135 (REMOTE HOST IDENTIFICATION HAS CHANGED) — этот IP не наш, тестовые данные удалены из БД
6. Taskboard показывал наших агентов — добавлен early return при наличии токена

## PM2 на Систематике (64.188.63.84)
| ID | Имя | Порт | Статус |
|----|-----|------|--------|
| 18 | office-3857 | 3857 | online |
| 13 | client-office | 3859 | online (не используется для основного офиса) |
| 14/17 | installer-bot | — | online |
| 10 | sys-dashboard | 3856 | online |
| 1 | office-sys | 3848 | online |

## Caddy конфиг (64.188.63.84)
```
office.systematika.pro → localhost:3857 (всё, включая /{token})
my.systematika.pro → localhost:3848
clients.systematika.pro → localhost:3859 (не используется)
```

## Ключевые пути на Систематике
- Основной офис: `/root/agent-office-sys-3857/` (копия из `/root/agent-office-sys/` с PORT=3857)
- Оригинал: `/root/agent-office-sys/` (PORT=3848)
- Клиент-офис Динэша (не используется): `/root/.openclaw/workspace-dzheff/team-copilot/client-office/`

## Антипаттерн дня
- Всегда проверять через какой порт/домен Caddy раздаёт маршрут ПЕРЕД изменением server.js
- При добавлении токен-роутинга в Caddy — все запросы к /api/* тоже попадают под regex и уходят не туда

## 2026-03-23 — validator: фикс пустого стоп-слова (Юра)

В `validator_moderation_rules` оказалось правило с `word=''` — блокировало всё.
Удалено из БД + добавлен guard в `moderation_service.py` + валидация в `admin.py`.
Правило: поле `word` в таблице всегда непустое, API блокирует создание пустых правил (HTTP 400).
Коммит: `2d91764` в GenGo2/validator-contenthunter
