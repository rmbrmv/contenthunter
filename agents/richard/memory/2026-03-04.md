## [15:01-15:10 UTC] Decodo — мобильные прокси (Europe)

### Credentials (мобильные, Europe continent)
- Host: `gate.decodo.com`
- Порты: 10001–10010
- Username: `user-sp50em9b0d-continent-eu`
- Password: `f9qTz8qxU5o2DrazD_`
- Тип: Sticky 10min, Europe

### Рабочие IP (протестированы)
| Порт | Страна | Город | ISP | IP |
|---|---|---|---|---|
| 10001 | PL | Warsaw | P4 | 83.175.177.29 |
| 10002 | GB | Westhill | BT | 217.44.6.22 |
| 10003 | PL | Szczecin | Orange PL | 80.48.2.13 |
| 10004 | GB | Newcastle | BT | 81.151.236.229 |
| 10005 | PL | Gdansk | Orange PL | 37.47.210.84 |
| 10006 | CH | Dietikon | Sunrise | 194.230.158.90 |
| 10007 | NO | Oslo | Get AS | 84.212.193.113 |
| 10008 | GB | Chester | Sky UK | 151.229.159.229 |
| 10009 | GB | Rochdale | Vodafone | 80.176.66.223 |
| 10010 | GB | Wigan | Vodafone | 84.68.202.202 |

Все 10 портов работают — реальные мобильные операторы.

### Статус ISP прокси (isp.decodo.com)
- Отдельный аккаунт с конкретными IT/DE/US IP
- Credentials пока не получены — нужен скриншот Authentication вкладки того аккаунта
- IP были: 🇮🇹 216.175.213.1, 82.21.9.29, 82.22.169.147 | 🇩🇪 69.167.159.147, 92.244.188.147 | 🇺🇸 9.142.93.116, 138.226.125.30


---

## [16:10-18:10 UTC] Producer Copilot — вкладка 🧬 Генерация аккаунтов (полный цикл)

### Задача
Создать первую вкладку "🧬 Генерация" в Producer Copilot для генерации профилей Instagram/TikTok/YouTube по брифам Airtable.

### Реализация (полная)

**Бэкенд (server.js):**
- `POST /api/accounts/generate` — принимает `{projectId, platform, type, count, prompt, mainAccount}`, читает бриф через `fetchProjects()`, генерирует профили через `callClaude()` (модель `gemini-2.5-flash` — claude-sonnet-4-6 баланс исчерпан), возвращает `{accounts: [{username, displayName, bio, avatarPrompt, platform}], projectName, sessionId}`
- `POST /api/accounts/generate-avatar` — принимает `{prompt}`, вызывает LaoZhang `/v1/images/generations` с `gpt-image-1-mini` (БЕЗ `response_format` — модель не поддерживает), сохраняет в `public/avatars/`, возвращает `{url: '/avatars/...'}`

**История и расходы (server.js):**
- `GET/DELETE /api/accounts/history` — история генераций (файл `data/accounts_history.json`)
- `GET /api/accounts/history/:sessionId` — конкретная сессия
- `GET /api/accounts/history/:sessionId/download` — скачать JSON
- `GET /api/accounts/spending` — расходы по дням (файл `data/accounts_spending.jsonl`)

**Фронтенд (index.html):**
- Вкладка `#tab-accgen` добавлена первой в навигации
- Суб-табы внутри: 🧬 Генерация | 📋 История | 💰 Расходы
- Форма: клиент (Airtable dropdown, 37 проектов), платформа, тип, кол-во, главный аккаунт, промпт
- Карточки аккаунтов в grid 1/2/3 cols
- Кнопки: скачать JSON, копировать

### Баги и фиксы
1. `initAccgenProjects()` делал `.map()` на объекте — фикс: `const data = await resp.json(); const projects = Array.isArray(data) ? data : (data.projects || [])`
2. `gpt-image-1-mini` не принимает `response_format: 'b64_json'` — убрали параметр
3. Генерация через `claude-sonnet-4-6` упала (баланс) — переключились на `gemini-2.5-flash`
4. `getPlatformIcon` не определена в истории — переименовали в `accGetPlatformIcon`

### Тестирование
- Прямой HTTP тест: генерация 2 аккаунтов за 11-15 сек ✅
- Аватарка gpt-image-1-mini за 33-45 сек ✅
- 37 клиентов из Airtable загружаются ✅
- История и расходы записываются корректно ✅
- PM2 #2 (producer) — online, порт 3850

### Стоимости генерации (оценочно)
- Профиль аккаунта (gemini-2.5-flash): ~$0.003 за аккаунт
- Аватарка (gpt-image-1-mini): ~$0.02 за изображение

### Важно: LaoZhang claude-sonnet-4-6 исчерпан
**Баланс LaoZhang для claude-sonnet-4-6 закончился (~18:00 UTC 04.03)**
- Генерация аккаунтов переключена на `gemini-2.5-flash` (работает)
- Другие функции producer (карусели, видео) могут падать если используют claude
- Нужно пополнить баланс на laozhang.ai (аккаунт связан с командой)

### Файлы
- HTML: `/root/.openclaw/workspace-genri/producer-copilot/public/index.html`
- Server: `/root/.openclaw/workspace-genri/producer-copilot/server.js`
- История: `data/accounts_history.json`
- Расходы: `data/accounts_spending.jsonl`
- Аватарки: `public/avatars/avatar_{ts}_{rand}.jpg`

---

## [18:14-18:20 UTC] Puppeteer скриншоты — урок

### Проблема
Отправлял скриншоты не глядя — оказывалось что на скрине совсем другая страница (список проектов, а не новая вкладка).

### Урок (важно!)
**ВСЕГДА перед отправкой скриншота:**
1. Запустить `image` тул и посмотреть что на скрине
2. Убедиться что видно именно то что нужно
3. Только потом отправлять пользователю

### Правильный флоу для Producer Copilot
- `#projectView` виден только когда открыт конкретный проект из `#projectsView`
- Чтобы добраться до 🧬 Генерация: Puppeteer должен кликнуть на карточку проекта → дождаться `#projectView` → тогда кликать на `tab-accgen`
- Просто `setTab('accgen')` не работает если `#projectView` скрыт через `hidden`

### Структура Producer Copilot (важно для будущей работы)
```
sidebar (switchSection):
  - Поиск трендов → section-trends
  - Уникализация → section-carousel
  - Генератор → section-generator
  - Тексты → section-texts
  - Видео → section-video
  - Расходы → section-spending
  - Настройки → section-settings

section-trends:
  - #projectsView (grid карточек проектов)  ← виден по умолчанию
  - #projectView (детальный просмотр)       ← виден после клика на проект
    - tabs: setTab('accgen' | 'videos' | 'accounts' | 'liked')
    - 🧬 Генерация → content-accgen
      - subtabs: accSwitchTab('generate' | 'history' | 'spending')
    - 📹 Видео → content-videos (tinder-style)
    - 👤 Аккаунты → content-accounts
    - ❤️ Понравилось → content-liked
```

---

## [18:20-18:27 UTC] Producer Copilot — 🧬 Генерация вынесена в сайдбар

### Изменение
Вкладка "🧬 Генерация аккаунтов" перенесена из внутренних табов `#projectView` в отдельный раздел сайдбара — первым пунктом, выше "Поиск трендов".

### Что сделано
- Добавлена кнопка `nav-accgen` в сайдбар (первой в списке)
- Создана секция `section-accgen` с суб-табами (Генерация / История / Расходы)
- `switchSection()` расширен: добавлен 'accgen', при переходе вызывает `initAccgenProjects()`
- Старый `content-accgen` внутри `#projectView` — заменён заглушкой (пустой div)
- Кнопка `tab-accgen` в хедере `#projectView` — удалена
- `setTab()` обновлён: убран 'accgen' из списка, убрана инициализация проектов

### Структура сайдбара (финальная)
```
1. 🧬 Генерация      → section-accgen  (НОВОЕ, первое)
2. 📹 Поиск трендов  → section-trends
3. 🎠 Уникализация   → section-carousel
4. 🎨 Генератор      → section-generator
5. ✍️ Тексты         → section-texts
6. 🎬 Видео          → section-video
7. 💰 Расходы        → section-spending
8. ⚙️ Настройки      → section-settings
```

### Урок о скриншотах (закреплено)
Перед отправкой любого скриншота — всегда проверять через `image` тул. Не отправлять вслепую.

---

## [18:27-18:35 UTC] Producer Copilot — Расходы переделаны под общий стиль

### Задача
Переделать таблицу расходов в разделе 🧬 Генерация под тот же формат что в `section-spending` (общие расходы API).

### Реализация
- **Карточки модулей** (3 шт): Профили (gemini-2.5-flash) / Аватарки (gpt-image-1-mini) / Всего ($)
- **Таблица** с заголовком "История операций по дням" + badge с итоговой суммой
- Колонки: Дата / Запусков / Аккаунтов / Аватарок / Стоимость
- Итоговая строка в footer таблицы
- Стиль: `bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden` — идентично другим разделам

### HTML элементы (обновлены)
- `acc-spending-cards` — grid карточек (было `acc-spending-summary`)
- `acc-spending-table-body` — tbody таблицы (было `acc-spending-table`)
- `acc-spending-total-badge` — badge с итогом в заголовке таблицы

### Функция `loadAccSpending()` полностью переписана
- Animate-pulse скелетон при загрузке
- Карточки с icon/label/sub/val
- Таблица через `.map()` + footer строка
- Обработка ошибок через catch

---

## [19:43-19:44 UTC] Producer Copilot — История переделана под стиль карусели

### Задача
Пользователь показал скриншот истории аккаунтов — старый формат (большие карточки с именами аккаунтов) не подходит.

### Реализация
История переделана под тот же стиль что в `loadCarHistory()`:
- Горизонтальные строки `flex items-center gap-4`
- Слева: аватар первого аккаунта (если есть) или эмодзи-заглушка 🧬 в `rounded-xl`
- По центру: название проекта (truncate) + дата · платформа · тип · кол-во аккаунтов + промпт
- Справа: кнопки `bg-blue-50/green-50/red-50` — «👁 Открыть», «⬇️ JSON», «🗑»
- Пустое состояние: `text-center py-12` с эмодзи 📭

### Шаблон карусели (референс для будущих историй)
```js
`<div class="bg-white rounded-xl border border-gray-100 shadow-sm p-4 flex items-center gap-4">
  <img/div preview>
  <div class="flex-1 min-w-0">title + meta</div>
  <div class="flex gap-2 flex-shrink-0">buttons</div>
</div>`
```

---

## [19:44-19:47 UTC] Producer Copilot — убрано поле «Главный аккаунт»

### Изменение
Поле «Главный аккаунт бренда» убрано из формы генерации аккаунтов.
Вместо него — аккаунты-референсы берутся автоматически из поля **«Конкуренты»** (`competitors`) в Airtable-брифе.

### Что изменено
**HTML:** удалён `<input id="accgen-main-account">`
**JS:** убраны `const mainAccount = ...` и `mainAccount` из тела запроса
**server.js:** убран `mainAccount` из деструктуризации `req.body`; в `briefContext` добавлена строка:
```js
project.competitors && `Аккаунты-референсы для стиля: ${project.competitors}`,
```

### Структура брифа Airtable (поля объекта `project`)
- `id` — Airtable record ID
- `name` — Name
- `description` — Описание проекта
- `audience` — Целевая аудитория
- `keywords` — Ключевые фразы (массив)
- `competitors` — Конкуренты (строка, содержит аккаунты-референсы)
- `language` — Язык
- `geo` — География
- `hashtags` — хэштеги

---

## [19:47-19:48 UTC] Producer Copilot — аккаунты из поля соцсетей Airtable

### Изменение
Вместо `competitors` используются реальные поля соцсетей клиента из Airtable.

### Новые поля брифа (добавлены в `fetchProjects()`)
- `accountNames` ← `f['Название аккаунтов']` — реальные аккаунты бренда в соцсетях
- `profileBio` ← `f['Шапка профиля']` — текст шапки профиля бренда

### В `briefContext` (для генерации)
```js
project.accountNames && `Аккаунты бренда в соцсетях: ${project.accountNames}`,
project.profileBio   && `Шапка профиля бренда: ${project.profileBio}`,
```

### Все поля Airtable таблицы бриф (полный список)
Name, Описание проекта, Целевая аудитория, Ключевые фразы, хэштеги, Конкуренты,
Материалы / заставки для соцсетей, Шапка профиля, Язык, География,
Воронка, **Название аккаунтов**, УТП, Прокси 1

---

## [19:48-19:57 UTC] Producer Copilot — поле «Соц. сети клиента»

### Финальное исправление
Поле для аккаунтов клиента: **`f['Соц. сети клиента']`** → `project.socialAccounts`

В `briefContext`:
```js
project.socialAccounts && `Соц. сети клиента: ${project.socialAccounts}`,
```

### Полный список полей Airtable (все 17 полей)
Name, Описание проекта, Целевая аудитория, Ключевые фразы, хэштеги, Конкуренты,
Материалы / заставки для соцсетей, Шапка профиля, Язык, География, Воронка,
Название аккаунтов, УТП, Прокси 1, **Соц. сети клиента**, Стоп‑слова, Контент клиента

---

## [19:57-20:01 UTC] Producer Copilot — валидация лимитов соцсетей

### Задача
Пользователь заметил что bio в карточке может превышать лимиты платформы.

### Реализация (два уровня защиты)

**1. Промпт усилен:**
```
ПРАВИЛА ПЛАТФОРМЫ (обязательны): Instagram: username СТРОГО до 30 символов
(только буквы, цифры, точки, подчёркивания — БЕЗ пробелов),
bio СТРОГО до 150 символов ВКЛЮЧАЯ переносы строк (каждый \n = 1 символ),
максимум 5 строк. ОБЯЗАТЕЛЬНО проверь длину bio перед ответом.
```

**2. Серверная валидация после парсинга JSON:**
- `username` — убираем `@`, нелегальные символы, обрезаем до `limits.userLen`
- `bio` — обрезаем до `limits.bioLen` символов; если обрезка попадает на середину строки — откатываемся к последнему `\n`; ограничиваем `limits.bioLines` строк

**Лимиты по платформам (`PLATFORM_LIMITS`):**
```js
instagram: { userLen: 30, bioLen: 150, bioLines: 5 }
tiktok:    { userLen: 24, bioLen: 80,  bioLines: 4 }
youtube:   { userLen: 100, bioLen: 150, bioLines: 5 }
```

---

## [20:01-20:03 UTC] Producer Copilot — перегенерация bio вместо обрезки

### Изменение
Простая обрезка bio заменена на перегенерацию через отдельный API-запрос.

### Логика валидации (server.js, после парсинга accounts)
1. Проверяем каждый аккаунт: `bio.length > limits.bioLen` || `lines > limits.bioLines`
2. Если превышение → отдельный запрос `callClaude(fixPrompt, 'gemini-2.5-flash')`:
   - Промпт: «Перепиши bio СТРОГО в N символов, сохрани суть и стиль»
3. Если перегенерированный bio тоже не влезает → берём строки сверху пока влезают
4. username всё равно очищается (убираем @, нелегальные символы, slice до userLen)

### Важно
- `accounts` стал `await Promise.all(accounts.map(async a => {...}))` — параллельные запросы
- Крайний случай обрезки остался только если оба запроса не уложились в лимит

---

## [20:03-20:20 UTC] Producer Copilot — кнопка «Доработать» на карточке

### Функционал
Кнопка ✏️ доработать в футере каждой карточки → модалка с textarea → перегенерирует только эту карточку, остальные не трогаются.

### Бэкенд: `POST /api/accounts/regenerate-one`
- Принимает: `{account, platform, projectId, refinement}`
- Промпт: «Улучши профиль @username с учётом пожелания: {refinement}»
- Валидирует лимиты (перегенерирует bio если превышает)
- Возвращает: `{account: {...updated}}`

### Фронтенд
- Переменные: `let _regenIdx = null` — индекс редактируемой карточки
- `openRegenModal(idx)` — открывает модалку, запоминает индекс
- `closeRegenModal()` — скрывает модалку
- `submitRegen()` — POST запрос → обновляет `generatedAccounts[_regenIdx]` → `renderAccountCards()` → восстанавливает аватарки через forEach
- Модалка HTML: `id="regen-modal"` с `id="regen-prompt"` textarea + `id="regen-submit-btn"`

### Важный трюк для Puppeteer тестов
`let generatedAccounts` не доступна через `window.generatedAccounts` — нужно:
```js
generatedAccounts.length = 0;
accounts.forEach(a => generatedAccounts.push(a));
```
Это мутирует существующий массив без пересоздания ссылки.

---

## [20:20-20:22 UTC] Producer Copilot — лайтбокс аватарок

### Функционал
Клик на аватарку → открывается на весь экран (тёмный фон). Закрыть: клик на фон или ×.

### Реализация
- `updateCardAvatar()` — добавлен `cursor-zoom-in` и `onclick="openAvatarLightbox('${url}')"`
- `openAvatarLightbox(url)` / `closeAvatarLightbox()` — показывают/скрывают `#avatar-lightbox`
- HTML: `id="avatar-lightbox"` — `fixed inset-0 z-50 bg-black/80 backdrop-blur-sm`, клик на фон закрывает; `id="avatar-lightbox-img"` — `max-w-[90vw] max-h-[90vh]`, клик на картинку не закрывает (`stopPropagation`)

---

## [20:22-20:24 UTC] Producer Copilot — перегенерация аватарки при доработке

### Проблема
После «Доработать» аватарка оставалась старой (avatarPrompt мог измениться).

### Фикс в `submitRegen()`
1. После получения `data.account` — сохраняем старую аватарку временно: `{ ...acc, ...data.account, avatarUrl: acc.avatarUrl }`
2. Восстанавливаем аватарки ДРУГИХ карточек (не _regenIdx)
3. Закрываем модалку
4. Фоново запускаем `POST /api/accounts/generate-avatar` с новым `avatarPrompt`
5. Когда готово → `updateCardAvatar(regenIdx, avatarData.url)` + сохраняем в `generatedAccounts[regenIdx].avatarUrl`

Пользователь видит: профиль обновился сразу, аватарка появляется через ~30-45 сек в фоне.

---

## [20:24-20:46 UTC] Producer Copilot — сессии и запоминание раздела

### Проблема 1: Авторизация сбрасывалась
**Причина:** MemoryStore (по умолчанию) — при рестарте PM2 все сессии теряются.
**Фикс:** Установлен `session-file-store`, сессии хранятся в `data/sessions/`

```js
const FileStore = require('session-file-store')(session);
store: new FileStore({ path: './data/sessions', ttl: 7*24*3600, retries: 0, logFn: ()=>{} }),
```

### Проблема 2: При обновлении кидало на «Поиск трендов»
**Фикс:** `switchSection()` сохраняет раздел в `localStorage('producer_section')`.
`DOMContentLoaded` читает и восстанавливает последний раздел.

```js
try { localStorage.setItem('producer_section', name); } catch(e) {}
window.addEventListener('DOMContentLoaded', () => {
  const last = localStorage.getItem('producer_section');
  if (last && document.getElementById('section-' + last)) switchSection(last);
});
```

---

## [20:46-20:52 UTC] Validator/Delivery — Аналитика аккаунтов (обсуждение)

### Задача (поставлена, не реализована)
Добавить в validator (delivery.contenthunter.ru) блок Аналитика с парсером демографии аккаунтов:
- **Метрики:** гео, пол, возраст
- **Режим:** ежедневный запуск (cron)
- **Охват:** все аккаунты в системе

### Ключевой вопрос (не решён)
Источник данных для демографии — три варианта:
1. **ADB** — Professional Dashboard Instagram → UI парсинг (~1-2 мин/акк × 173 = 3-6 часов)
2. **Instagram Graph API** — `/me/insights?metric=audience_gender_age,audience_city` — нужны access_token для каждого бизнес-аккаунта
3. **Сторонние сервисы** — HypeAuditor, Analisa.io (платные)

### Контекст
- `analytics_collector.py` (autowarm/) — существующий скрипт сбора постов, НЕ демографии
- Пользователь говорил "мы уже делали это" — источник не установлен, ждём уточнения
- Validator структура: Vue frontend, FastAPI backend, `/accounts` роут для аккаунтов проекта

### Следующий шаг
Ждём ответа пользователя: какой метод используем для получения гео/пол/возраст

---

## [20:52-21:07 UTC] Validator — Страница Аналитики аудитории

### Задача
Добавить раздел Аналитика в validator.contenthunter.ru — парсинг гео/пол/возраст аккаунтов через ADB + Professional Dashboard Instagram/TikTok/YouTube.

### Открытие
`analytics_collector.py` в autowarm/ уже содержал функции:
- `collect_audience_instagram()` — навигация в Professional Dashboard, скриншоты
- `collect_audience_tiktok()` — TikTok Creator Analytics
- `collect_audience_youtube()` — YouTube Studio Analytics
- `analyze_audience_screenshot()` — AI-анализ скриншотов
- `save_audience_snapshot()` — сохранение в `account_audience_snapshots`
- Коллектор запускается **ежедневно в 00:00 UTC** через scheduler.js

Таблица `account_audience_snapshots` уже существует: **355 записей** (Instagram: 74, YouTube: 281). НО geo/age/gender пустые — AI-анализ не работал из-за устаревшего Anthropic OAuth token.

### Фикс AI-анализа (analytics_collector.py)
Функция `analyze_audience_screenshot()` переключена с Anthropic API → **LaoZhang OpenAI-compatible API**:
- URL: `https://api.laozhang.ai/v1/chat/completions`
- Model: `gemini-2.5-flash`
- Формат: OpenAI Chat Completions с image_url (data:image/png;base64,...)
- Парсинг: `resp.json()["choices"][0]["message"]["content"]`
- Ключ: `LAOZHANG_API_KEY` из .env (уже был там)
- Добавлена константа `LAOZHANG_API_KEY = os.getenv('LAOZHANG_API_KEY', '')` в топ файла

### Созданные файлы

**Backend:**
- `/root/.openclaw/workspace-genri/validator/backend/src/routers/analytics.py`
  - `GET /api/analytics/audience/latest` — последний снимок по каждому аккаунту + агрегированный summary (top_geo, gender_split, age_split)
  - `GET /api/analytics/audience` — история с фильтрами (platform, date_from, date_to)
  - `GET /api/analytics/audience/projects` — список проектов с данными
- Добавлен в `main.py`

**Frontend:**
- `/root/.openclaw/workspace-genri/validator/frontend/src/pages/manager/AnalyticsPage.vue`
  - Фильтры: проект + платформа
  - Карточки: итого аккаунтов/подписчиков/пол
  - 3 блока: 🌍 Топ гео | 👥 Пол | 📊 Возраст (с прогресс-барами)
  - Таблица аккаунтов с ✅/— статусом заполненности данных
- Роут: `/analytics` (roles: manager, admin)
- Сайдбар: пункт 📊 Аналитика в разделе Менеджера

### Статус данных
- 355 записей с `followers`, но `geo/age/gender` пустые
- Первые реальные данные будут после 00:00 UTC 05.03.2026

### DB схема (account_audience_snapshots)
```sql
account TEXT, platform TEXT, project TEXT, snapshot_date DATE,
geo JSONB, age_groups JSONB, gender JSONB,
followers INT, reach_7d INT, impressions_7d INT,
audience_raw TEXT, collected_at TIMESTAMP,
UNIQUE(account, snapshot_date)
```

---

## [21:09-21:15 UTC] Profile Inspector — Распознавание Instagram-профиля

### Задача
Распознавать Instagram-профиль через ADB: имя, ник, аватар, описание, ссылка, 3 последних поста с текстом. Оценивать аккаунт 1-10.

### Реализация
Файл: `/root/.openclaw/workspace-genri/autowarm/profile_inspector.py`

**Шаги:**
1. `open_profile(serial, port, account)` — ADB deep link `https://www.instagram.com/{account}/`
2. 2 скриншота шапки (с прокруткой для полного bio)
3. `extract_profile_data()` — LaoZhang vision → {username, display_name, bio, link, followers, following, posts_count, avatar_description}
4. Прокрутка до сетки постов, тап по 3 постам
5. `extract_post_data()` — {type, caption, hashtags, likes, views}
6. `score_account(profile, posts, brief)` — оценка 1-10 по 7 критериям (username, display_name, bio, visual, content, engagement, compliance)

**API в autowarm/server.js:**
- `POST /api/profile/inspect` — async (возвращает jobId)
- `GET /api/profile/inspect/:jobId` — статус/результат
- `POST /api/profile/inspect/sync` — синхронный, ждёт до 5 мин

**CLI:**
```bash
python3 profile_inspector.py -a @username -s SERIAL -p PORT [--brief "..."] [--json]
```

### Статус
- ✅ Скрипт написан, импорт проверен
- ✅ API добавлен в autowarm server.js, autowarm рестартован (PM2 #0)
- Реальное тестирование на устройстве — при следующей задаче

---

## [21:15-21:19 UTC] Profile Inspector — Интеграция с Airtable

### Доработка
Добавлена автоматическая загрузка брифа из Airtable в `profile_inspector.py`.

### Что добавлено
- `fetch_airtable_projects()` — загружает 37 проектов из Airtable (кеш 2 мин)
- `find_project(query)` — fuzzy поиск по названию (точное → substring)
- `build_brief_context(project)` — строит текстовый бриф из полей Airtable
  Поля: name, description, audience, geo, language, utp, profileBio, accountNames, keywords, hashtags, funnel, stopWords, socialAccounts
- Airtable config: `/root/.openclaw/workspace/integrations/airtable/config.json`
- `inspect_profile()` теперь принимает `project_name` и `project_id`
- API эндпоинты принимают `project_name` и `project_id` в теле запроса

### CLI
```bash
python3 profile_inspector.py --list-projects          # список 37 проектов
python3 profile_inspector.py -a @acc -s SERIAL -p PORT --project "Synatra"
python3 profile_inspector.py -a @acc -s SERIAL -p PORT --project-id recXXX
```

### Статус
- ✅ Airtable загружает 37 проектов успешно
- ✅ `--list-projects` работает без обязательных аргументов
- autowarm рестартован (PM2 #0, online)

---

## [21:19-21:23 UTC] Profile Inspector — Автоопределение устройства из factory DB

### Доработка
Скрипт теперь полностью автономен — serial/port не нужны если аккаунт есть в factory DB.

### Добавлено в profile_inspector.py
- `find_device_for_account(account)` — ищет serial+port+project в factory DB по username
  SQL: factory_inst_accounts → pack_accounts → factory_projects → device_numbers → raspberry_port
- `get_any_online_device()` — любое онлайн-устройство (для тестов)
- `inspect_profile()` — если serial/port не переданы, автоматически определяет из factory DB
- Если проект не указан — берёт из factory DB (поле `api_name`)

### Проверено
- `supervpn_26` → устройство RF8YA0W59CB:15058, проект Synatra ✅
- Любое устройство: RFGYA19BT8N:15048 ✅

### Минимальный запуск
```bash
python3 profile_inspector.py -a @supervpn_26
# Автоматически: device=RF8YA0W59CB:15058, project=Synatra, бриф из Airtable
```

### API
```json
POST /api/profile/inspect/sync
{"account": "supervpn_26"}
// serial и port опциональны
```

### Factory DB структура (для автоопределения)
```sql
factory_inst_accounts (username, active, pack_id)
  → pack_accounts (device_num_id, project_id)
    → device_numbers (device_id=serial, raspberry)
      → raspberry_port (adb=port)
    → factory_projects (api_name=project)
```
