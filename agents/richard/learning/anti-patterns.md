# Anti-Patterns — что НЕ работает
_Формат: [дата] Задача | Ошибка | Урок_

---

## Код / JS

[2026-03-02] Producer Copilot — потеря 4 разделов | Генри написал Тексты/Видео/Расходы/Настройки, не закоммитил → PM2 restart → всё потерялось | Любой значимый код сразу `git add . && git commit`. "Деплой без коммита" = код не существует

[2026-03-02] Дублирующиеся JS функции в HTML | `carSwitchSource`, `carLoadGenHistory`, `CAR_PRESETS` определены дважды → `SyntaxError: already been declared` → весь JS отваливался | После добавления новых функций проверять grep на дубли: `grep -n "function имя\|const имя"` — убивает тихие баги

[2026-03-02] Опечатка в имени env-переменной | `LAOZHANG_BASE_URL` вместо `LAOZHANG_URL` в 4 местах img2vid/video-edit → URL становился `undefined/v1/videos` → "Ошибка запроса" | При добавлении нового кода — проверять что все используемые переменные объявлены: `grep -n "const LAOZHANG"` сразу показал бы несоответствие

[2026-03-02] Инициализация DOM в скрытых секциях | `document.getElementById('car-dropZone')` в скрытой section-carousel → null → crash → весь JS блока не выполнялся | Никогда не обращаться к DOM-элементам скрытых секций без null-guard. Особенно опасно в глобальном `<script>`

---

## API / Интеграции

[2026-03-02] LaoZhang quota exhausted | Генерация картинок падала с разными ошибками ("Нет Gemini API ключа", "OAuth not supported") — все от одной причины: нет баланса | Когда несколько разных ошибок генерации одновременно — первым делом проверять API ключ curl-ом, не копаться в коде

[2026-03-02] ElevenLabs бесплатный план | Записали голос Натальи (2:18), загрузить не получилось — кредиты кончились на free tier | Бесплатные API ключи для TTS/клонирования голоса исчерпываются быстро. Для продакшна сразу Starter план ($5/мес)

---

## Архитектура

[2026-03-02] Несколько версий генератора в HTML | section-generator существовал в 2 версиях (от subagent + от Ричарда) с разным HTML но одинаковыми JS именами → конфликт | При восстановлении потерянного кода: сначала grep на `id="section-generator"` — убедиться что секции нет, только потом добавлять

[2026-03-02] res.sendFile с абсолютным path.join | `res.sendFile(path.join(__dirname, 'public', 'monitor.html'))` → NotFound от express.static | Express `sendFile` конфликтует со static middleware при абсолютных путях. Использовать `res.sendFile('relative/path', { root: __dirname })`

---

## FastAPI / роутеры

[2026-03-03] Добавление роутера без проверки prefix | Написал `prefix="/producer"` — все запросы шли на `/producer/...` вместо `/api/producer/...` → 404 | Это уже второй раз: всегда `grep "prefix=" src/routers/*.py` перед написанием нового роутера. Занимает 3 секунды.

## TypeScript / Vue

[2026-03-03] Event handler без каста типа | `@change="e => updateStage(o.id, e.target.value)"` → TS18047: `e.target` possibly null, TS2339: `value` not on EventTarget | В Vue TS event handlers всегда кастить: `(e.target as HTMLSelectElement).value`. Без этого — TS build error.

[2026-03-03] null в typed ref поле | `form.value.deadline = null` когда тип поля `string` → TS2322 | В emptyForm() явно типизировать nullable поля: `null as number|null`, для string-полей использовать `delete payload.field` или `payload.field = undefined as any` при отправке.

## Процесс

[2026-03-02] Диагностика без curl-теста | Потратил время анализируя код перед тем как проверить API ключ | Порядок диагностики внешней интеграции: 1) проверить ключ/доступность curl, 2) проверить логи PM2, 3) читать код

[2026-03-02] Запуск субагента без проверки существующего кода | Субагент восстановил секции, но generator мог уже быть в HTML от предыдущего субагента | Перед делегированием: grep по ключевым id/именам — убедиться что задача ещё актуальна и не сделана наполовину

[2026-03-03] Не фиксировать актуальный пароль в памяти | Пароль admin слетел после миграций, пришлось сбрасывать через psql + искать что было | При создании/смене пароля сразу записывать в memory: кто, какой, куда прописан. Не полагаться на "помню".

---

## Навигация / UX

[2026-03-04] Sidebar redirect без визуальной разницы | nav('whatsapp-tasks') → nav('whatsapp') открывал одну страницу и для Аккаунты и для Задачи → пользователь думал что кнопки сломаны | Если два разных пункта меню ведут в одну секцию — добавляй ВИДИМОЕ различие (скролл к нужному блоку, разные вкладки, show/hide). Иначе UX=broken.

[2026-03-04] authHeaders() в autowarm fetch-запросах | Использовал `{ headers: authHeaders() }` в autowarm — функция существует только в validator. ReferenceError → весь план выкладки падал | Autowarm auth = session-based. НЕТ функции authHeaders(). НЕТ Bearer токенов. Просто `fetch(url)` без headers.

## Схема factory в БД

[2026-03-04] Поиск таблицы не в той схеме | `FROM factory_inst_accounts` → ERROR: relation does not exist. Таблица в схеме `factory.` | При ошибке "relation doesn't exist" сразу: `SELECT schemaname, tablename FROM pg_tables WHERE tablename LIKE '%имя%'`. Две минуты — и найдёшь нужную схему.

[2026-03-04] Старая ссылка после рефакторинга SQL | Заменил JOIN с `factory_projects fp` на `autowarm_project_mapping pm`, но в ORDER BY оставил `fp.project` → ERROR | После замены таблицы в SQL — grep по всему запросу на старые aliases (`fp.`, `dn.` etc.). ORDER BY и WHERE часто забываются.

## Producer Copilot

[2026-03-05] LaoZhang баланс → смена модели без теста | Переключили с claude на gemini-2.5-flash, но не проверили все endpoint'ы — некоторые продолжали падать | После смены дефолтной модели: протестировать ВСЕ AI-вызовы через curl, не только основной генератор.

[2026-03-05] Puppeteer вместо простого curl | Для проверки API-ответа запускали headless браузер с авторизацией когда достаточно curl с cookie | Puppeteer — только для визуальных проверок и кликов. Для API тестов: curl. Puppeteer стоит ~3-5 мин на запуск vs 2 сек curl.

[2026-03-05] Скриншот не той страницы | Puppeteer открыл список проектов, а не вкладку Генерация — отправил как "результат" | Перед отправкой скриншота: `image` тул для анализа содержимого. Не отправлять вслепую.

## Процесс разработки

[2026-03-04] Несоответствие ТЗ и адресата | ТЗ от Джеффа написано "Кому: Генри" но отправлено Ричарду | Перед стартом задачи уточнять: кто реально берёт работу. Не начинать разработку пока не подтверждён исполнитель.

---

## Субагенты

[2026-03-08] Субагент редактирует не тот файл | Субагент добавлял вкладку в monitor.html — случайно задел index.html → 3 синтаксических ошибки → офис не открывался | При делегировании субагенту: ЯВНО указывать файлы которые трогать НЕЛЬЗЯ. `"Работаешь ТОЛЬКО с monitor.html, index.html не трогай"`.

[2026-03-08] Откат офиса без понимания какая "первая версия" | Пользователь сказал "откатить до первой версии" — откатили на git commit 9291385 (400 строк пикселей), это оказалось не то | Прежде чем откатывать: уточнить "первая версия" = что конкретно? Описание или имя файла. Откат по backup-файлу надёжнее git.

## Regex / Парсинг

[2026-03-08] Правка в комментарии вместо кода | TikTok resource-id `x5b` → `yrd`: заменили в строке-комментарии, не в regex → снова не работало, второй проход | После `sed -i` замены — проверить `grep` что новое значение стоит именно в нужном месте (код, не комментарий).

## API / Внешние сервисы

[2026-03-08] Устаревшие Groq vision модели | `llama-3.2-11b-vision-preview` и `llama-3.2-90b-vision-preview` сняты с продакшна → ошибка при вызове | При интеграции vision API: проверять актуальный список моделей через `curl api/models`. Не копировать имена из старой документации. Актуальная Groq vision: `meta-llama/llama-4-scout-17b-16e-instruct`.

[2026-03-08] Архивация аудитории — не та точка зрения | analytics_collector.py шёл в Professional Dashboard правильно, но с "чужого" аккаунта — Insights недоступен для чужих профилей | Перед реализацией парсинга статистики: убедиться что устройство залогинено В ЦЕЛЕВОЙ аккаунт, а не в прогревочный. Иначе Insights/Аудитория просто не откроется.

## Canvas / Офис

[2026-03-08] `if(!AL[dd.id]) continue` скрывает агентов молча | Агенты без pre-assignment просто не рендерились — пользователь не знал что они есть | Для overflow-агентов: лучше рендерить на резервном месте чем скрывать. Если скрываем — логировать в консоль сколько агентов пропущено.

---

## ADB / Инфраструктура

[2026-03-09] ADB_HOST захардкожен неправильно | analytics_collector.py имел `ADB_HOST = '82.115.54.26'` — но это хост только Pi #9. Остальные 9 малинок на `147.45.251.85` → ADB команды молча не находили устройства | Никогда не хардкодить ADB_HOST. Загружать `PORT_TO_HOST` из `factory.raspberry_port` при старте. Один неправильный хост = все устройства недоступны.

[2026-03-09] Убрал factory. префиксы из SQL без проверки таблиц | `/api/devices` — убрал `factory.` из `device_state`, `device_numbers`, но эти таблицы НЕ синхронизированы в локальную БД → запрос падал с другой ошибкой | Перед убиранием schema prefix: `SELECT tablename FROM pg_tables WHERE tablename='...'` — убедиться что таблица вообще существует в локальной БД.

## Субагент / Таймаут

[2026-03-09] Субагент 300s не хватило для задачи средней сложности | e32c3b6f добавлял расходы в /api/agents + popup — таймаут 300s, задача незавершена | Для задач с чтением файлов (session JSONL) + два файла изменений — ставить 600s+. Либо дробить: субагент 1 = server.js, субагент 2 = HTML.

## Vue / TS

[2026-03-09] npm run build не запускали после правки .vue файла | CrmContractors.vue — добавил опцию 'ai', но не пересобрал. Изменения не попали в /var/www/validator/ | После ЛЮБОГО изменения .vue файла: `npm run build` + проверить что dist обновился. Vue не hot-reload в prodакшне.

## ❌ Instagram: "Новые подписчики" для сбора аудитории (2026-03-10)
**Никогда не использовать маршрут "Профессиональная панель → Новые подписчики" для сбора гео/возраст/пол.**
Там показана статистика ТОЛЬКО новых подписчиков за период — не вся аудитория.
Данные будут неверными и нерепрезентативными.
Правильный маршрут: через **"Просмотры"**.

## Аналитика / источники данных

[2026-03-11] account_daily_delta для просмотров | Показывала 733k вместо реальных 174k | Первый снимок без предыдущего = весь накопленный total записывается как дельта. Нерегулярный сбор (раз в 3 дня) умножает ошибку. НИКОГДА не использовать account_daily_delta для просмотров без проверки baseline.

[2026-03-11] SUM(views_count) за период | Накопительный total суммируется N раз — в разы превышает реальные просмотры | views_count = накопительное значение с момента публикации. Суммировать за период нельзя. Только sum_views (дневная дельта) или MAX-MIN per video.

[2026-03-11] Суммирование snapshot total без baseline | delta = sum_today - 0 (нет вчерашнего снимка) = весь исторический объём | При отсутствии предыдущего снимка — не записывать дельту вовсе (NULL/skip), а не 0. Иначе первая точка = весь накопленный объём.

---

## SQL в FastAPI (asyncpg)

[2026-03-11] base_join шаблон с LEFT JOIN LATERAL после | Вставил `{base_join}` в подзапрос, после добавил LEFT JOIN LATERAL → syntax error | asyncpg/SQLAlchemy: нельзя добавить JOIN после текстового шаблона который заканчивается на WHERE. Для запросов с LATERAL — писать полный FROM..JOIN вручную, не использовать шаблоны.

[2026-03-11] port 8000 уже занят после pm2 restart | validator-api падал с "address already in use" в цикле | После pm2 restart старый процесс иногда не освобождает порт. `fuser -k 8000/tcp` перед restart или использовать `pm2 reload` вместо `restart`.

---

## TikTok / ADB навигация

[2026-03-10] Deep link TikTok | am start -d tiktok://... открывает в режиме гостя | Deep link обходит авторизацию → профиль без "Меню профиля". Только monkey + tap Profile tab. Проверено многократно.

[2026-03-10] sleep(3) для TikTok creator mode | Профиль не успевает переключиться в creator mode | Минимум sleep(5) после tap Profile. Лучше: retry loop, poll dump_ui на наличие "Меню профиля" до 15 секунд.

[2026-03-10] dump_ui в TikTok Studio/Analytics | Аналитика = WebView, dump_ui всегда пустой | Никогда не проверять содержимое аналитики через dump_ui. Только скриншот + AI парсинг (Groq).

---

## Instagram навигация

[2026-03-10] "Новые подписчики" для audience stats | Данные только по новым подписчикам, не по всей аудитории | Маршрут через "Новые подписчики" показывает статистику ТОЛЬКО по вновь подписавшимся. Для полной аудитории — маршрут через "Просмотры" → inline карусель Аудитория.

[2026-03-09] Предположение о залогиненном аккаунте | На 70%+ устройств залогинены варм-аккаунты, не таргет-аккаунты | Перед сбором данных — всегда проверять кто залогинен (`instagram_get_current_account()`). Audience Insights доступны только владельцу бизнес-аккаунта.

---

## Auth / Telegram hash verification

[2026-03-12] Добавление extra-поля в Telegram auth payload | `_redirect` добавлен в tgData ДО вызова verifyTelegramAuth → hash всегда fails | verifyTelegramAuth считает HMAC по Object.keys(data без hash). Любое лишнее поле = другой checkStr = неверный hash. Всегда извлекать кастомные поля через деструктуризацию ДО вызова verify: `const { _myField, ...tgData } = req.body`.

[2026-03-12] in-memory crossTokens при нестабильном PM2 | pendingCrossTokens.Map() очищается при каждом рестарте → пользователь получает "invalid_token" | Для cross-auth токенов TTL=60с: если PM2 рестартует в этот промежуток — все pending токены теряются. Решение: увеличить TTL или хранить в файле/Redis при нестабильности.

---

## PM2 / Инфраструктура

[2026-03-12] pm2 start без pm2 save | Запустили producer вручную, забыли `pm2 save` → после рестарта демона процесс исчез | `pm2 save` ОБЯЗАТЕЛЕН после любого ручного `pm2 start`. Без этого дамп устаревает и resurrect ничего не восстанавливает.

[2026-03-12] pm2 restart несуществующего процесса | `pm2 restart office` когда демон перезапустился = "Process not found" | После рестарта PM2 демона (или `pm2 kill`) — всегда `pm2 resurrect` ПЕРВЫМ шагом, потом restart. resurrect без имени процесса восстанавливает всё из дампа.

---

## Работа с чужим кодом (исправление без понимания)

[2026-03-12] Правка в template literal вместо переменной | encodeURIComponent внутри backtick-строки с экранированными кавычками не выполнялся → URL не кодировался | При сложных template literals: вычислять выражение ДО res.send() в отдельную переменную. `const url = ...; res.send(\`href="\${url}"\`)` — всегда надёжнее чем вложенные escape-последовательности.

[2026-03-12] Исправление не той страницы (index.html вместо monitor.html) | Пользователь спросил про токены в "офис монитор" → сделал панель в index.html | При задаче "добавить в офис": уточнить `monitor.html` (дашборд с таблицами) vs `index.html` (canvas с 2D офисом). Это разные страницы одного сервиса.

---

## JS / Scope

[2026-03-13] let в Temporal Dead Zone при восстановлении вкладки | `let tokensCustomFrom` объявлен на строке 964, IIFE вызывает loadTokens() на строке 367 → ReferenceError → catch → "Ошибка загрузки" | Для переменных используемых в IIFE при загрузке страницы: объявлять в самом начале скрипта рядом с остальными globals. Симптом: страница падает с catch сразу при загрузке нужной вкладки.

## Маппинги / Данные

[2026-03-13] Неполный AGENT_API_KEY — забыл -v2 агентов | Прописал editorial-v1, но не editorial-v2 (pisaka-v2, vseznayka-v2, akaketo-v2, apochemu-v2) → они падали в Default | При заполнении agent→key маппинга: открывать org-structure.json и проходить ВСЕ entries включая teams. Не копировать из памяти — там всегда забываешь варианты.

[2026-03-13] apiKey обогащение из 24h кэша для week/month периода | token-stats кэш = 24ч окно, token-usage показывает неделю. Агенты неактивные сегодня → apiKey=undefined → попадают в Default | Обогащение данных через связанный эндпоинт работает только если у них одинаковое временное окно. Когда окна разные — включать нужные поля в основной эндпоинт (token-usage), не в отдельный кэш.

## Процесс

[2026-03-11] Не консультироваться с профильным агентом | Потратил несколько итераций на диагностику SQL перед тем как спросить Фёдора | У каждой задачи есть профильный агент. Fyodor = аналитика и SQL. Спрашивать его первым — он сэкономил бы 30 минут.

[2026-03-10] Cron для долгих задач | Сбор аудитории Instagram (2+ часа) убивается cron таймаутом 7200s | Долгие задачи (>30 мин): только exec background=true + nohup, не cron. Cron — только для коротких (<5 мин) или запуска скрипта, который сам себя демонизирует.

---

## OpenClaw конфиг

[2026-03-14] Конфиг затирается через pipe | `ssh ... | python3 ... > config.json` — если python3 падает, файл 0 байт | НИКОГДА не писать конфиг через pipe напрямую. Паттерн: скачать → изменить локально → scp. Проверять `wc -c` перед перезапуском gateway.

[2026-03-14] auth-profiles.json формат — tokenRef vs token | Использовал tokenRef ссылку на secrets.json которого нет на новом сервере → No API key | На новом сервере без secrets.json — только прямой `"token": "sk-ant-..."` в auth-profiles.json. tokenRef работает только когда настроен filemain provider.

[2026-03-14] openclaw.json схема — агрессивная валидация | Угадывал схему итеративно (6+ попыток) — agents.list=array, plugins=object, groupPolicy без "mention" | Читать конфиг-схему из документации или копировать рабочий конфиг как шаблон. Не угадывать поля.

[2026-03-14] Bindings забыты при переносе агентов | Агенты работали, но Эдвард отвечал как Джони | При переносе агентов: скопировать AGENT_API_KEY маппинг И bindings из старого openclaw.json. Без bindings routing сломан.

## Агрессивные удаления

[2026-03-14] Удалил воркспейсы нужных агентов | При попытке убрать whitelist удалил слишком много воркспейсов | Перед `rm -rf workspace-X` — убедиться что агент точно не нужен на этом сервере. Сначала список → проверить → потом удалять.

---

## OpenClaw / Gateway

[2026-03-16] Рестарт gateway не перезапускает бэкенд | Починил support_chat.py, но бэкенд uvicorn не подхватил — старый процесс висел | После изменения Python роутера: `kill -9 PID` + запустить заново из правильного cwd (`/workspace/validator/backend`). Gateway restart ≠ uvicorn restart.

[2026-03-16] Запуск uvicorn из неправильного cwd | `cd /workspace-richard/validator && uvicorn src.main:app` → ModuleNotFoundError | uvicorn должен запускаться из `backend/` папки, не из корня проекта. Проверить реальный cwd работающего процесса: `ls -la /proc/PID/cwd`.

[2026-03-16] duplicate import в App.vue | Добавил SupportChat импорт дважды → TS2300 Duplicate identifier | После добавления импорта: `grep -n "SupportChat" App.vue` перед build. Дубликат ломает сборку.

[2026-03-16] shared/RULES.md отсутствует у новых агентов | Эдвард молчал из-за "Embedded agent failed" — SOUL.md требовал читать RULES.md которого нет | При создании нового агента: всегда копировать shared/RULES.md из шаблона. Без него агент падает при первом сообщении.

[2026-03-16] Конфигурировать агента не в том воркспейсе | Роутер support_chat.py написан в workspace-richard/validator, а реально работает workspace-genri/validator — это один репо | workspace-richard и workspace-genri шарят одни и те же файлы через симлинки или один путь. Проверить ls -la перед правкой.

[2026-03-16] marked.parse() без tailwind prose | v-html с markdown без CSS — заголовки/списки выглядят как обычный текст | Для рендеринга markdown в Tailwind: добавить класс `prose prose-sm` на контейнер. Без него теги <strong>, <ul>, <h3> не стилизуются.

---

## 2026-03-17

[2026-03-17] onMounted отрабатывает до логина | App.vue проверял auth при монтировании — получал 401, catch глотал ошибку, onboardingStage оставался 5 → онбординг не показывался | Два отдельных try/catch: один для /auth/me, другой для /onboarding/status. При ошибке /auth/me — не падать, а ждать. После логина — перезапускать checkAuth.

[2026-03-17] h-full внутри overflow-y-auto не работает | flex items-center justify-center на дочернем элементе не центрирует если родитель overflow-y-auto без фиксированной высоты | Правильно: ставить flex/items-center/justify-center на самом overflow-y-auto контейнере через :style binding, или использовать min-height: calc(100vh - headerH) с точным значением из getBoundingClientRect.

[2026-03-17] Бэкенд падал при импорте get_current_user | from ..services.auth_service import get_current_user → ImportError | get_current_user живёт в dependencies.py, не в auth_service.py. Всегда: from ..dependencies import get_current_user.

[2026-03-17] Автоматический переход stage 1→2 при пустом брифе | Проверяли только EXISTS записи в brand_profiles, не заполненность | Проверять минимум 3 заполненных поля: brand_name, niche, description, target_audience, usp. Пустая запись ≠ заполненный бриф.

---

## 2026-03-18

[2026-03-18] Samsung screencap в Settings/Add Account | screencap возвращает чёрный 20KB файл при открытом экране аккаунта | Samsung блокирует screencap на security-экранах (osp.app.signin). Решение: Chrome браузер — screencap работает нормально. Никогда не регистрировать через Settings→Add Account если нужен visual feedback.

[2026-03-18] account_factory.py без --task-id генерирует новые данные | При запуске --step instagram без task-id создался новый аккаунт вместо использования Gmail Даши | Всегда передавать --task-id когда продолжаешь регистрацию существующего аккаунта. Без task-id → новый аккаунт с нуля.

[2026-03-18] factory_reg_tasks.account_id был NULL | task_id=21 не имел account_id → JOIN не нашёл данные → скрипт сгенерировал новые | При создании task через CLI без --task-id: account_id пишется сразу. При ручном INSERT в factory_reg_tasks: не забывать account_id.

[2026-03-18] Caddy try_files не работает для .html расширений | try_files {path} {path}.html не помог для новых страниц систематики | Caddy в file_server mode: файл должен быть либо директорией с index.html, либо иметь .html в URL. Решение: mkdir /var/www/systematika/page-name && cp file.html index.html. Работает стабильно, не зависит от конфига.

[2026-03-18] onlinesim.io недоступен с нашего сервера | curl timeout — GeoIP блок или Cloudflare | Сервис недоступен напрямую с fra-1-vm. API ключ проверять и использовать только с телефонов через ADB, не с сервера напрямую.

---

## 2026-03-19

[2026-03-19] Запись данных без визуального доказательства | register_social.py отработал успешно (exit 0), но скриншотов профилей не сделал → пришлось доказывать через логи и ручное открытие | Любой скрипт регистрации должен делать screencap после завершения каждой платформы. Без скриншота = нет доказательства. Добавить screenshot-шаг в register_social.py.

[2026-03-19] DB save error в SocialRegistrar | SocialRegistrar наследует AccountFactory но не инициализирует account_id → _save_to_db падает с AttributeError | SocialRegistrar должен принимать account_id как параметр __init__ и писать UPDATE, не INSERT. Или использовать factory_reg_accounts напрямую по gmail.

[2026-03-19] Прописал пути на CH сервере агенту на Систематике | Обновил SOUL.md Елены с путями /root/.openclaw/workspace/... — это CH сервер, а не Систематика | Перед правкой SOUL.md: всегда проверять НА КАКОМ сервере живёт агент. workspace-elena на Систематике ≠ workspace-elena на CH.

[2026-03-19] Копировал фреймворк не туда | Скопировал elena-hr v1.0.0 на CH в /workspace/projects/frameworks/ — но это была старая версия без данных | Правильный фреймворк жил в /workspace-dzheff/systematika/frameworks/elena-hr/. Не делать поверхностный поиск — проверять мета-файл manifest.json и дату.

---

## 2026-03-20

[2026-03-20] Проверка SSH данных из БД без верификации | Вписал фиктивный IP 45.82.14.135 в тестовые данные | Тестовые записи в БД должны содержать реальные доступы. Фиктивный IP → SSH падает → офис показывает offline для всех агентов. Всегда проверять SSH подключение перед записью в БД.

[2026-03-20] Caddy роутинг с regex перехватывает /api/* | @client path_regexp отправлял API запросы на client-office | При написании Caddy matcher'а для /{token} — проверять что /api/agents, /assets/ и корень / не попадают под тот же матч. Тестировать curl'ом ДО деплоя.

[2026-03-20] Суммирование накопительных токенов | $10.58 вместо реальных $0.53 | totalTokens в JSONL = накопительное значение за сессию. Суммировать нельзя. Нужно брать одно максимальное значение per файл. Диагностика: если токены в 10x раз больше ожидаемого — скорее всего суммируется накопительное.

[2026-03-20] Изменение server.js без проверки Caddy | Долго патчил server.js пока проблема была в Caddy | Перед изменением сервера: проверить что Caddy вообще проксирует на этот порт. `curl -I https://domain/path` → смотреть `via: Caddy` и статус. Если 404 от Caddy — это не проблема сервера.

[2026-03-20] Запуск правильного server.js с неправильными зависимостями | office-3857 падал — нет node-ssh/pg | После копирования директории на новый путь: `npm install` обязателен, зависимости не копируются сами. Симптом: `MODULE_NOT_FOUND` в pm2 logs.

---

## 2026-03-22

[2026-03-22] Токен клиента в БД не сохранился после перезапуска | БД на Систематике через SSH туннель к CH — при перезапуске туннеля данные остаются на CH, запись в другом месте | При работе с мультисерверной БД: всегда проверять на каком сервере реально хранятся данные и туда же писать. Токен Алёны писался на Систематику но читался с CH — несовпадение.

---

## 2026-03-24

[2026-03-24] Спрашивал пользователя вместо диагностики | «Это старый чат?», «Бот добавлен?» — потратил час и получил раздражение | Перед любым вопросом пользователю — проверить всё что можно самостоятельно. Сессии, конфиг, getChat, grep по JSONL. Вопрос задавать только если данных реально нет.

[2026-03-24] Изменил БД без понимания контекста | Поставил onboarding_stage=5 проекту 78 думая что помогаю | Скриншоты — это описание задачи, не инструкция к действию. Сначала уточнить что нужно сделать, потом трогать БД.

[2026-03-24] autowarm: publish_tasks — отсутствующие колонки (повтор) | column pt.pre_warm_protocol_id does not exist | Уже был в MEMORY.md. Это повторяющийся баг при разворачивании. Нужно добавить проверку в скрипт деплоя.

---

## 2026-03-25

[2026-03-25] Проверял OAuth токен как API key | curl с x-api-key header → 401, сделал вывод что токен мёртвый | OAuth oat01 токены проверяются через `Authorization: Bearer` + `anthropic-beta: oauth-2023-11-15`. Стандартный x-api-key header для них не работает. Всегда уточнять тип токена перед проверкой.

[2026-03-25] Убрал рабочий токен из ротации на основе неверной проверки | sys100-2 убирал/возвращал трижды | Сначала понять механизм (OAuth vs API key), потом делать выводы. Одно неверное действие + откат = дважды потраченное время.

---

## 2026-03-27

[2026-03-27] Конфиг перезаписывался после фикса | Исправил allowlist→allowAgents, через минуту вернулось | Когда конфиг возвращается к старому значению — значит кто-то его перезаписывает. Нужно проверять кто: lsof, inotifywait или grep по логам что писало файл. Не фиксить повторно не разобравшись в причине.

[2026-03-27] Новый агент добавлен с неверным форматом конфига | garry: token вместо botToken, allowlist вместо allowAgents | При добавлении нового агента вручную — всегда проверять валидность конфига запуском gateway на тестовом порту ПЕРЕД перезапуском основного.

---

## 2026-04-01

[2026-04-01] Не выяснил причину 401 до начала фикса | Сразу предположил что ключ Anthropic мёртвый | Всегда сначала проверить endpoint curl'ом с валидным токеном. Если 200 — ищи проблему на клиентской стороне, не в ключе.

[2026-04-01] Оставил открытый вопрос по БД Gmail без ответа | Два варианта (мигрировать БД vs переписать скрипт), Роман не ответил, ушли на другую задачу | При блокирующем вопросе — задать один раз чётко и не уходить на другие задачи пока нет ответа. Иначе задача висит открытой.
