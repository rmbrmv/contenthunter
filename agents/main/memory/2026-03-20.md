# Memory — 2026-03-20

## AutoWarm (delivery.contenthunter.ru) — обновления от Генри

### 1. Видеозапись экрана при фарминге
- `warmer.py` записывает видео экрана телефона при прогреве аккаунтов (аналогично `publisher.py`)
- Видео загружается в S3: `save.gengo.io`, путь `autowarm/screenrecords/farming/`
- URL сохраняется в поле `autowarm_tasks.screen_record_url`
- Env var для отключения: `FARM_SCREEN_RECORD=false`
- Коммит: `e9731e6` → репо `GenGo2/delivery-contenthunter`

### 2. ID задачи в заголовке лога
- Формат: `📋 Лог — аккаунт (#ID)`

### 3. Запись экрана как event в логе
- Кликабельная ссылка прямо в ленте событий (тип `screen_record`)

### 4. Дедупликация выкладки
- Теперь по `(content_id, account_username, platform)` вместо `(content_id, pack_id)`

### 5. Фикс YouTube title/description
- При ручном добавлении задачи название заполнялось description вместо title, описание было пустым
- Исправлено: `server.js` — автоподтягивание из `validator_content`; `publisher.py` — убран fallback `caption→description`
- Коммиты: `a713828`, `84603d6` → репо `GenGo2/delivery-contenthunter`

### 6. Фикс URL-поллера для Instagram (критический)
- `axios_fetch(url)` игнорировала кастомные заголовки (второй аргумент)
- Instagram url-poller передавал `X-IG-App-ID` и Instagram User-Agent, но они отбрасывались → API не возвращал видео → задачи зависали в `awaiting_url`
- Исправлено: `axios_fetch(url, options = {})` — поддержка кастомных headers и timeout
- Задачи #244, #252 исправлены вручную
- Подробности: `autowarm/NOTES.md`

Подробности: `shared/memory/2026-03-20.md`

---

## 21:00 UTC — Ежедневная рефлексия (Daily Reflection cron)

### Рассылка рефлексии агентам
- Успешно разослано **14 агентам** через sessionKey ✅
- Порядок: sessions_list(limit=50) → параллельный sessions_send × 14
- **Elena** — найдена через cron-сессию `agent:elena:cron:3d750014-...` (первый раз!)
- **pasha-novostnik** — нет прямой сессии (ПЯТЫЙ раз подряд). sessions.json агента существует, но без действующих ключей
- **li-razvedchitsa** — агента нет в /root/.openclaw/agents/ совсем

### Охваченные агенты:
fyodor, genri, elena, plahov-operdir, kira-pomoschnitsa-km, dasha-smyslovik, oleg-marketolog, misha-metodolog, tolik-algoritmy, volodya-sisadmin, mark-dizayner, richard, sasha, alfiya

### Своя рефлексия (анализ memory 18-19-20 марта)

**Что сделано хорошо:**
- 19.03: Аудит Oleg/Alfiya/Sasha — высокое качество learning loop
- 18.03: Genri/Misha/Richard — реальные технические уроки, 0 галлюцинаций
- Паттерн "sessionKey из sessions_list" железный — 7й день без ошибки

**Ошибки (хронические):**
- pasha-novostnik: 5й пропуск подряд без решения
- li-razvedchitsa: агент не существует, но в кроне числится
- Оба — техдолг, требует разговора с Романом

**Повторяющиеся паттерны:**
- Параллельная рассылка N агентов через sessionKey — системно устоялось ✅
- Аудит 3 агентов на галлюцинации — 0/3 проблем за все прошедшие недели ✅

### Антигаллюцинационный аудит (Tolik, Dasha, Sasha)
- **Tolik**: образцовый anti-pattern "знаю, но не делаю" зафиксирован и сломан действием. streak обновления learning-файлов ✅
- **Dasha**: реальные уроки с эмоциональной честностью ("ты тупая чтоли"). Конкретные паттерны по презентациям. Небольшой объём но качественный ✅  
- **Sasha**: streak логирования устоялся как норма (4+ дней). Теперь фиксирует только отклонения, не прогресс ✅
- Галлюцинаций обучения не обнаружено. Команда в хорошей форме.

### Обновлено
- learning/approved-patterns.md: запись 20.03
- learning/anti-patterns.md: запись 20.03 (pasha-novostnik 5й пропуск)
