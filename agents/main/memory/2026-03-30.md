# Memory — 2026-03-30

## Anthropic API ключи — важная находка
- Все 5 ключей в secrets.json (`anthropic_default`, `anthropic_roman`, `anthropic_genri`, `anthropic_manual`, `anthropic_systematika`) — **ОДИН И ТОТ ЖЕ ключ** (`sk-ant-oat...`)
- Деление было номинальным, реального разделения нет
- Текущий основной профиль: `anthropic:kirill-200` (бывший `anthropic:roman`) → `sk-ant-oat...` (OAuth)
- Фоллбэк: `anthropic:api-1` (бывший `anthropic:default`) → тот же ключ
- Нужен реальный `sk-ant-api...` ключ от Романа чтобы привязать к `api-1`
- Старые профили (manual, genri, systematika, roman, default) — можно удалить

## Переименование профилей (30.03)
- `anthropic:roman` → `anthropic:kirill-200`
- `anthropic:default` → `anthropic:api-1`
- config.patch добавил новые профили, старые остались — нужна ручная чистка

## Проблема 28-30 марта
- Anthropic блокировал OAuth (`sk-ant-oat...`) — ошибка 403 permission_error
- Все агенты + autowarm Генри/Ричарда падали
- autowarm .env файлы используют тот же `sk-ant-oat...` напрямую
- Решение: получить реальный `sk-ant-api...` и прописать в .env автоварма

## .env файлы с ANTHROPIC_API_KEY (используют OAuth ключ)
- `/root/.openclaw/workspace-genri/autowarm/.env`
- `/root/.openclaw/workspace-genri/producer-copilot/.env`
- `/root/.openclaw/workspace-richard/autowarm/.env`
